# === Secrets(絕不 commit)===
.env
.env.local
.env.*.local
# `.env` 嘅備份 —— 2026-08-20 加。改 `.env` 之前備份係啱嘅做法,但備份出嚟嗰個檔
# 同原檔一樣係真 secret,而上面三條**一條都唔會擋佢**(`.env.bak.xxx` 唔 match
# `.env.*.local`)⇒ 實測 `git check-ignore` exit 1、`git status` 見到佢 `??`。
# 📌 更好嘅做法係根本唔喺 repo 入面備份;呢條係嗰個習慣失手嗰陣嘅安全網。
.env.bak*
.env.*.bak*
# Azure deployment 真值 params 檔（含 DB 密碼 / API keys / JWT / admin 初始密碼）——
# 絕不 commit;只 commit template aca.json + 無值範本 aca.params.example.json。
deploy/azure/*.params.*.json
!deploy/azure/aca.params.example.json

# === AI agent 本機工具 ===
# .claude/settings.json + commands/skills/hooks 團隊共享(track);只 ignore 本機 override。
.claude/settings.local.json
.claude/scheduled_tasks.lock
# .claude/          # 若唔想 track 成個 .claude/,uncomment
# .agents/

# === 依賴 / build（NestJS / TypeScript）===
node_modules/

# === 真實 license 參考數據（H4 敏感 · 唔入 repo）===
docs/06-reference/02-doc-sample/*.xlsx

# n8n workflow 匯出（SEC-001）。原始匯出曾含明文 WinRM 服務帳號密碼;該處已 scrub,
# 但呢啲檔仍然帶住內部 email、AI system prompt、基建 hostname 同 n8n credential id,
# 所以預設唔入 repo。要收返入 repo = 有意識嘅決定(owner 拍板 → 刪呢兩行),
# 唔應該靠 `git add .` 順手發生 —— 一 commit 就永遠喺 history。
# 真相 SSOT 係 n8n instance 本身;呢度只係 read-only 參考副本。
docs/06-reference/03-n8n-workflow/
dist/
build/
coverage/
*.tsbuildinfo

# === Prisma ===
# generated client 落 node_modules;若改 output path 記得同步 ignore

# === 本機雜項 ===
*.log
npm-debug.log*
.DS_Store
Thumbs.db
# Word / Excel / PowerPoint 開住個檔嗰陣嘅鎖檔(~$xxx.docx)——
# 會喺 docs/ 出現,唔屬於 repo,亦唔應該連同 doc 改動一齊 add 落去。
~$*
# .claude/skills/docgen/ 嘅 Python helper 跑完會留低 bytecode cache。
__pycache__/
*.pyc

# === Playwright MCP 本機截圖 / snapshot artifact（唔入 repo）===
.playwright-mcp/
