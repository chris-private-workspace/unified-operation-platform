**Tooltip** — dark hover chip, short delay. Wrap icon buttons or compact cells.

```jsx
<Tooltip label="RHK: 60 allocated · 54 assigned">
  <span style={{fontFamily:'var(--font-mono)'}}>6</span>
</Tooltip>
```

Keep labels to one short line. Default placement `top`; use `bottom` near the viewport top.
