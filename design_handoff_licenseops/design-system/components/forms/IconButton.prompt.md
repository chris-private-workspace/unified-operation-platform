**IconButton** — square, bordered, icon-only control for the top bar and toolbars.

```jsx
<IconButton title="Toggle theme" onClick={toggle}><SunIcon/></IconButton>
```

Default 34px. Set `active` for a pressed/selected look (uses `--active`).
