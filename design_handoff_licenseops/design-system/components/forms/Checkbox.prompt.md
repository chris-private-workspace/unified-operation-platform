**Checkbox** — accent checkbox, optional label. Used for notification prefs and n8n event toggles.

```jsx
<Checkbox checked={on} onChange={e=>setOn(e.target.checked)} label="License assigned" />
```
