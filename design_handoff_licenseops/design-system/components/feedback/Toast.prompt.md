**Toast** — bottom-center confirmation chip (always dark). Fire after operator actions.

```jsx
<Toast message="License assigned to user" tone="ok" />
```

Keep messages terse and past-tense. The parent clears `message` after ~2.6s.
