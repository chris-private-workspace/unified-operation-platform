---
artifact: risk-register
version: 1.1
status: living
last_updated: 2026-08-19
---

# Unified Operation Platform — Risk Register(living)

> **用途**:項目風險嘅 living 登記。新風險 / status update 入呢度。
> **更新 trigger**:新風險識別 / bug postmortem 揭出新 pattern(PROCESS §4.4,Sev1/2)/ mitigation 狀態變 / 定期 review。

**狀態圖例**:🟢 Resolved · 🟡 Mitigating(部分緩解)· ⚠️ Open(未緩解)· ⚫ Accepted(接受風險,無 active 緩解)
**Severity 圖例**:🔴 Critical · 🟠 High · 🟡 Lower

---

## 1. Risk Index(at-a-glance)

| ID | Risk | Source | Likelihood | Impact | Mitigation | Status |
|---|---|---|---|---|---|---|
| R1 | 公司 proxy 阻擋 `binaries.prisma.sh`(Prisma engine CDN)→ generate/migrate/seed/boot 卡住 | W01 執行(2026-07-09) | High(已發生) | 🔴 High(阻 backend runtime) | **Workaround 已用**:流動網路跑一次 generate/migrate → engine cache 落 `node_modules`,返公司網即用本機 binary。長遠靠 IT allowlist `*.prisma.sh`。⚠️ clean reinstall(刪 node_modules)前需再轉流動網路。 | 🟡 Mitigating |
| R2 | `AUTH_DEV_BYPASS=true` 誤帶入 production → 繞過 JWT 驗證、注入 seed ADMIN(權限完全繞過) | W09 AUTH-1(2026-07-10) | Low | 🔴 High(權限繞過) | 預設 `false`;開啟時啟動打 warning log;prod path `getOrThrow(ENTRA_TENANT_ID/ENTRA_API_AUDIENCE)` 未設即 boot 失敗(fail-fast)。ADR-0002 記錄。部署時確保 `.env` 無此 flag。 | 🟢 Resolved(Mitigated) |
| R3 | n8n on-prem AD → Entra Connect sync 延遲:push 帶 `azureSyncedAt` 但平台即刻 `findUser(upn)` 仲搵唔到 → assign fail | W24 AGENDA A4(2026-07-15) | Med(on-prem 常態) | 🟡 Lower(assign 短暫 fail,可 retry) | assign 端唔純信 `azureSyncedAt` timestamp,以 `findUser` 真命中為 gate;命中前 retry / 留 queue(D2 approach)。DESIGN §7 sync 時序註。<br>⚠️ **校正(2026-07-21,W31)**:ADR-0010 §7.2 講 item 6「解 RISK R3」係**錯引用**。W31 / ADR-0011 解嘅係 **outbound 提交失敗**(`audit-and-integration-observability.md §2.4` 第 5 點「冇交付保證」),同呢條 sync 延遲致 assign fail **唔係同一件事** —— `OutboundFailure` 三個 kind 都唔涵蓋 assign 路徑。**本條完全不受 W31 影響。**<br>✅ **Mitigation 路線已定(2026-07-26)**:**ADR-0015 Accepted** —— 排程 sync sweep 主動向 Graph `findUser` 證實,命中即開 gate 並寫 `RequestEvent(SYNC)`。呢個係本條 mitigation 欄「命中前 retry / 留 queue(D2 approach)」嘅落實,但改為**平台主動輪詢**而唔係綁喺人手 retry 上(理由:本風險嘅本質係「唔知等幾耐」,retry 解唔到「幾時可以開始」)。副產品:`RequestEvent` timeline 會記錄實際 sync 延遲,可以答「Entra Connect 真實延遲幾耐」。 | 🟡 **Mitigating**(**W37 已實作 + live 驗證**,2026-07-27)—— `SyncSweepService` 每 10 分鐘主動 `findUser` 證實;live 用真 tenant 帳號驗到 gate 自動開(seed → 下一個 tick → `azureSyncedAt` 填 + `RequestEvent` 寫「verified against Microsoft Graph」),同輪其餘唔合資格嘅單冇被誤掃,kill switch A/B 亦驗過。**未完全 Resolved 嘅殘留**:① 平台仍然只係**輪詢**,最壞情況要等一個 10 分鐘週期(webhook = ADR-0015 明文保留嘅升級路徑,要新 ADR)② UPN 打錯 / 帳號已刪嘅單 30 日後放棄,之後仍要人手處理 ③ 多實例部署會重複跑(現 UAT 單實例;ADR 明文 YAGNI 唔預先解) |
| R5 | **外部系統回傳嘅字串被當成安全內容記錄** —— vendor(Graph / ServiceNow / n8n)嘅 error message、response body、echo 返嘅 payload,內容**唔喺我哋 code 入面睇得到**,所以 code review 幫唔到手;而平台已知會送 UPN 出去(outbound create 個 `short_description`、Graph `/users/{upn}` path)⇒ 一旦 vendor 喺錯誤訊息裡面引用返請求,PII 就靜靜入咗 plaintext log | **BUG-007 postmortem 觸發點**(2026-07-28)—— BUG-004 postmortem 明文寫「**若果第三次出現同類洩漏,就應該升級成 register 一條『外部字串處理』嘅 risk**」。數:BUG-001(自己格式化)→ BUG-004(Graph message)→ BUG-007(SN response body)= **第三次** | **Med**(已發生三次;每次都係新接一個 vendor / 新一條路徑時重犯) | 🟡 Med(H4 policy violation;log 可能被轉發 / 備份 / 畀冇必要知嘅人睇。唔係外洩畀第三方) | ① **共用 `scrubPii()`**(唔准每處自寫 regex)—— 已用喺 4 個 Graph identity 路徑 + `ServiceNowService.request()` ② **凡 assert「冇 PII」嘅 test 必須 spy logger**,唔可以只 assert exception message(BUG-004 就係咁匿咗 18 日) ③ **接新 vendor / 加新 log 點嗰陣要逐個 caller 查證 path 同 payload**,唔可以憑「呢類唔涉及 user」呢種斷言劃線(BUG-007 正正揭穿 BUG-004 呢個劃線理由唔準確) ④ 🔴 **已知未覆蓋**:`scrubPii()` 只捉 email 形狀;非 email 格式嘅識別碼(員工編號 / 電話 / sys_id 對應嘅人)冚唔到,而**刻意唔擴闊 regex** —— 會食走 AADSTS 碼呢啲我哋 log 呢段文字嘅唯一原因 | ⚠️ **Open**(技術緩解只覆蓋 email 形狀 + 已知路徑;結構性傾向仍在 —— 每次接新 vendor 都要人記得) |
| R4 | **`allocatedQuantity` 由「顯示數字」變成「會擋人嘅 gate」,但冇任何自動流程令佢跟上現實** —— 買咗 licence 唔會加 allocated ⇒ 完成採購之後 assign **仍然被擋**;操作員嘅最短路徑係搵 ADMIN override,而唔係改 allocated。長期演化 = override 變日常、allocated 完全失去意義、gate 名存實亡 | W36 / ADR-0016 上線(2026-07-27);plan §5 OQ2 + §6 R2/R4 | **Med-High**(唔係會唔會,係「幾時」——除非有人被指定負責) | 🟡 Med(gate 失效 = 退回 W36 之前嘅狀態,但多咗一堆 override audit 噪音) | ① runbook `OPCO-BUDGET-GATE-ROLLOUT.md` 明文寫死斷點 + 兩條出路,並指出「唔指定人就冇人做」 ② 前端 override dialog 直接寫住「買 licence 唔會加 allocation,請去 License assets 改」 ③ **監察手段 = `/admin/audit` filter `assign.budget_override`**(獨立 action 就係為咗呢個而設,ADR-0016 R4)—— **要定期睇,唔睇就等於冇** ④ 🔴 **三項全部係程序性,零技術強制** ⇒ 自動化 = **新 ADR**(會掂 ADR-0004 Excel↔平台 SSOT 未解張力),唔可以喺實作裡面偷偷加 | ⚠️ **Open**(mitigation 全屬程序性;要收窄需 ① 指定負責人 ② 定期查 override 次數 ③ 或解決 allocated SSOT) |
| R6 | **平台寫落 ServiceNow catalog task 嘅 work note 係 write-only** —— `work_notes` 係 journal input field,Table API GET 永遠返空,真正內容喺 `sys_journal_field`,而 integration account 對嗰張表**一行都讀唔到**(2026-07-31 實測:唔限 element_id 都返 0 行)。同一個 PATCH 送 `state` + `work_notes` + `assigned_to`,ServiceNow 完全做得到**收 state 而靜靜 drop work_notes**(field-level ACL),兩種情況喺平台睇嚟一模一樣:同一個 200、同一個 `updated`、`ticketHeldAt` 照寫、`OutboundFailure` 佇列照樣 0 行 | **REQ0044038 HOLD 路徑 live 驗證**(2026-07-31)—— 驗 note 內容時發現讀唔到,再 probe 先知係 ACL 而唔係「冇寫」 | **未知(冇能力量度)** —— 呢個就係風險本身:唔係「大機會出事」,係「出咗事都唔會有人知」 | 🟡 Lower-Med(**state 先係語意載體**,SN 流程靠 state 推進,已獨立驗到真係 1→2;note 係畀人讀嘅上下文 ⇒ 丟失唔會令流程停,但人手接手時會見到 task 變咗 In Progress / Closed 而**冇任何原因**。CLOSE 路徑影響大過 HOLD:「licence 已 assign」呢句冇咗,下手會以為冇做過) | ① 🔴 **目前零技術緩解** ② 唯一驗證途徑 = **人手開 SN 望** ③ ops script `intake-from-servicenow.ts --notes` 已加 **readability probe**:空結果之下會再問一次「呢張表畀唔畀讀」,所以佢**唔會**幫你確認 note 寫咗,佢嘅唯一價值係**阻止把「讀唔到」誤讀成「冇寫」**(第一次跑就差啲中招)④ **defer 追 journal read 權限**(要 ServiceNow admin,同 n8n 三接縫嗰批接線一齊開較有效率)⑤ ⚠️ **`N8nTicketProvider` 路徑同樣中招,而且更盲** —— workflow 2004 用 `neverError`,SN 拒絕都返 200<br>🔴 **⬆️ 2026-08-04 加一個 consumer(W43 / ADR-0026 D2)**:gate ② 開閘時寫嘅 `target_user` 更正 note 行**同一條路**,所以同樣中招。⚠️ 順帶校正一句話:ADR-0026 講 work note 路徑「**已證實寫得到**」—— 精確講,CH-010 證實嘅係**個 PATCH 唔會 403**(對比 `sc_item_option` 一定 403),**唔係**「note 一定 land」。本條就係嗰個分別。即使如此,選 work note 仍然係啱嘅:一條**可能**寫得到嘅路,好過一條**已證實一定唔得**嘅路 | ⚠️ **Open**(同 R5 / DD-4 同族:平台側信號全綠 ≠ 對面真係收到) |
| R7 | **UOP 同 n8n 共用同一個 ServiceNow integration account(`n8napiservice1`)** —— ⇒ `sys_updated_by` / `assigned_to` **永遠分唔到邊個系統做過乜**。後果唔止「查唔到」:CH-020 / ADR-0024 **D5 個 rationale 就係咁記錯**(以為 SCTASK0071807 被人手閂,實情係 UOP 自己喺驗證期間閂嘅),而嗰個誤判一路傳到成份 ADR 嘅前提。唯一可靠指紋係 **`close_notes` 文字**,而佢係**慣例唔係機制** —— 兩邊改一改 wording 就冇咗 | W43 F0 調查(2026-08-03)—— 實測 `SERVICENOW_USER === 'n8napiservice1'` = true | **High(已係現況)** | 🟡 Med —— 唔影響功能正確性,影響**事後查證同歸因**。而歸因錯咗會令架構決定建喺錯前提上(ADR-0024 就係活例) | ① 目前唯一手段 = **`close_notes` 指紋**,UOP 側文案要保持同 n8n 唔同(`… assigned via platform.` vs `Closed & Handled by n8n`)② 🔴 **查 SN 側「邊個做」嗰陣一律唔可以信 `sys_updated_by`**,要睇 `close_notes` / journal ③ 正解 = **同 SN admin 開一個 UOP 專屬 account**,同 ADR-0018 OQ-2 嗰個 least-privilege 缺口、同 DD-5 嘅 `sc_item_option` 寫權**一齊傾**(三件都係「同 SN admin 要嘢」)④ 未攞到之前,凡靠 SN 側痕跡推論行為嘅結論,**必須寫明個指紋係咩** | ⚠️ **Open**(零技術緩解;靠慣例) |
| R8 | **外部憑證到期會靜靜失效,而我哋冇任何機制知道幾時到期** —— Entra client secret 過期之後,受影響嘅呼叫全部返 401/403,但症狀係「突然登入唔到」/「對帳突然唔 work」,**冇任何嘢會指向「憑證過期」**。🔴 更要緊嘅係:我哋**只知道其中一個嘅到期日**。SSO app(`08fa14bf-…`)infra 明講 **2028-07-28**;而 **Graph app(`27d329e5-…`,`App-N8N-LicenseManagement`)嘅到期日我哋唔知** —— 查佢要 `Application.Read.All`,而我哋兩個 SP 都冇。**licence assign / 對帳 / drift 全部行呢個 app** | W44 F9(2026-08-06)—— infra 交 SSO app registration 時附帶 expiry;順帶發現 Graph 嗰個從來冇人問過 | **確定會發生**(唔係機率問題,係日曆問題) | 🔴 High —— Graph 嗰個一過期,**assign / 對帳 / drift 全線停**,而第一個症狀會出現喺 operator 面前而唔係 log 入面 | ① **已知日期入 doc**:SSO app = 2028-07-28(`09-dev-as-built.md` B9 節)② 🔴 **未做:向 infra 問 Graph app(`27d329e5-…`)嘅 secret expiry**,並要求兩個都喺到期前 60 日通知 ③ 🔴 **零技術偵測** —— 平台唔會喺過期前提醒,亦唔會喺過期後講明原因。要做嘅話係一個新 deliverable(例如 boot 時 decode 一次 token 睇 `exp`,或者定期 probe)④ 中短期唯一實際手段 = **喺 calendar 落兩個提醒**,而唔係靠人記得 | ⚠️ **Open**(一個日期已知、一個未知;零技術緩解) |
| R9 | **監控面講嘅嘢同 runtime 實際做緊嘅唔同** —— Settings › Integrations 個 panel 係操作員判斷「licence 而家由邊個派 / ticket 由邊個閂」嘅**唯一畫面**,而佢兩次都講錯過。🔴 **關鍵係佢從來冇「錯到睇得出」**:panel 答得出一個**言之成理**嘅狀態,而嗰個狀態嚟自一個**同 runtime 唔同嘅來源或者唔同嘅時刻**。冇 error、冇 log、冇 failing test —— 唯一嘅偵測方式係有人**同時**知道 panel 講乜同 runtime 做乜,而呢個正正係個 panel 存在嘅原因 | **BUG-011 postmortem 提早升級**(2026-08-10,Chris 拍板)。🔴 **冇跟 R5 個「第三次先升級」先例**,理由:R5 三次係散落三個唔同 vendor 路徑,而呢兩次**出喺同一個 panel、同一組 seam、同一個 method**(`n8nLicenseSelected()` 呢一行本身就係 BUG-005 個修法)—— 同一個位置連續錯兩次,第三次唔值得等。**數**:BUG-005(panel 讀 env / runtime 讀 DB)→ BUG-011(panel 讀**當下** DB / runtime 讀 **boot 嗰陣**嘅 DB) | **Med** —— 每次改動 provider 解析方式(加 seam / 改 resolve 時機 / 加 cache)都有機會重犯,而三個 seam 已經係三倍表面積 | 🟡 **Med-High** —— 唔影響功能正確性,影響**操作員對系統行為嘅認知**。後果係「唔會去 n8n 查一個 n8n 造成嘅故障」,或者「以為已經轉返 Graph 而其實未」。BUG-005 個 code comment 講得最準:**「a monitoring surface that contradicts the runtime is worse than no surface」** | ① **BUG-005 留低嘅規則要連埋『幾時』一齊講**:唔止「問 runtime 問嗰個 call」,而係「**答案要嚟自 runtime 真正用緊嗰一刻**」。BUG-011 嘅 `SeamRuntimeRegistry` 就係咁做 —— 記低 boot 決定,唔係再 resolve 一次 ② **凡 panel 顯示嘅嘢有『生效時機』,一定要顯示得出『未生效』呢個狀態**(`pendingRestart`)—— 一個只有兩態嘅 badge 冇能力誠實 ③ 🔴 **記 effective 值,唔記 raw 配置值** —— 每個 seam 都 fail-safe,顯示 raw 會把一個從未生效嘅 typo 講成 runtime 狀態 ④ **加新 seam / 改 resolve 時機嗰陣,`pendingRestart` 要一齊加**;`integration-status.service.spec` 有一條由 `CONNECTOR_KEYS` derive 嘅覆蓋 test,但佢**只覆蓋 shape 唔覆蓋時機** ⑤ 🔴 **已知未覆蓋**:呢條係**設計紀律唔係技術強制** —— 冇任何嘢阻止下一個 panel 欄位再犯,因為「講錯」睇落同「講啱」一模一樣 | ⚠️ **Open**(BUG-011 修好咗三個 seam 嘅具體實例,但結構性傾向仍在) |
| R10 | **叫做「DEV」嘅環境對真 production M365 tenant 有寫權** —— `RG-RAPO-UOP-DEV` 個 `GRAPH_TENANT_ID` 就係公司 M365 tenant(`d1ea071a-…`),`GRAPH_CLIENT_ID` 同本機 `.env` **逐字一致**,而 Graph app 有 **`LicenseAssignment.ReadWrite.All`** ⇒ **喺 DEV 撳一次成功嘅 assign = 真派一個 licence 畀一個真人**,同喺 production 撳冇分別。🔴 **危險唔喺「有寫權」,喺「個名叫 DEV」** —— 環境名本身就係一個會令人放鬆嘅信號,而呢個環境入面**真係有真人資料**:2026-08-13 唯讀探測數到 9 條 line item 全部 `RAPO/IT`,其中 **3 條 `READY` 而且兩個 sync gate(`azureSyncedAt` / `serviceNowUserSyncedAt`)都已經開咗** ⇒ 撳落去**直達 Graph**,只剩 budget 閘一道 | W44 `F6-14` 執行前嘅唯讀探測(2026-08-13);前置事實由 W45 live-verification 分析(2026-08-11)確立 | **High** —— 唔係「會唔會有人撳」,係「呢個環境設計上就係畀人撳嘅」。每一次 live 驗證都係一次機會 | 🔴 **High** —— 後果係一個真人**真收到**一個佢唔應該有嘅 licence(兼 seat 被佔、ledger 被寫、audit 有紀錄),而且**冇 undo 掣** —— 要另外行一次移除 | ① **已生效嘅操作紀律**:喺 DEV 撳任何 assign 之前**先唯讀探測 gate 狀態**,揀一條**兩個 sync gate 都 null** 嘅 line item ⇒ **結構上到唔到 Graph**(2026-08-13 `F6-14` 就係咁做:改用 `REQ0043934`,擋喺 `sync-azure`)② 🔴 **唔好靠 budget 閘做安全網** —— 佢係**業務規則**唔係**安全邊界**,`budgetOverrideReason` 一送就過 ③ 🔴 **`sync-check` 返 `FOUND` 證明唔到個 user 存在**(2026-08-12 硬對照:同一 UPN 同一分鐘,`sync-check` `FOUND` 而真 assign 個 `directory` 閘返 not found)—— 探 UPN 存唔存在唯一可靠方法係直接打 Graph `/users/{upn}` ④ **正解 = 用 ADR-0017 個 `LicenseOperationsProvider` seam**:DEV 應該切去一個唔會真寫嘅 provider,或者用獨立 tenant。**seam 已經喺度,冇人切過** ⑤ 未切之前,凡計劃「喺 DEV 驗 assign 成功路」嘅工作,**必須喺計劃階段就講明點樣避免真派**,唔可以臨撳先諗 | ⚠️ **Open**(靠操作紀律;技術手段[seam 切換]存在但未用) |
| R11 | **`AgentMessage` 載 PII 兼永久增長** —— transcript 係 model 講過嘅嘢,而 `get_request` **必須**原文回傳 `rawRequestText`(scrub 咗就係交白卷,ADR-0037 E6)⇒ 落庫嗰刻先 scrub,而 `scrubPii` **只捉 email 形狀**(同 R5 同一個已知缺口)。ADR-0036 D6 明文寫住呢個係**知情決定**唔係疏忽:transcript 冇咗就查唔到 agent 點解咁提議 | W46 plan §6(2026-08-15) | **High**(每個 run 都會發生) | 🟡 Med —— 唔外洩,但 DB 入面永久累積可識別文字 | ① `scrubPii` 落庫前,**一個 exit point**(`toTranscript`)② ADMIN-only 讀 ③ 🔴 `getRun` 用明文 `select` 而**唔用 `include`** —— `runState` 載住 model 未 scrub 嘅 message history,`include` 會把佢一齊出街(一個冇 error 冇 log 嘅洞,由較短嗰個寫法開出嚟)④ ⚠️ **零 retention policy** —— 「永久」係字面意思 | ⚠️ **Open**(知情決定;retention 未定) |
| R12 | **Tool allow-list 蠶食** —— agent 見到嘅 tool 就係佢做得到嘅嘢(ADR-0036 D2:「見唔到」比「叫佢唔好用」強一個數量級)。加一個 tool 係一行 code,而佢擴闊嘅係 blast radius | W46 plan §6(2026-08-15) | Med(每次加功能都有壓力) | 🔴 High(一個 read tool 變 write tool = agent 由建議者變執行者) | ① §3 張表 = **ADR 級**,改佢要新 ADR ② `tool-registry.spec.ts` `exposes exactly these tools, in this order, with these approval flags` —— **多一個少一個都紅** ③ `agent.boundary.spec.ts` assert registry **零 DB write** | 🟢 **Mitigated**(test 鎖死) |
| R13 | 🔴 **Proposal 被 rubber-stamp** —— ADR-0036 D3 擺一個人喺每個寫入前面,而**嗰個人就係 Tier 1 成個安全論據**。一個冇人真係做嘅審批步驟,會把個論據變成形式,而**每個畫面照樣印住一個人名喺個決定側邊**。系統一啲都唔會睇落唔同 | W46 plan §6(2026-08-15) | **Med-High** —— 同 R4 同族(程序性 gate 冇人睇就會退化) | 🔴 High(Tier 1 嘅安全論據失效,而且冇任何技術信號) | ① **G7 `GET /agent/review-stats`**(ADMIN only)—— per-reviewer 批准率 + median 秒數 + `fastDecisions` ② 🔴 **`fastDecisions` 先係證據**(幾秒就決定 = 冇讀過);**median 唔係** —— 個鐘由 proposal **建立**開始行,長 median 可以係「審得仔細」亦可以係「冇人喺度」③ 🔴 人口 = `decidedAt != null`,**排除** `abortRun` / `expireRun` 批量 reject 嘅 ⇒ 一個 metric 唔可以喺佢量緊嘅行為變差嗰陣自己變靚 ④ ⚠️ **要有人定期睇,唔睇就等於冇**(同 R4 第③點一模一樣) | ⚠️ **Open**(有數字,但要人睇) |
| R14 | 🔴 **SDK 升級令 tracing 靜靜開返** —— `@openai/agents` **預設把 tool call 送去 OpenAI 嘅 trace backend**,而 tool 回傳值入面有 UPN ⇒ 忘記關就係靜靜洩 PII,**冇嘢會紅**(H4)。🔴 **轉咗 Azure 唔會令佢變安全**:trace exporter 打嘅係 OpenAI backend,同 inference 指去邊完全無關(ADR-0037 E5) | W46 plan §6(2026-08-15) | Med(升 SDK 版本嗰陣) | 🔴 High(靜靜洩 PII 畀第三方) | ① **三重關**(ADR-0036 D11)② `openai-agents.provider.spec.ts` `tracing is off (A4 / D11 / H4)` —— 🔴 **佢自己處理咗 vacuous-pass**:jest 之下 tracing 本來就關,所以條 test **先開返佢再驗關** ③ 🔴 **升 `@openai/agents` 之後一定要跑嗰條 test** —— 佢係唯一會紅嘅嘢 | 🟢 **Mitigated**(test 係唯一防線,但佢真係會紅) |
| R15 | **LLM 幻覺一個唔存在嘅 `skuId`** —— catalogue 有多過一個 E5 變體,所以 prompt 明文要求用 GUID;但 prompt **唔係安全邊界**(D2) | W46 plan §6(2026-08-15) | Med(model 行為) | 🟡 Med(400 而唔係派錯 licence) | ① `propose_line_items` **tool 側**驗格式 **同埋**驗存在性(跟 ADR-0020 先例)② `tool-registry.spec.ts` `propose_line_items (A9 / R15)` 三條:靠名 / 唔存在 GUID / 存在但 inactive ③ 🔴 **belt-and-braces**:即使 model 完全無視 prompt,得到嘅係 400 唔係一個錯 licence<br>🔴🔴 **2026-08-17:呢個風險喺 DEV 第一次真發生咗**(W47 `G8` live,部署 #10 之後)。同一段 text、同一個 model,一個 profile 提咗 `POWER_BI_PRO` = `f8a1db68…`(catalog 真有),另一個提咗 **`c1d032e0-5619-4761-9b5c-75b6831e1711` —— 101 個 catalog row 一個都對唔上**。⚠️ **但唔係缺陷**:proposal payload **明文設計成唔驗**(`ai-assist.service.ts:1031-1034`「unvalidated by design — the tool re-checks on execution … must not be treated downstream as a fact about the catalogue」),兩道閘住喺 tool body,而 tool body **approve 之後先行** ⇒ 幻覺 GUID **入得到 proposal,但入唔到 line item**。📌 **值得記低嘅唔係「有冇擋到」,係「擋喺邊」** —— 操作員喺 approve 之前睇到嗰個 proposal **就係未驗嘅版本**,而佢冇任何信號話畀你聽嗰個 GUID 係假嘅。呢個係 product question 唔係 bug,**未開單**。 | 🟢 **Mitigated**(但 2026-08-17 首次 live 命中,見左欄) |
| R16 | 🔴 **`AgentRun.runState` 存住 SDK 內部結構 ⇒ SDK 升級可能令舊 run resume 唔返** —— 佢係 opaque vendor 結構,唔係 audit truth(D4) | W46 plan §6(2026-08-15) | Med(升級嗰陣) | 🟡 Med → 🟢 **已收窄** | ① resume 唔到就 **fail loud**,唔可以靜靜當成功(靜靜重跑 = 喺一個**冇人批過**嘅 approval 之下執行新一批 tool call)② 🔴🔴 **G5-A 修咗一個真 bug**:嗰個早退 **throw 咗但唔改 row**,而佢排喺 `try` **之前** ⇒ 永遠唔經 `failRun` ⇒ run 停喺 `awaiting_approval` **永遠**,加上 OQ-3 ⇒ **嗰張 request 永遠開唔到新 run,平台冇自愈路**。而家行 `expireRun` ③ ⚠️ **版本標記 + 主動對比未做**(OQ-5 ③ 係 AI 建議,Chris 未逐條講) | 🟡 **Mitigating**(fail loud + 自愈已做;版本標記未做) |
| R17 | 🔴 **Azure OpenAI「deployment name」填錯 → 404,而錯誤訊息一個字唔提 deployment** —— SDK 設咗 `deployment` 就會把 base URL 改寫成 `…/deployments/{name}`(`openai/azure.d.ts:19-20`)。🔴 **而「deployment」呢個字對 infra 嚟講預設係 ARM deployment,唔係 model deployment** —— 同 ADR-0028 嗰個 "Application ID URI" 三輪往返攞唔到**完全同族** | ADR-0037 / infra 請求 B5(2026-08-16) | Med(一次性配置,但錯咗好難 debug) | 🟡 Med(功能唔 work,診斷成本高) | ① 📤 版**逐字解釋咗個詞**兼要求**原文回報,copy 唔好重打** ② `.env.example` 要明文寫「Azure 之下 `AGENT_MODEL` 填 deployment name」(**未做**,infra 信 B4 列住) | ⚠️ **Open**(信未發) |
| R18 | 🔴 **「轉咗 Azure」被讀成「PII 問題解決咗」** —— ADR-0037 E1 個論據係「收件人變咗,同 Graph / M365 同一個信任面」,而**呢句只喺冇第三方人手覆核嘅前提下成立**。Azure OpenAI 預設會為 abuse detection 保留 prompt 一段時間,flagged 內容**可以畀 Microsoft 員工睇** | ADR-0037 E5 / infra 請求 B5(2026-08-16) | **Med-High** —— 一件令人安心嘅事實,擺喺一個佢答唔到嘅問題隔籬 | 🔴 High(整個 E1 論據建喺一個未證實嘅前提上) | ① **`Q3` 本身就係防 R18** —— 問返 abuse monitoring 實際點行 ② 🔴 **答案若係「有人手覆核」,`E1` 個論據就要收窄**(而唔係當冇事發生)③ ⚠️ **`Q3` 我係由記憶寫嘅,repo 證唔到** ⇒ 寫成「請確認」唔寫成結論 | ⚠️ **Open**(信未發) |
| R19 | **Azure OpenAI resource 由 infra 持有,佢改配置我哋只見 404** —— 換 deployment / 改 quota / 停 resource,平台側嘅症狀全部一樣 | infra 請求 B5(2026-08-16) | Med | 🟡 Med(功能停,原因指向錯方向) | 🔴 **未減。** 要一個「agent 打唔通」嘅監測面,而佢唔喺 W46 scope | ⚠️ **Open**(零緩解) |
| R20 | **兩個 agent SDK 各自 ship 自己嘅 `zod` / HTTP client ⇒ 版本撞**,而 tool schema 就係靠 `zod` | ADR-0038(2026-08-16) | Low | 🟡 Med | 🟢 **實測之後幾乎唔存在**:`@anthropic-ai/sdk@0.117.1` 得**兩個** dependency,而 **`zod` 係 optional peerDependency**(唔係 dependency)⇒ 兩個 SDK 冇各自 bundle 一份。⚠️ 2026-08-17 實測 workspace 解析到 **`zod@4.4.3` 一個版本** | 🟢 **Resolved**(實測推翻咗原假設) |
| R21 | 🔴 **「裝咗個 SDK」被讀成「可以打 Anthropic」** —— 而 **OQ-7 Claude 半邊從來未答過**(ADR-0037 E7 個禁令一個字冇郁:唔可以引用 OpenAI 嗰個答案)。有人填個 `ANTHROPIC_API_KEY` 就真打。**同 R18 完全同族** | ADR-0038 D3 / D5(2026-08-16) | Med(一個 env 之遙) | 🔴 High(未經批准嘅 PII 出境) | ① provider **未配就 503**,兼且**一條 test 守住「唔會建立真 HTTP client」**(`refuses without an API key AND never constructs a client`)—— 呢條 test 係唯一防線 ② ADR-0038 D5 把 target 收窄成「**真打 Anthropic 之前**」而唔係「G4 之前」,**收窄唔係推翻** ③ ⚠️ `ANTHROPIC_API_KEY` 喺 `.env.example` 明文寫住「**今日唔應該填**」 ④ 🔴 **2026-08-17:個窗口真係開過** —— W46 worktree 嗰份 `apps/api/.env` 實測有一個 **108 字元**嘅 `ANTHROPIC_API_KEY`,即係 ①嗰道「未配就 503」嘅閘**當時唔會擋**。Chris 同日批准清走,**已清(len 108 → 0)**。⚠️ **誠實界線:「填咗」唔等於「真打過」** —— 同一份 `.env` 個 `AGENT_RUNTIME` 係 `openai-agents`,而 runtime 選路行 `ConnectorConfigService` DB-first,所以 Claude provider 大機會由頭到尾冇被建;但**我哋冇任何 log 證得到呢點**,所以只可以講「冇證據真打過」,唔可以講「肯定冇打過」。📌 **呢條記低嘅價值唔喺個 key 本身,係「一條 test + 一句註釋」呢種 mitigation 嘅真實強度** —— 佢擋到 code path,擋唔到有人填個 env | 🟡 **Mitigating**(靠一條 test + 一句註釋)· ⚠️ **2026-08-17 一度變成 Occurred(key 真係填咗),同日清返** ⇒ 呢條 risk 已經證實**唔係假想** |
| R22 | 🔴 **ACA ingress buffer 住 SSE ⇒ 畫面一路空白然後一次過跳完** —— 三層(NestJS / nginx / ACA ingress)之中**頭兩層喺我哋手上兼已配好**,第三層改唔到亦未驗證。⚠️ **本機結構上測唔到**(本機冇 ACA ingress) | ADR-0039 F7(2026-08-16) | **Unknown** —— 呢個就係風險本身 | 🟡 Med(功能睇落壞咗,但唔會出錯數據) | ① `apps/web/nginx.conf.template` 一個 **regex location 專用**:`proxy_buffering off` / `proxy_cache off` / `proxy_read_timeout 3600s` / `Connection ''` —— **唔關掉成個 `/api/`** ② **25 秒 heartbeat**(idle 連線正正係 buffering proxy 會捉住嗰種)—— ⚠️ **佢係緩解唔係解決** ③ **`Q6` 問返 infra**(唔使開任何資源,純行為問題)④ 🔴 **回退路 = polling**,而 **ADR-0039 F2 令嗰時契約唔使再改** | ⚠️ **Open**(只有 DEV 答得到 = `B6`) |
| R23 | **Redis 一冇,agent 功能整個停**(queue + pub/sub 都靠佢)—— 而 ADR-0036 D2 個 kill switch 係**故意**要人撳嘅。🔴 **一個因為 Redis 掛咗而靜靜停晒嘅 agent,同一個被人閂咗嘅 agent,喺畫面上要分得出**,否則第一條問題(「係咪有人閂咗?」)會得到一個**自信而錯**嘅答案 | ADR-0039 F4 / F5(2026-08-16) | **High** —— DEV 今日**根本冇 Redis** | 🔴 High(部署即停) | ① 🟢 **code 側已減**:enqueue 失敗返一句明文講「background queue (Redis) is unreachable … **not the agent kill switch**」,而一條 test 用 `/Redis.*not the agent kill switch/s` 守住 ② 🟢 `skipWaitingForReady: true` ⇒ **快啲失敗**,唔會 hang 住個 HTTP request(hang 住喺畫面上同「agent 諗緊」分唔開)③ 🟢 queue / events / producer **三個 connection 各自有 `error` listener** ⇒ 一個 Redis outage **拉唔冧成個 Nest process**(冇 listener 嘅 EventEmitter 會 throw = unhandled rejection)④ 🔴 **另一半未減:令 Redis 真係存在** ⇒ infra 信 `Q5` | 🟡 **Mitigating**(誤診已防;停擺未防) |
| R24 | **In-process worker 令一個長 run 佔住 api 嘅 event loop 份額** —— ADR-0039 F3 否決咗第三個 container(§5.2 locked stack 寫「app + postgres + redis」冇 worker,而本 phase 已經有一個外部依賴等緊) | ADR-0039 F3(2026-08-16) | Low-Med | 🟡 Lower —— LLM call 係 **I/O-bound**(等 socket,唔係計嘢)**所以可接受,但唔係零** | ① `AGENT_RUN_CONCURRENCY` 預設 **2**(1 會令成個 deployment 排隊等最慢嗰個,而個等以分鐘計;大數字就係扮 event loop 免費)② **個 knob 存在就係為咗畀真環境嘅數字取代呢個猜測** ③ ⚠️ **未量度過** | ⚫ **Accepted**(知情;有 knob) |
| R25 | 🔴 **`rediss://` vs `redis://` 一個字之差** —— `ioredis` 靠 **scheme** 決定行唔行 TLS,而 Azure Cache for Redis **預設只收 TLS**(6380,non-TLS 6379 預設停用)。填錯個症狀係「連唔到」,而錯誤訊息**一個字唔提 TLS**。**同 R17 完全同族** | infra 請求 B2(2026-08-17) | Med(一次性配置) | 🟡 Med(功能唔 work,診斷成本高) | ① 📤 版**逐字解釋咗兩個 scheme 嘅分別**兼要求**原文回報成條 connection string** ② ⚠️ **本機 default 係 `redis://127.0.0.1:6379`(docker,冇 TLS,啱嘅)⇒ 本機同 DEV 兩條 URL 個 scheme 會唔同**,唔係 bug 但要有人知 ③ 🔴 **connection string 帶 access key ⇒ 佢係 secret**,只落 env(H4) | ⚠️ **Open**(信未發) |
| R26 | 🔴 **`AgentProfile.prompt` 落 DB = 平台第一個「改咗行為但唔會出現喺任何 code review」嘅面** —— 一個 ADMIN 改個 system prompt 就改咗 agent 點諗嘢。⚠️ **呢個係本 phase 唯一一個咁樣嘅位,唔好將來靜靜多幾個** | W47 `R1`(2026-08-17) | Med | 🟠 High | ① 🟢 **每次寫都入 audit `before`/`after`**(`OQ-C`)—— 要答嘅係「**改成咩**」唔止「有人改過」;no-op PATCH 唔寫 row,否則嗰條 query 會被冇改過嘢嘅 edit 塞爆 ② 🟢 **tool allow-list 仍然淨係喺 code**(`ADR-0036 D1`)⇒ 最壞情況係 agent 亂噏,**做唔到未授權嘅嘢** ③ 🟢 `MAX_PROMPT_LENGTH = 8000` **喺 DTO 同 service 各夾一次** —— 個 cap 就係「呢個欄係**配置**唔係**內容**」呢個講法嘅唯一支撐,而 audit whitelist 收 `prompt` 正正係用嗰個講法爭返嚟 ④ 🟢 prompt **取代**內建 instructions 而唔係 append —— 兩套互相矛盾嘅指示會產生冇人預期過嘅行為,而 admin 睇住自己寫嘅 prompt 唔會知前面仲有啲乜 | 🟡 **Mitigating**(三道防線齊;本質上仍然係一個 runtime 行為面) |
| R27 | 舊 run(`profileId = null`)—— **部署嗰日全部 run 都係咁**,讀取路徑一 assume 有 profile 就會**一次過**喺 request detail 卡爆晒 | W47 `R2`(2026-08-17) | High(必然發生) | 🟡 Lower | ① `profileId` **nullable 而且會長期 nullable** —— 追溯 back-fill 就係聲稱一件冇發生過嘅事 ② 🟢 `F1-5` 一條 test 釘住「舊 run 讀得返」 ③ `OQ-D`:畫面顯示「(W47 之前)」**唔隱藏** —— 隱藏會令「W47 之前有幾多 run」變成答唔到嘅問題,而佢正正係新列表最易被信錯嗰個數 | 🟢 **Resolved**(nullable + test + 顯示策略) |
| R28 | profile 被硬刪 ⇒ 歷史 run 講唔出「當時用咩跑」 | W47 `R3`(2026-08-17) | Med | 🟡 Med | ① **唔提供 DELETE**,只 `active=false` ② 🔴 **`onDelete: Restrict`** —— 2026-08-17 讀 migration SQL 先發現 Prisma 對 optional relation **預設 `SET NULL`**,即一個直接落 DB 嘅 delete 會把嗰個答案**一次過**變成 unknown 而**冇任何錯誤**。⇒ **一個決定喺 API 層守住咗,而同一個決定喺 DB 層預設係反方向嘅** ③ ⚠️ **仍然答唔到嘅一半**:profile 本身 mutable(model 改得),所以 `profileId` 答到「用邊個 profile」但答唔到「嗰一刻嗰個 profile 係咩 model」。要真答就要喺 `AgentRun` 存 snapshot = schema 改動(H1),**未開單** | 🟡 **Mitigating**(刪唔到;但 mutation 仍然覆蓋得到歷史) |
| R29 | 多 profile ⇒ Azure 側成本 / tracing 歸屬散開 | W47 `R4`(2026-08-17) | Med | 🟡 Lower | run 列表出 `profile.name` + 可以按 `profileId` 篩。⚠️ **成本本身唔喺本 phase**(H3);今日平台冇任何成本可見度 | ⚫ **Accepted**(知情) |
| R30 | run 列表分頁做得求其(`take` 一個大數扮分頁)⇒ run 多咗就爆 | W47 `R5`(2026-08-17) | Med | 🟡 Med | ① cursor + `skip: 1` + `take: limit + 1`;limit 上限 **DTO 同 service 各夾一次**(只夾喺 HTTP 邊緣嗰個,第二個 caller 一出現就唔再成立)② 🔴 orderBy 有 **`id` tiebreak** —— 兩個 run 可以同一毫秒開,order 唔穩定就會喺頁邊界跳行或者重複,而佢**淨係喺有負載嗰陣出現、永遠 reproduce 唔到** ③ 一條 test 釘住 `take` | 🟢 **Resolved** |
| R31 | ⚠️ **`modelForLegacyRun` 由「相容路」滑成「fallback」** —— 佢係唯一一個仲可以由 env 決定 model 嘅位,而 `resolveForRun` 明文拒絕咁做 | W47 `F3-5`(2026-08-17) | Low-Med | 🟡 Med | ① **結構上到唔到**:`startRun` 每一行都寫 `profileId`,所以只有 W47 之前開嘅 run 行得到 ② 佢**必須**存在 —— 部署嗰刻卡喺 `awaiting_approval` 嘅 run 冇 profile,喺嗰度 refuse 就會令佢永遠 resume 唔到,而 OQ-3 令嗰張 request 再開唔到第二個 run(= 期二 `G5-A` 嗰個 permanent block 第三次由另一道門入嚟)③ 行到就 **warn log** ④ 冇配置一樣 **503,唔會靠估** ⑤ 🟢 `agent.boundary.spec.ts` 釘住**兩個 adapter 都唔准 import `ConnectorConfigService`** ⇒ 呢條路只可以存在喺 service 一個地方 | 🟡 **Mitigating**(有意保留;有閘有 log) |
| R32 | 🔴🔴 **chat 變成一條繞過 approval 嘅路** —— 對話介面令「叫佢做嘢」感覺輕,而 `ADR-0036 D3` 個 human-in-the-loop 係 **Tier 1 成個安全論據** | W48 `R1`(2026-08-18) | Med-High | 🔴 High | ① chat 開嘅 run **就係普通 `AgentRun`**,行返同一個 tool registry 同一道 approval gate(`ADR-0041 D8`)—— 唔係「小心咗」,係冇第二條路 ② UI 兩條 test,而**第二條先係持久嗰條**:behavioural 只擋到一個串「Approve」嘅掣,**source scan** 擋到將來叫 `Accept` 嗰個(`assistant.tsx` 唔可以出現 `useDecideProposal` / `useApprove` / `/proposals`)③ live 實測 run 泊喺 `awaiting_approval` 時,畫面只有一條去 request 嘅 link | 🟡 **Mitigating**(結構 + 兩層 test) |
| R33 | ⚠️ **chat transcript 冇「清」嘅路徑,而佢一樣含 PII** —— `OQ-G` 答「一直存在,直至清掉」,但**「清」嘅形狀今日仍然唔存在**:`ADR-0041 D7` 只做 archive(隱藏,零刪除) | W48 `R2`(2026-08-18) | Med | 🔴 High(H4) | ① `AgentChatTurn` 係**新表**,`ADR-0036 D6`(`AgentMessage` 永久保留)覆蓋範圍一個字冇郁 ② 讀路 **owner-only,連 ADMIN 都唔見**(`assertOwner`)⇒ 今日 blast radius = 講嗰個人自己 ③ 🔴 **但「保留幾耐」仍然冇答案** —— archive 唔係刪除,而平台冇任何路徑刪一條對話 | ⚠️ **Open**(retention 未定形狀) |
| R34 | **對話成本隨長度非線性升** —— 每個 turn 帶住成段 history,而 blast-radius 係 per-run 唔係 per-conversation | W48 `R3`(2026-08-18) | High | 🟡 Med | ① `MAX_HISTORY_TURNS = 20` + `MAX_HISTORY_CHARS = 20_000`,**截斷嗰陣出聲唔係靜靜切** ② `MAX_TURN_LENGTH = 4000` 夾單一 turn ③ ⚠️ **仍然冇 per-conversation 上限**(嗰個係 `T2-e`)⇒ 一條長對話嘅總成本今日冇人睇得到 | 🟡 **Mitigating**(有 cap,冇可見度) |
| R35 | 🔴 **一條開住嘅 SSE thread 可以永久失去 live 更新,而畫面唔會講** ⇒ 一次 api 重啟(= 一次部署)就中,用戶見到嘅係「agent 唔答我」。<br>🔴🔴 **2026-08-19(W49 `F4-3`)更正咗成因,而原本嗰個描述唔啱**:原文寫「連續失敗 3 次就放棄」,而 live instrument(wrap `EventSource` 記低每個 event,再殺 api)攞到嘅係 —— **`open` 之後 18 秒零 event**:冇 `error`、冇重連、`readyState` 一直 **OPEN**。proxy 喺 upstream 死咗之後仲揸住個 socket,瀏覽器冇方法知。⇒ **個 3 次 bound 根本 fire 唔到**,W48 `F7-5` 把畫面唔郁歸因於佢係**歸錯因**。<br>⚠️ 另一條路(**新開** connection 打一個已經死咗嘅 api)實測係 **1 個 error + `readyState` 即刻 CLOSED,永不重試** ⇒ 一樣去唔到 3 | W48 `F7-5` live(2026-08-18)· **成因由 W49 `F4-3` live instrument 更正(2026-08-19)** | **Med-High**(每次部署) | 🟡 Med | 🟢 **W49 `F4-3` 減咗,分三段**:①`readyState === CLOSED` ⇒ **即刻**報,唔數(數落去永遠唔會夠)②**staleness timer** —— 超過 60s 冇收到任何嘢(含 heartbeat)就當斷,呢個係唯一捉得到「零 event」嗰種嘅方法;個數由後端 `AGENT_SSE_HEARTBEAT_MS`(default 25s)推導 ③畫面出 banner + **`Reconnect`** 掣(之前唯一嘅解係「切走 thread 再切返」,冇人估到)。⚠️ **`MAX_CONSECUTIVE_FAILURES = 3` 保留** —— 佢對「重試中」嗰種仍然啱,而喺嗰啲時候閃 banner 會訓練人無視佢 | 🟡 **Partial** —— dock 已減(`agent-conversation-events.ts` + 12 條 test);⚠️ **`/assistant` 仍然忽略 `disconnected`**,登咗 `BACKLOG ASSISTANT-HONESTY`。🔴 **仲有一件未驗**:上述行為只喺**本機(vite dev proxy)**觀察過,DEV 行 nginx + ACA,可能會 close 個 stream(咁 `error` 就會 fire)⇒ 目前個實作兩種都蓋到,但「DEV 實際係邊種」未知 |
| R36 | ⚠️ **`ADR-0041 D3` 擋到「見唔到」,擋唔到「填錯」** —— 冇 request context 嘅對話真係攞唔到 request 工具(live 對照實驗證過),但**有** context 嗰條,`get_request` 個 id 仍然係 model 自己填 | W48 `F7-6` live(2026-08-18) | Med | 🟡 Med | ① 實測失敗方向**安全**:model 攞人講嘅 `REQ0044067` 當 cuid ⇒ `Request not found` ② tool 自己行啟動者嘅 OpCo scope(Tier 2 `OQ-2`)③ 🔴 **未驗嗰半**:「填一個**存在而屬於第二個 OpCo**」嘅 id,本次冇做過 live | ⚠️ **Open**(一條未做嘅 live) |
| R37 | **catalog 全部 SKU 個 `businessAlias` 係 `null`** ⇒ agent 收到人話 SKU 名 search 唔到、幫唔到手;**而佢把「搵唔到」講成「攞唔到」** | W48 `F7-6` live(2026-08-19) | High(已發生 ×2) | 🟡 Med | ① agent **冇亂估**(明文「can't propose the licence without guessing」)= 好行為 ② 🔴 但 tool step **`status: ok` 返 `[]`**(「搵唔到」)而 agent 對用戶講「couldn't **retrieve** the active catalogue」(「攞唔到」)⇒ **用戶會當系統故障去搵 IT** ③ 修法喺 **catalog curation 唔喺 agent**(同 `CH-026 G-7` 同族),已登 `BACKLOG` | ⚠️ **Open**(唔喺 W48 scope) |

<!-- 範例:
| R1 | 某外部服務單點故障 | BUG-0XX postmortem | Med | High | 加 fallback + 熱切換(ADR-00XX) | 🟡 Mitigating |
| R2 | 依賴 X 有 license risk | 設計審查 | Low | High | 只讀 reference,唔 copy(H-參考) | 🟢 Resolved |
| R3 | 規模化前唔做 sharding | 團隊決定 | Low | Med | 接受;規模到再處理 | ⚫ Accepted |
-->

## 2. Risk Detail(逐條明細,重要風險先展開)

### R1 — {Risk name}
- **First observed**:YYYY-MM-DD
- **Description**:{風險係咩}
- **Trigger / Recurrence**:{幾時會發生 / 有冇 recurrence log}
- **Mitigation**:{做緊咩}
- **Status**:{emoji}

## 3. Review Cadence
- 每個 phase closeout 掃一次:有冇新風險?狀態有冇變?
- Sev1/Sev2 bug postmortem → 檢視有冇對應風險要加/升級。

## 4. Maintenance Protocol

| Operation | How |
|---|---|
| Add risk | Index 加一行 + 重要嘅展開 §2 明細;commit `docs(risk): add R-n` |
| Status change | 改 Index status emoji + 明細;commit `docs(risk): R-n status → X` |
| Resolve | 改 🟢,**保留 entry 做 audit trail**(唔刪) |
| New pattern from postmortem | 加新 R-n + link postmortem |
