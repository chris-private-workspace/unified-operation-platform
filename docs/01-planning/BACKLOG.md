# Unified Operation Platform — 工作 Backlog(中央 dashboard 入口)

> **用途**:本項目**所有** pending 工作 / next-candidate 嘅**單一一覽入口**。AI 或用戶想知「而家有咩 pending、可以揀邊個做」→ 第一站睇呢度。
>
> **定位 = index 層,唔複製細節**:每行只記「任務 / 狀態 / 前置·阻塞 / 來源連結」,細節一律 link 去 source-of-truth(`adr/README.md` / `DEFERRED_REGISTER.md` / 對應 phase folder),避免雙重維護變 stale。
>
> **同步 = binding(PROCESS.md R7)**:phase kickoff / closeout、ADR Accept、defer/blocked 決定、新 candidate 被識別 → 必須同步本表,**唔可以 silent drift**。維護規則見文末。

> ## ⚠️ **W36 有兩個** —— 引用時一律帶後綴
>
> | Folder | 內容 | Kickoff |
> |---|---|---|
> | `W36-opco-budget-gate` | **ADR-0016** 落地(OpCo 預算 assign 硬 gate) | 2026-07-27 **09:12** |
> | `W36-n8n-intake-adapter` | **ADR-0017 戊**(n8n native intake adapter) | 2026-07-27 **11:26** |
>
> 成因:兩個平行 session 喺**獨立 worktree**,後者揀號時只睇到 `main`(當時最新係 W35),睇唔到前者仲喺**未 merge** 嘅 branch 上面。兩個都 closed 咗先發現。
>
> **刻意冇改名** —— commit message / merge commit / PR 標題全部寫死咗「W36」,改 folder 只會令 git history 同 tree 永久對唔上,即係用一個外觀問題換一個修唔到嘅矛盾。兩個 folder 路徑本身唔撞(`git merge-tree` 實測零衝突)。
>
> ⇒ **淨講「W36」= 有歧義,必須帶後綴。** 下一個可用號 = **W38**(W37 = `W37-sync-sweep`,ADR-0015)。
> 防再犯:**PROCESS.md §2.1** 已加「揀 `{NN}` 前必須 `git fetch --all` 兼掃晒所有 branch」。

**最後更新**:2026-07-28(**W40 = ADR-0017「辛」✅ 收官 ⇒ rollout 戊己庚辛 4/4 全數交付** —— `TicketUpdateProvider` 雙實作 + `n8n-ticket` connector + close/WIP trigger;api 528→**588** · lint 0 · tsc 0。詳見 A 區 `N8N-SEAMS-己庚辛` row。**三樣值得記住**:①**D3 一處 blocking 落差** —— 2004 **冇任何 mode 係「加 note 而唔改 state」**,照 ADR 做會令切掣時 **assign 成功嘅單被標成 Work in Progress**(違反 D0 兼語意相反)⇒ OQ-A 拍板 `addWorkNote` **唔入介面**,同 W38 收窄 D2 同一判斷 ②**四次被自己嘅守門捉到**,其中兩次係我自己判斷錯:boundary test 個**正面半邊**揭穿 **TS `abstract` 方法冇 runtime 存在**(`getOwnPropertyNames` 只返 `["constructor"]` ⇒ 條 negative assertion **永遠綠**);`resolve()` 個 typed `ConnectorKey` 揭穿 F2/F3 條線畫喺**唔存在嘅接縫**上(R3) ③**順帶揭穿 4 個從來未驗過洩漏嘅 config key**(含 `SERVICENOW_USER`)—— 因為 leak test fixture 係**手抄清單**,加守門令覆蓋面由 `CONNECTOR_CONFIG` derive 之後即刻紅。🚧 **真切換仍然零 live 驗證**:2004 secret 仍 `CHANGE_ME_SHARED_SECRET` · patchUrl **hardcode DEV host** · credential **row-level ACL**(而 2004 用 `neverError:true`,所以呢個失敗會係 **HTTP 200 + `status:'error'`**)⇒ runbook 08 新增 §6,含 **§6.3 切之前必做:n8n sticky 寫死 RITM 分工**(1007 已經自己 PATCH `state=3`,而兩邊冇任何自動守門)。之前:**BUG-004 + BUG-005 ✅ 一批收** —— api 499→**528** · lint 0 · tsc 0。**BUG-004**[H4:vendor error 原封入 log 而 Graph 404 body 帶 UPN]潛伏 **18 日**,期間三個 phase 掂過嗰啲檔 —— 因為既有 test **assert exception message 而洩漏喺 log line**,而個 doc comment 仲寫住一句**佢做唔到嘅承諾**「never log the target UPN」。修法 = 共用 `scrubPii()`,**12 處只修 4 處**[直接處理 user identity 嗰啲],其餘明列唔順手改。**BUG-005**[面板 selection 只讀 env,而 runtime 自 W34 已 DB-then-env]⇒ 面板話「未啟用」而平台其實行緊 n8n;修法除咗改用 resolver,仲**移走咗 `ConfigService`** —— 兩者並存嘅話重新 drift 只差一行。🔴 **兩個 bug 同一個教訓**:**兩處各自實作同一件事,而冇嘢逼佢哋一致**[BUG-004 = comment vs code;BUG-005 = 面板 vs runtime]。連 W39 數過嗰三次,呢個 pattern 已經**第五次** —— 解法一律係「**由單一來源 derive,唔好各自維護一份**」。postmortem 已寫[recurring]。之前:**W39 = ADR-0017「庚」✅ 收官** —— `N8nLicenseProvider` + `n8n-license` connector + probe + **雙向 contract test**;api 467→**499** · lint 0 · tsc 0。詳見 A 區 `N8N-SEAMS-己庚辛` row。四樣值得記住:①**H1** —— 加 connector **必然改 schema**(`ConnectorConfig` 係具名 column 唔係 key-value bag),教訓入咗 **ADR-0013 補註**因為**下個加 connector 嘅人會踩同一坑** ②**實讀 workflow JSON 揪到 ADR D2 四處對唔上** ⇒ 凡平台送出/解析嘅嘢,**JSON 係 SSOT,唔可以照抄 ADR 轉述** ③**六次被自己嘅守門攔住**(其中兩次係**同一結構**:一個手抄清單假裝自己係完整清單 —— probe G2 手抄 4 個 key、`list()` 手寫陣列;兩次都改成由 inventory derive)④**「test 全綠」唔等於「嘢做咗」** —— 我 Day 2 見 spec 全綠就推斷「自動 derive」,結果真相係新 connector 根本冇顯示,而條 test 因為手抄住舊嘅四個所以照綠。🚧 **真切換仍然零 live 驗證**[n8n secret 仍 `CHANGE_ME_SHARED_SECRET` · UAT 未接通 · 平台未部署]—— 唔可以當通過。🚧 新 follow-up:`integration-status` selection state 只讀 env 唔讀 DB override[影響 `n8n-outbound` + `n8n-license` 兩行,要一次過修]。**下一個 = 辛,號碼 W40**。之前:**W38 = ADR-0017「己」✅ 收官** —— `LicenseOperationsProvider` + `GraphLicenseProvider`,純重構零行為改變。Chris 四項拍板令介面**細過** ADR D2 個表(5 個方法收 3 個),而且 seam **只覆蓋 assign 路徑** —— 查證揭 `getSubscribedSkus` 原來有 **4 個** consumer,`reconcile`[對帳基準]/`integration-probe`[要探嘅正是被換走嗰個執行器]/`catalog` 全部留 vendor 直連,由 `license-ops.boundary.spec.ts` **靜態鎖死兼把理由寫入 test 名**。🔴 最要緊嗰個係 **OQ-2 補時序空白**:ADR-0017 **07-26** Accepted 而 `sync-sweep` **07-27**(W37)先存在 ⇒ D2 表寫嗰陣佢未有;經 n8n 2005 去「證實」sync 就係**直接推翻 ADR-0015**「由 n8n 聲稱升級為平台證實」。**G2 = `16 加 / 0 刪`**,關鍵手法係 spec **唔 mock provider** 而係 wire 真 provider 包住既有 `GraphService` mock —— 否則 raw→503 個 wrap 跌出測試鏈,兩條 BUG-002 regression 會靜靜降級成廢話但照樣綠。**G5 fails-before** 插 import 落 `reconcile` → `1 failed / 7 passed`。**G8 live** 真 boot + 真 HTTP 503 逐字不變 + log 出自 `[GraphLicenseProvider]` + 四個 count 零變動;🚧 assign **成功**路徑明標未驗(跑通就真派 licence 畀真人)。**兩個 AP 中過招**:AP-11[3100 跑緊另一 checkout 嘅 dist]· AP-7。api 450→**467**。**新登 BUG-004**[H4:`graphUnavailable()` 原封 log vendor error,而 Graph 404 body 帶 UPN;同 BUG-001 唔同 —— 嗰次修嘅係我哋自己格式化嘅字串]。⇒ **庚解封,號碼 W39**。之前:**收四條 pending branch 返 main —— W36-opco-budget-gate · W37-sync-sweep · CH-008 · CH-009**。四者原來係一個 stack[`ch-008 ⊆ w37-sync-sweep ⊆ w36-budget-gate`,而 ch-009 個 tip 已 merge 咗 w36-budget-gate],實證三條相對 `docs/w36-budget-gate` **各自 0 個真 commit 遺留** ⇒ **merge 一條涵蓋全部**。43 檔 / +3279。唯一衝突 = 本檔 B 區 table[ours 6 行 vs theirs 3 行,**兩邊各有對方冇嘅 row** ⇒ `--ours`/`--theirs` 都會靜靜刪嘢],照本檔當時嘅逐行指引 resolve:`SYNC-sweep`/`Budget-gate` 取 theirs[已 ✅ 完成]· `n8n-intake-handoff`+`N8N-SEAMS`×3 取 ours[theirs 完全冇嗰三行]。<br>**W36-opco-budget-gate ✅**(ADR-0016,OQ1 = 選項 A):assign 預算硬 gate + ADMIN 具名 override&audit + 前端入口 + **preflight SQL** + rollout runbook。retro 最抵一條:「SQL 真跑要對得返 plan §2」呢條 acceptance **證偽咗 plan** —— 另揭一個 grounding 盲點:**完全冇 ledger row 嗰批喺 22 行超支名單裡面隱形**。carry:gate 400 嘅 live 驗證要真 synced user[部署後第一項]· **CH-008 toggle 由「方便」升級為「必需」**[gate 上線後]· TD-1。<br>**W37-sync-sweep ✅**(ADR-0015,OQ1=A·OQ2=B·OQ3=A):`@Cron` 掃 `azureSyncedAt IS NULL`,`azureSyncedAt` 由「n8n 聲稱」升級為「平台證實」。🔴 **RISK R3 只到 🟡 Mitigating,唔係 🟢** —— 仍係輪詢[最壞等 10 分鐘]· 30 日後放棄仍要人手 · **多實例 scale-out 會重複跑**[ADR 明文 YAGNI,**唔可以順手加 lock**]。retro 自認一個紀律 miss:用咗 `sed -i` 改 checklist **違反 H8**。<br>**CH-008 ✅**(0/0 ledger 行預設隱藏 + 修正 `assetStatus` 誤導狀態)· **CH-009 ✅**(assign 前可用量可見度)。<br>⚠️ **兩個 W36 folder 而家真係並存**(`W36-n8n-intake-adapter` / `W36-opco-budget-gate`)—— 見本檔頂部說明,引用必須帶後綴;**下一個可用號 = W38**。之前:**W36 N8N-SEAMS 戊 ✅ 完成 —— inbound intake adapter** — **G1-G9 9/9**;**零 schema / 零新 runtime dep / 零新 ADR**[ADR-0017 事前 Accepted,全程冇踩出範圍]。交付:`POST /requests/intake/n8n` 收 n8n 原生信封 → 三個 resolve[**licenseCode→skuId GUID**(businessAlias→skuPartNumber,active-only,**唯一命中否則拒**)· **department→opcoCode**(18 條 code 常數表 = OQ-1 拍板,**零 normalize / 零 alias / 零 fallback**)· **REQ number→sysId**(`getRecordByNumber`)] → 交既有 `IntakeService`;**G2 三個受保護檔零 diff**[`intake.service.ts` / canonical DTO / W24 `CONTRACT.md` **LOCKED 一個字冇改**]—— 呢個係核心設計:**加一條 route 而唔係放鬆合約**。OQ-2 拍板**平台新增 OpCo `RAPO/IT (RDC2)`**(即使 AD 側 description 仍係 `RAPO/IT`,平台刻意切細)· OQ-3 **兩條 route 共用同一個 `INTAKE_API_KEY`** · OQ-4 值仍未知,靠 4xx 回顯做發現機制。api **390→407** test[17 個新 spec,**wire 真 `IntakeService` 唔 mock**,令「零寫入」係貫穿路徑嘅真證據;**G3 fails-before 實證**:拆走歧義守衛 → 個 test 印出「靜靜建咗一張完整 Request」]。**live 8 個 case 真 output**[401 / 201 / 冪等 / 三種 resolve 400 / RDC2 / 壞信封],DB 逐行查過,**負面實證**:6 個被拒 case DB 零行 **且冚唔到 mock-SN log** ⇒ cheapest-first ordering 有行為證據。doc-sync 3 處[`N8N-INTAKE-HANDOFF` §0/§2/§7/**新 §8** · `N8N-INTEGRATION-SETUP` §0/**新 §1.5**/§1.4/§5/§6 · `N8N-WF1-CHANGES` **新 §2.5/§2.6**]。🔴 **F4 揪出三個 n8n 側 blocking 接線缺口**[①兩個 `WF1 - Call UOP Intake` **完全冇送 `X-Intake-Key`**(無 `sendHeaders`、`credentials` 空)⇒ **一 enable 就全部 401**,呢個 node 存在好耐但從來冇人真試過 ②`UOP_INTAKE_URL` 要指去 `/requests/intake/n8n`(好消息:係 `$env`,改 env 唔使郁 node)③1005 個 node 仍 disabled]—— **等 Chris 喺 n8n UI 做**。⚠️ **carry-over**:**真 SN 端到端未驗證**(live 行 demo-harness mock,憑證 placeholder,plan R2,**唔當 pass**)· **各環境要補 OpCo `RAPO/IT (RDC2)`** 且該 OpCo `allocated=0` ⇒ **ADR-0016 gate 上線前要設 allocation** · `licenseCode` 可為 `null` ⇒ **成張單** 400 而非跳過該 line。✅ **1001 vs 1005 payload shape 實讀確認一致**。計劃外改動一項:`mock-servicenow.js` GET query form 本來返 object 而真 Table API 返 array(R3 已 log)。新增 **AP-11**[驗錯咗第二個 checkout —— port 3100 跑緊嘅係 `C:\Users\CLai03\unified-operation-platform` 另一棵樹,404 唔係 stale build]+ **AP-12**[只驗 happy path,冇驗「唔應該發生嘅嘢真係冇發生」]。新登 **SEC-001**[1002 明文 WinRM 密碼,見 A 區]。**下一個 = 己**[`LicenseOperationsProvider` + `GraphLicenseProvider`,純重構零行為改變]。之前:2026-07-26 **ADR-0017 n8n 執行接縫化 Accepted** —— Chris 交出 n8n phase 1[6 個 workflow]+ phase 2[4 個 for-UOP webhook]全部 JSON,要求規劃「可轉換連接方式」架構。讀畢全部 10 個 workflow + 平台 integration 層後拍板 **三接縫逐個可切換**:①outbound 建單[W26 既有,不改] ②`LicenseOperationsProvider` graph\|n8n[2002/2003/2005] ④`TicketUpdateProvider` direct\|n8n[2004];③inbound intake **必然不可切換**但加 adapter。四個 OQ 全拍板:**Q1 逐接縫 3 個掣**[否決一個總掣[冇彈性]/逐 operation[配置爆炸]]· **Q2 平台加 adapter endpoint**[mapping 真相喺平台,唔應複製落 n8n code node]· **Q3 `getRecordByNumber` 反查 REQ sysId**[零 schema,唔改 `@unique` 語意]· **Q4 擴至 close/WIP**[閉環完整,代價 = 新行為 + 要同 1007 分工]。🔴 **D0 不變式 = 只換執行器唔換決策者**,所有 gate 留平台。🔴 **關鍵發現:場景一合約對唔上** —— n8n `WF1 - Prepare UOP Intake` 送 `{event, request:{}, targetUser:{}, licenseItems:[{licenseCode}]}` 信封,平台 DTO 要 `{targetUpn, opcoCode, serviceNowSysId, lineItems:[{skuId GUID}]}`,**6 項落差,現狀一 push 必 400**;D4 adapter 正是 `N8N-INTAKE-HANDOFF` §0 要求嘅「owner approve + ADR」,非「靜靜 map」。rollout 續天干 **戊**[intake adapter,唯一 blocker]/**己**[Graph provider 純重構]/**庚**[n8n license]/**辛**[ticket]。ADR-0008 已加範圍註記。**同批發現待辦**:🔴 `phase 1/1002` JSON 內含明文 WinRM 服務帳號密碼且目錄 **untracked 未 gitignore**[commit 前 scrub + rotate]· phase 2 四條仍 `CHANGE_ME_SHARED_SECRET` hardcoded · 2004 hardcode DEV host · **1005 排程路徑 Call node 仍 DISABLED**[場景一只有即時路徑鋪好]· 1002 全部 LDAP/WinRM 行 STUB。之前 **W30 Integration 狀態 ✅ 完成 —— 整合 rollout item 4 落地,ADR-0010 首個交付** — **G1–G9 全 9/9 pass**;**零 schema / 零新 dep / 零新 ADR**;`GET /admin/integrations` + `POST /admin/integrations/:key/test`[`@Roles(ADMIN)`]+ Integrations tab 真內容取代 placeholder;**設計上兩個誠實修正**:①唔講 `configured` 講**三態**[`required`/`active`/`inactive`]—— 查證揭 Graph/SN/intake key 喺 constructor `getOrThrow`,缺 config app 直頭 boot 唔起,「configured: true」係恆真廢話 ②n8n inbound **交白卷**[`Request.origin` default 就係 `'onboarding-intake'`,seed 同真 intake 分唔開]—— 寧可標「無法區分」都唔畀個似層層但錯嘅時間,因為運維會照住佢判斷 connector 死咗未;**兩條硬紅線 test 先行**[G1 餵六個假 secret assert 零洩漏 / G2 assert `createRecord`·`updateRecord`·`addWorkNote` 從未被呼叫];**探針零新 vendor 方法**[重用 `getSubscribedSkus()`/`query('',table,1)`],**n8n webhook 永不探**[會建真 ticket,`PROBEABLE` 寫成資料 + 註明「Do not add a probe here」];api 266→**286** · web 93→**99**;live 全驗[回應+頁面零 env 值 · 403 三重 · 切 provider `inactive`→`active` · 第二撳 **429** · light+dark · 零 DocuWare];**D6 清走 DocuWare 文案**[後端零實作 + H3 排除,唔可繼續承諾];ui-design 自檢揪到 3 個位即場修;**next = INTEG-3**[人手 retry,⚠️ 仍要新 model = H1]/ FE-activity[⚠️ 受 ADMIN-only 限制]/ AUTH-2b[🔴 卡 IT]/ DEPLOY。之前 **W29 `AuditLog` ✅ 完成 —— Chris 四項 audit 需求全數落地,audit rollout item 1-3 收官** — **G1–G8 全 8/8 pass**;ADR-0009 Decision 4 **十三個事件**[user CRUD/role_change/password_reset · login success/failed/locked · opco ×2 · catalog · import · drift resolve]落 6 個既有 service,**與主操作同一 `$transaction`**[rollback test 證];**H4 三層邊界**[per-target allow-list / 永久 blacklist 覆蓋 whitelist / `metadata` 固定 key set]**test 先行寫喺任何 hook 之前**,G2 硬紅線由頭到尾冇一刻未驗證;`GET /admin/audit`[ADMIN-only 唔可放寬 · 篩選 · 分頁 cap 100 雙重防守]+ **獨立 `/audit` 頁**[Q2 拍板,零 primary action,`canSeeAdminNav` gate]+ **`design-system.md` §6 新增「prototype 以外 owner-approved 畫面」表**登記;**schema additive 零新 dep 零新 ADR**[ADR-0009 事前已 Accepted;僅補 Decision 5 `metadata` 實作補註 = 收緊非推翻];api 213→**263**[31→32 suites]· web 85→**92**;live 403 三重驗證 + G2 DB 抽查真 row 冇 secret + browser light+dark;**關鍵實績**:R1[改 6 個 service transaction 邊界]既有 test **冇一條 assertion 要改** · **W28 迴歸網當日見效**[加 `AuditController` → permissions snapshot 即捉到新 route];解封 **`FE-activity`** + 新登 **`audit-retention`** candidate;🟡 carry = self-service 改密碼冇 audit[H3 冇擅自加,待 Chris];**下一個 = INTEG-1**[connector 狀態 + test connection,rollout item 4]。之前 **W28 權限矩陣 ✅ 完成 + audit/integration rollout 開跑** — **ADR-0009 Accepted**[Chris 拍板 OQ-1 記白名單 before/after · OQ-2 = P-B 白名單可含 PII,附 4 條連帶義務];**W28**[rollout item 2]交付 `GET /admin/permissions` runtime derive + Settings › Permissions 唯讀 tab + drift/unguarded 防護 test,**零 schema/dep/新 ADR,零行為改動**,api 213→**223** · **live 34 route / 零 unguarded** · 403 三重驗證 · fails-before 雙實證;**關鍵教訓**:手寫矩陣寫低唔夠一日就已 drift[`PATCH /license/ledger/:id` 允許 OPCO_IT 被我抄錯成「GET only」]→ 印證 Decision 8.5「唔起 permission table」;**CRLF 383 error 查明唔係技術債**[`.gitattributes` 早已根治,一次 checkout 清走,零 commit];**下一個 = AUDIT-3**[`AuditLog` 落地,無阻塞 —— 已於 **W29 完成**,見上]。之前:**新增 audit + integration rollout 6 項候選** — Chris 提三問[n8n 接口 / integration UI / audit 需求]→ 查證後寫 pre-ADR 分析 `02-architecture/audit-and-integration-observability.md` + **ADR-0009 draft[Proposed,待拍板 OQ-1 記唔記 before/after · OQ-2 PII 策略]**;新候選 **AUDIT-1**[ADR,待拍板]/ **AUDIT-2**[權限矩陣,**無前置可即開**]/ **AUDIT-3**[AuditLog 落地,等 ADR]/ **INTEG-1**[connector 狀態+test connection]/ **INTEG-2**[n8n 回程 webhook,🔴 blocked on n8n owner 合約,C 區]/ **INTEG-3**[outbound retry,⚠️ BullMQ 從未啟用];關鍵查證:**通用 AuditLog 曾於 ADR-0007 前置分析被 reject** → 本次採「共存而非取代」繞開原理由 · **`FE-activity` 由 AUDIT-3 解封**。之前 **ADR-0008 W25 Phase 乙(outbound-direct)✅ 完成** — IT 平台開單 → 主動 create `sc_request`/`sc_req_item` + 建 two-level mirror + 前端 `/requests/new` 開單畫面 + `GET /opcos` picker enabler;`ServiceNowService.createRecord` + `RequestSubmissionProvider`/`DirectServiceNowProvider` + `Request.origin`;api 176→**187** · web 75→**85** test;live curl + browser 驗;代表性 SN 合約待 live 對齊。加 `.gitattributes`(CRLF hygiene)。之前 **W24-request-intake Phase 甲 ✅ 完成** — 獨立 request 建單 + n8n 雙向整合 + **D365 完整納入 scope**[catalog/ledger/對帳];**D0 doc-sync + D1 合約 lock**[AGENDA 10 條 Chris 自答,n8n=Chris 本人]+ **D2 code**[`POST /requests/intake` + `IntakeKeyGuard` fail-closed + `IntakeService` two-level REQ/RITM mirror + additive migration + 回寫 per-line RITM]+ **D3 H5 test**[api 157→**176**]+ **D4 verify**[build/lint/test + 端到端 curl 4 case 全綠;e2e data 清]。commits `41578ec`[D0/D1]+ `f4fa97d`[AGENDA lock+16 修正]+ D2 commit;**已 push 併入 `main`**（2026-07-20 回填 — 原記「本地未 push」已 stale）。**Phase 乙(outbound-direct)carry**。之前 **W23-A OpCo ledger 手動管理 ✅**[ADR-0007;`PATCH /license/ledger/:id` 逐格 set allocated/assigned + `LedgerAdjustment` audit + OPCO_IT scope 403 + 對回機制 activated;assigned 語意擴展[assign +1 並存人手校正];over-alloc flag 不 block;api 157→165 test;live curl + DB audit 驗;DESIGN §5/§6/§10 更新;拆 **W23-B** frontend inline edit 待做]。**W23-B Assets inline edit ✅**[Row edit mode ✎→input+Save/Cancel · `useUpdateLedger` · `evaluateLedgerDraft` 純函數 · web 63→75 · **live browser 端到端驗**改 661→665→Save→表+stats 2371→2375+toast · 負數擋 · light+dark]。之前 **W22 AUTH-3b 前端真 role scope ✅** — 純前端 display/gating + identity-hook,**無 schema/dep/ADR**:`useMe`(`GET /me` SSOT)+ local profile initialData · 退 `store/ui.ts` 假 `role` toggle · `roleScopeLabel` subtitle · role badge[menu/sidebar/settings]· `canSeePlatform`/`canSeeAdminNav` proactive gate[Platform switcher/admin nav]· 「My queue」[handledById===me.id,client-side];web 48→63 test;live **ADMIN vs OPCO_IT 對照**[subtitle「Admin — all OpCos」vs「RHK — RHK only」· admin nav 見/隱 · Platform 見/隱 · My queue tab]+ backend RHK scoping 仍 work;真 OPCO_IT **SSO** e2e 隨 AUTH-2b。之前 **W21 AUTH-4c-B session hardening ✅** — **ADR-0006 §7**[H1 RefreshToken schema / H2 cookie-parser dep / H4 cookie·refresh]:本地 session localStorage Bearer → **httpOnly + SameSite=Strict cookie + rotating refresh token**[access 15min / refresh 7d]· guard **cookie-first**[local]/ Entra header 不變 · `refresh-token.service`[randomBytes 256-bit + SHA-256 只存 hash · rotate 防重放]· `POST /auth/refresh` · `logout` · 前端無 token + `credentials:include` + 401 single-flight refresh-retry;api 140→157 · web 40→48 test;live curl cookie 端到端 + browser[vite proxy Set-Cookie 轉發 · sign-out 401];cookie-parser 唯一新 dep[Chris approve]。之前 **W20 AUTH-4c-A 密碼自助管理 ✅** — **ADR-0006**[H1 additive schema 4欄:mustChangePassword/failedLoginCount/lockedUntil/passwordChangedAt,無新 dep]:改密碼 `PATCH /me/password` + admin 重設 `POST /admin/users/:id/reset-password` + 嚴格 policy[min12/≥3類/≠email/新≠舊]+ force-change-on-first-login[FE gate + backend 403]+ lockout[per-account 5次/15min];api 121→140 · web 35→40 test;live curl 端到端 + FE browser。之前 **W19 AUTH-4b 本地 user 管理 ✅** — 無 schema/dep:admin CRUD `GET/POST/PATCH /admin/users` + `GET /admin/opcos` + Settings › Users&roles 真表[local+SSO]取代 stub;`@Roles(ADMIN)` + 安全閘[last-admin / 409 / 400×3];api 109→121 · web 25→35 test;live 端到端。之前 **W18 AUTH-4a 本地密碼登入 ✅** — **ADR-0005**[本地密碼認證與 Entra 並存]:dual-provider `AppUser`[entraOid nullable + passwordHash + authProvider]+ **argon2id** + `POST /auth/login`[本地簽發 HS256 JWT]+ **guard dual-issuer**[iss uop-local→HS256 resolve by sub / 否則 Entra;Entra config 改 optional + no-provider fail-fast]+ seed 本地 admin[env `LOCAL_ADMIN_INITIAL_PASSWORD`]+ 前端 Login form 接 + localStorage session + `authHeader` local 優先 + sign-out;**argon2 native-build 首閘過**;api 100→109 test · web 24→25;live login 端到端[backend curl + FE browser:login→token→guarded 200→/me→sign-out;錯密碼 401 + FE error]+ dev-bypass regression;**解 IT-app-reg 卡死**[本地有真登入,唔等 Entra];分階段 **4b user 管理 / 4c 密碼生命週期** 待做。之前 **W17 FE-Assets-platform ✅** — Assets 加 **Platform mode**(mode 切換 By-OpCo/Platform + tenant 三層表 Owned/Allocated/Assigned/Unalloc 按 category 分組 + subtotal + grand-total + 3 recon tile + over-alloc pill;consume `/license/tenant-skus` + `/stats`);**403 graceful restricted state**(OPCO_IT 點 Platform,零 FE-role 依賴,tab 常顯);純前端無 schema/dep/ADR;web 17→24 test;live 截圖+DOM 驗[ADMIN grouped table + e3 over-allocated + light+dark;OPCO_IT 403 restricted / By-OpCo 2 RHK 行];**Assets 兩 mode 齊,Compare 仍 future**。之前 **W16 BE-tenant-owned ✅** — `GET /license/tenant-skus` rows[per-SKU tenant 三層:owned(TenantSkuSnapshot.prepaidEnabled)/allocatedToOpcos(Σ ledger)/assignedToUsers/unallocated/overAllocated]+ `/tenant-skus/stats`[totalOwned/Allocated/Assigned/Unallocated/skusOverAllocated];`@Roles(ADMIN,REGIONAL)` **OPCO_IT 排除**[Platform=管理視圖];純 query-layer 無 schema/dep/ADR[TenantSkuSnapshot 已存 owned 數,唔打 Graph];api 96→100 test;live 驗[ADMIN e3 over-allocated alloc 2285>owned 2000/e1 unalloc 14 · stats totalUnallocated −271 · OPCO_IT 403];**FE Assets Platform mode 解封**。之前 **W15 FE-Assets ✅** — License Assets 畫面[By-OpCo 誠實表:OpCo|SKU|Allocated|Assigned|Available|Utilization bar/%|狀態,consume `/license/ledger`]+ OpCo filter chips + SKU search + stats strip[`/ledger/stats`]+ **Overview 第 4 KPI 換「Licenses assigned」**;純前端無 schema/dep/ADR;web 8→17 test;live DOM 驗[ADMIN 4 行/2-OpCo · run-as OPCO_IT 只 2 RHK 行/scoped · Overview Licenses assigned · light+dark 取色];honest gap = Platform tenant-Owned/Unalloc[後端無 endpoint]留 note→新 candidate。之前 **W14 BE-ledger-read ✅** — `GET /license/ledger` rows[headroom/overAllocated]+ `/ledger/stats`,opco-scoped[AUTH-3a]、active-only、純 query-layer 無 schema/ADR;api 92→96 test;live scoped 驗[ADMIN 4/2-OpCo · OPCO_IT 2/自己]。之前 **W13 allocation-import ✅ + DD-1 Close** — Chris 決 admin CSV upload[ADR-0004];`POST /license/ledger/import`[dry-run+commit,curation-as-scope,**allocatedQuantity-only** invariant]+ FE Settings›Integrations upload UI;api 81→92 test,live round-trip[dry-run 4/commit 4/idempotent 0]+ FE upload light+dark 驗;**DD-1 close → BE-ledger-read / FE-Assets 解封**[殘留=生產 curation deploy ops step]。之前 **CH-001 FE-bundle-split ✅** — `manualChunks` 拆 react/msal/query vendor,587KB 單 chunk → 最大 254KB « 500KB 警告消失,零 runtime 改;首個 Change workflow CH doc;查證 MSAL root singleton 不可 lazy 故只拆 vendor。更前:**tech-debt 清理批次** — DS-flag ✅ resolved[Avatar tokenize `--accent-deep` 消硬 hex + design-system.md DS-7 加 Avatar 例外 + SKILL.md 同步];FE-vuln → **defer DD-2**[實跑證非-force `npm audit fix` 一個都清唔到,全繫 breaking major];新登 DEFERRED DD-1[allocation-import]/DD-2[npm-vuln]+ RISK R2[dev-bypass 誤帶 prod,🟢 Mitigated]。前:**AUTH-1 ✅**[W09 後端 Entra JWT 驗證 + `@Roles(ADMIN,REGIONAL)` guard 落 11 endpoint,ADR-0002 + dev-bypass,api 56 test,401/200 wiring live 驗];**FE-Assets 被 allocation-import 卡** → 轉咗 AUTH。下一個 = AUTH-2[FE SSO,前置 IT 開 SPA app reg] / AUTH-3[OPCO_IT scope] / FE-Assets[前置 allocation import 決定])

---

## 狀態 lifecycle
`候選`(已識別未規劃)→ `已規劃`(plan 建咗)→ `進行中` → `完成` / `defer`(實證或決定暫不做)/ `blocked`(卡外部·用戶決定)

分區按「可開工性」:**A 可立即開工** / **B 已設計·暫緩** / **C blocked on 外部** / **D 已實證 defer** / **E 持續技術債** / **F out-of-scope**。

---

## 進行中(Active — 當前處理中)

| ID | 任務 | 狀態 | 下一步 / 阻塞 | 來源 |
|---|---|---|---|---|
| W01 | Backend Bootstrap（monorepo `apps/api` + PrismaModule + docker-compose） | ✅ **完成**（2026-07-09；G1-G4 全 pass） | — | `W01-backend-bootstrap/`（retro 已寫） |
| W02 | **Module C:SKU Catalog 字典 + 總量層對帳 / drift**（純後端） | ✅ **完成**（2026-07-09；G1–G4 全 pass） | — | `W02-catalog-reconcile/`（retro 已寫） |
| W03 | **Module D-1:Request 生命週期骨架**（intake → line items → triage → stage machine;無 side-effect） | ✅ **完成**（2026-07-09；G1–G4 全 pass） | — | `W03-request-lifecycle/`（retro 已寫） |
| W04 | **Module D-2:履行動作**（sync gate → `assignLicense` → `assignedQuantity` +1 → 回寫 ServiceNow → `ASSIGNED`） | ✅ **完成**（2026-07-09；G1–G4 全 pass） | — | `W04-assign-fulfilment/`（retro 已寫） |
| W05 | **FE-scaffold**:`apps/web` app shell + token/theme（Vite+React+TS+Tailwind+shadcn;H6） | ✅ **完成**（2026-07-09；G1–G5 全 pass;light+dark 截圖驗） | — | `W05-fe-scaffold/`（retro 已寫） |
| FE-1 | **前端畫面 1**:Overview dashboard + **SKU Catalog**（首次接後端 `/license/*` `/fulfilment/*` via TanStack Query;OD 全 default = A。**2026-07-09 deviation**:第二個 screen License Assets → SKU Catalog,因 Assets 全靠 ledger 無 endpoint） | ✅ **完成**（2026-07-09；G1–G6 全 pass;light+dark 截圖驗;真數 seed 驗） | — | `W06-fe-overview-assets/`（retro 已寫） |

| FE-2 | **前端畫面 2**:Requests 列表 + Request detail（讀 + **寫操作** advance/assign/sync;OD1=B）——首個寫操作 UI,接 `/fulfilment/requests*` | ✅ **完成**（2026-07-09；G1–G6 pass;advance/mark-synced round-trip 端到端驗;light+dark） | — | `W07-fe-requests/`（retro 已寫） |
| FE-3 | **前端畫面 3**:Drift Alerts（接 `GET /license/drift` 真數 + `POST /license/reconcile`;OD1=A 只 Drift,Settings/Login 隨 AUTH defer）——含 **BE-graph-harden**（reconcile/catalog `getSubscribedSkus` wrap→503,OD2=A） | ✅ **完成**（2026-07-10；G1–G7 全 pass;light+dark 對 prototype;harden round-trip[503 toast + API 唔 crash] live 驗;api 42 test 綠） | — | `W08-fe-drift-harden/` |
| W13 | **Allocation import**（O365 Excel matrix → `OpcoSkuLedger.allocatedQuantity`;`POST /license/ledger/import` dry-run+commit,curation-as-scope,**allocatedQuantity-only** invariant + FE Settings›Integrations upload UI） | ✅ **完成**（2026-07-13;G1–G7 全 pass;ADR-0004;api 81→92 test;live round-trip[dry-run 4/commit 4/idempotent 0]+ FE upload light+dark 驗;**DD-1 close**） | — | `W13-allocation-import/` · `docs/adr/0004-*` |
| W14 | **BE-ledger-read**（`GET /license/ledger` rows[headroom/overAllocated]+ `GET /license/ledger/stats`,opco-scoped,active-only） | ✅ **完成**（2026-07-13;G1–G4 全 pass;純 query-layer 無 schema/ADR;api 92→96 test;live scoped 驗 ADMIN 4/2-OpCo · OPCO_IT 2/自己;**FE-Assets/Overview KPI 解封**） | — | `W14-ledger-read/` |
| W15 | **FE-Assets**（License Assets 畫面:By-OpCo 誠實表 consume `/license/ledger` + OpCo filter chips + SKU search + stats strip;**Overview 第 4 KPI → Licenses assigned** consume `/ledger/stats`） | ✅ **完成**（2026-07-13;G1–G7 全 pass;純前端無 schema/dep/ADR;web 8→17 test;live DOM 驗 ADMIN 4 行/2-OpCo · run-as OPCO_IT 只 2 RHK/scoped · Overview Licenses assigned · light+dark 取色;honest gap Platform tenant-Owned→新 candidate） | — | `W15-fe-assets/` |
| W16 | **BE-tenant-owned**（`GET /license/tenant-skus` per-SKU 三層 owned/allocatedToOpcos/assignedToUsers/unallocated/overAllocated + `/tenant-skus/stats`;ADMIN/REGIONAL only） | ✅ **完成**（2026-07-13;G1–G5 全 pass;純 query-layer 無 schema/dep/ADR[TenantSkuSnapshot.prepaidEnabled 已存];api 96→100 test;live ADMIN e3 over-allocated/e1 unalloc 14 + stats · OPCO_IT 403;**FE Platform mode 解封**） | — | `W16-tenant-owned/` |
| W17 | **FE-Assets-platform**（Assets Platform mode:mode 切換 + tenant 三層表按 category 分組 + subtotal + grand-total + recon tile;403 graceful restricted） | ✅ **完成**（2026-07-13;G1–G6 全 pass;純前端無 schema/dep/ADR;web 17→24 test;live 截圖+DOM ADMIN grouped table + e3 over-allocated + light+dark · OPCO_IT 403 restricted;Compare 仍 future） | — | `W17-fe-assets-platform/` |
| W18 | **AUTH-4a 本地密碼登入**（dual-provider AppUser + argon2 + `POST /auth/login` 本地 JWT + guard dual-issuer + seed 本地 admin + 前端 Login form/session/sign-out;ADR-0005） | ✅ **完成**（2026-07-13;G1–G7 全 pass;**ADR-0005**[H1/H2/H4];schema migration;api 100→109 test · web 24→25;live login 端到端 backend+FE + dev-bypass regression;argon2 native-build 首閘過） | — | `W18-auth-local-login/` · `docs/adr/0005-*` |
| W19 | **AUTH-4b 本地 user 管理**（admin CRUD `GET/POST/PATCH /admin/users` + `GET /admin/opcos` + Settings › Users&roles 真表取代 stub;`@Roles(ADMIN)`;admin 打初始密碼;表涵蓋 local+SSO） | ✅ **完成**（2026-07-13;G1–G8 全 pass;ADR-0005 §6;**無新 schema/dep**[純 query/service-layer,同 AUTH-3a/W16];api 109→121 test · web 25→35;live backend curl 端到端[create→login→OPCO_IT scoped 200 · 無 hash · 409/400×3 · 非-admin 403 · last-admin guard 400] + FE browser[admin 4 行真數 · Add/Edit Dialog+Select · light+dark #E60027 · OPCO_IT 403 restricted];順手修 account sign-out MSAL orphan） | — | `W19-auth-user-admin/` |
| W20 | **AUTH-4c-A 密碼自助管理**（改自己密碼 `PATCH /me/password` + admin 重設 `POST /admin/users/:id/reset-password` + 嚴格 policy + force-change-on-first-login + lockout;guard `ensurePasswordChanged` backend 強制） | ✅ **完成**（2026-07-13;G1–G10 全 pass;**ADR-0006**[H1 additive schema 4欄,無新 dep];api 121→140 test · web 35→40;live curl[force-change gate 403 · change 204→解封 · admin-reset→再鎖 · lockout] + FE browser[/change-password gate + Settings password + Must-change badge + dark token]） | — | `W20-auth-password-lifecycle/` · `docs/adr/0006-*` |
| W21 | **AUTH-4c-B session hardening**（refresh token + httpOnly cookie:localStorage Bearer→**httpOnly+SameSite=Strict cookie** + rotating refresh + guard cookie-first + `POST /auth/refresh`·`logout`；ADR-0006 §7） | ✅ **完成**（2026-07-14,W21;G1–G10 全 pass;**ADR-0006 §7**[H1 RefreshToken schema / H2 cookie-parser dep / H4 cookie·refresh]；access 15min+refresh 7d rotating；guard cookie-first[local]/Entra header 不變；api 140→157 · web 40→48 test；live curl cookie 端到端[set httpOnly · rotate 防重放 · logout revoke · force-change gate cookie]+ browser[vite proxy Set-Cookie 轉發 · storageHasJwt=false · sign-out 401]；cookie-parser 唯一新 dep[Chris approve]） | — | `W21-auth-session-hardening/` · `docs/adr/0006-*` §7 |
| W22 | **AUTH-3b 前端真 role scope**（consume `/me` 真 role/opcoScope + 退假 `role` toggle + role badge + `canSeePlatform`/`canSeeAdminNav` gating + 「My queue」解封;純前端 display/gating + identity-hook） | ✅ **完成**（2026-07-14,W22;G1–G10 全 pass;**純前端無 schema/dep/ADR**；`useMe` `GET /me` SSOT + local profile initialData；退 `store/ui.ts` 假 toggle；web 48→63 test；live **ADMIN vs OPCO_IT 對照**[subtitle/admin nav/Platform switcher/My queue]+ backend RHK scoping 仍 work） | 真 OPCO_IT **SSO** e2e 隨 AUTH-2b（🔴 IT app reg） | `W22-auth-frontend-role/` |
| W23-A | **OpCo ledger 手動管理（backend + audit）**（`PATCH /license/ledger/:id` 逐格 set allocated/assigned + `LedgerAdjustment` audit + OPCO_IT scope 403 + 對回機制啟動;ADR-0007） | ✅ **完成**（2026-07-14;G1–G8 全過[G6 邏輯];**ADR-0007**[H1:assigned 語意擴展 + 新寫入路徑 + LedgerAdjustment schema,additive];api 157→**165** test;live curl set assigned→200/負數 400/空 400 + DB audit;DESIGN §5/§6/§10 更新） | **W23-B** frontend inline edit（另 kickoff,backend 已就緒） | `W23-assets-manual-ledger/` · `docs/adr/0007-*` |
| W23-B | **Assets By-OpCo inline edit**（Row edit mode:每行 ✎ → allocated/assigned input + Save/Cancel + reason;consume W23-A `PATCH /license/ledger/:id`） | ✅ **完成**（2026-07-14;G1–G8 全過[G5 靠 backend];**純前端無 schema/dep/新 ADR**;`useUpdateLedger` + `evaluateLedgerDraft`;web 63→**75** test;live browser 端到端[改→Save→表+stats+toast · 負數擋 · light+dark #E60027]） | OPCO_IT run-as live 對照（低風險 carry） | `W23-B-assets-inline-edit/` |
| W24 | **Phase 甲 inbound intake**（n8n onboarding request push → 平台 `POST /requests/intake` + m2m auth + 即時建本地 Request mirror;ADR-0008 D2/D4/D6） | ✅ **完成**（2026-07-15;D0-D4 全 pass;n8n=Chris 本人,AGENDA 10 條自答→合約 lock;`IntakeKeyGuard` fail-closed + `IntakeService` two-level REQ/RITM mirror + idempotent + additive migration + 回寫 per-line RITM;api 157→**176** test;端到端 curl 4 case[401/建/idempotent/400]+ e2e data 清） | **Phase 乙 ✅**（見 W25）| `W24-request-intake/` · `docs/adr/0008-*` |
| W25 | **Phase 乙 outbound direct**（IT 平台開單 → 主動 create sc_request/sc_req_item → 建 mirror + 前端開單畫面;ADR-0008 D1/D3/D4） | ✅ **完成**（2026-07-15;D0-D7 全 pass;`createRecord` + `RequestSubmissionProvider`/`DirectServiceNowProvider` + `POST /requests`[two-level mirror,origin=platform-created,SN-first fail-closed]+ `Request.origin` migration + `GET /opcos` enabler + 前端 `/requests/new`;api 176→**187** · web 75→**85** test;live curl[opcos 200/requests 400]+ browser[render+picker 真數];代表性 SN 合約待 live 對齊） | **Phase 丙 ✅**（見 W26）| `W25-request-outbound-direct/` · `docs/adr/0008-*` |
| W26 | **Phase 丙 n8n outbound**（平台開單改走 n8n webhook — `N8nWorkflowProvider` + provider 選路 + webhook 合約;ADR-0008 D2/D3） | ✅ **完成**（2026-07-15;D0-D5 全 pass;**backend-only 零 schema/dep/migration/前端改**;3 Fork 拍板[F1 代表性 mock / F2 同步回 / F3 config 單選];`N8nWorkflowProvider`[POST webhook→同步 response→`SubmittedRequest`,fail-closed 5 規則]+ exported `requestSubmissionProviderFactory`[env `REQUEST_SUBMISSION_PROVIDER`,default direct 不破]+ `CONTRACT-OUTBOUND.md` + `.env.example`;consumer[`OutboundRequestService`/`/requests/new`]零改 = 抽象紅利;api 187→**197** test[provider 6/factory 4];live boot smoke[n8n fail-fast crash stack trace 證 DI wiring / default direct 200 + POST /requests 400];代表性 webhook 合約待 Chris[n8n owner]live 對齊[CONTRACT-OUTBOUND §6]） | **Phase 丁 ✅**（見 W27）| `W26-request-n8n-outbound/` · `docs/adr/0008-*` |
| W27 | **Phase 丁 D365 scope**（D365 license 完整納入 — assign/catalog/ledger/對帳/drift 一視同仁;ADR-0008 D5） | ✅ **完成**（2026-07-15;D0-D4 全 pass;**grounding 揭平台早 SKU-agnostic — 無 code filter 要拆**[curation gate 只在 allocation-import 一處;ADR-0004 當年 reject name-denylist]→ scope A[confirm+test+doc,Chris AskUserQuestion];**零 schema field/機制/前端改**[schema 只改 comment,prisma validate ✓];4 個 D365-parity lock-in test[allocation-import curated-D365 import / reconcile drift / assign +1 / tenant-owned;api 197→**201**]+ doc-sync[architecture/DESIGN 確認已 ADR-0008 synced · ADR-0004 範圍註 · surgical de-M365-only main.ts title+3 schema comment]+ `CURATION-D365.md` deploy-time runbook;真數 curation = deploy-time ops[ADR-0004]） | **ADR-0008 rollout 收官**;回 BACKLOG 揀 pending | `W27-d365-scope/` · `docs/adr/0008-*` |
| W28 | **權限矩陣**（由 `@Roles` runtime derive → `GET /admin/permissions` + Settings › Permissions 唯讀 tab + drift/unguarded 防護 test;ADR-0009 Decision 8.5 落地 = audit rollout item 2) | ✅ **完成**（2026-07-20;G1–G7 全 pass;**零 schema/dep/新 ADR,零行為改動**;F0 spike 先行[metadata 可讀 ✅ / jest 載唔到 AppModule ❌ `jose` ESM → F1 用 `DiscoveryService`·F3 用 glob,共用 `derivePermissions()` 純函數,**兩路唔一致本身即 bug signal**];**live 34 route / 10 controller / 零 unguarded**;`/requests/intake` 正確報 `m2m`+`IntakeKeyGuard` 而非 public;api 213→**223** · web 85 不降 · **全 repo lint 0**;**fails-before 雙實證**[改 `@Roles`→snapshot 紅 / 加無 guard route→unguarded 紅];403 三重驗證;light+dark 驗) | — **W29 已接力完成** | `W28-permission-matrix/` · `docs/adr/0009-*` |
| W29 | **`AuditLog` 落地 + Audit UI**（ADR-0009 Decision 4 十三個事件 + 三層白名單基建 + `GET /admin/audit` + 獨立 `/audit` 頁 = audit rollout item 3;**Chris 四項 audit 需求最後一項**) | ✅ **完成**（2026-07-21;**G1–G8 全 8/8 pass**;**ADR-0009 已 Accepted 故無新 ADR**,schema additive[`AppUser` 只加 relation field]零新 dep;**H4 三層邊界**[per-target allow-list / 永久 blacklist 覆蓋 whitelist / `metadata` 固定 key set]**test 先行寫喺任何 hook 之前**,G2 硬紅線由頭到尾冇一刻未驗證;13 事件落 6 個既有 service 且**與主操作同一 `$transaction`**[R1 實績:既有 test 冇一條 assertion 要改 —— `$transaction` mock 傳返同一個 prisma mock];`GET /admin/audit` ADMIN-only[P-B 連帶義務,放寬須重開 ADR]+ 篩選 + 分頁 cap 100 雙重防守;獨立 `/audit` 頁[零 primary action · `canSeeAdminNav` gate · 403 restricted]+ **`design-system.md` §6 新增「prototype 以外 owner-approved 畫面」表**登記;api 213→**263**[31→32 suites]· web 85→**92**;live 403 三重驗證 + **G2 DB 抽查真 row 冇 secret** + browser light+dark;**W28 迴歸網當日見效**[加 `AuditController` → permissions snapshot 即捉到新 route 要 review]） | ✅ carry **已收**:self-service 改密碼 audit —— Chris 2026-07-21 approve → 補 `user.password_change`(trivial change,PROCESS §1.1;ADR-0009 Decision 4 已補註,**首階段覆蓋 14 個事件**;api 266 · web 93;live 真 session 驗證[關 dev-bypass → login → `/me` 403 force-change gate → change 204 → DB 見 reset(actor≠target)vs change(actor===target)並排,payload 全空]) | — **W30 已接力**(整合 rollout item 4) | `W29-audit-log/` · `docs/adr/0009-*` |
| W30 | **Integration 狀態 + Test connection**（`GET /admin/integrations` 三態 read-model + 唯讀探針 + Integrations tab 真內容;ADR-0010 item 4 = INTEG-1) | ✅ **完成**（2026-07-21;**G1–G9 全 9/9 pass**;**零 schema / 零新 dep / 零新 ADR**;**設計上兩個誠實修正**:三態取代恆真嘅 `configured`[Graph/SN/intake 喺 constructor `getOrThrow`,缺就 boot 唔起]· n8n inbound **交白卷**[`origin` default 令 seed 同真 intake 分唔開,寧可講「無法區分」都唔畀錯時間];探針**零新 vendor 方法**,**n8n webhook 永不探**[會建真 ticket];**兩條硬紅線 test 先行**[餵假 secret assert 零洩漏 / assert 零 write call];api 266→**286** · web 93→**99**;live 全驗[零 env 值 · 403 三重 · provider 切換 · 429 節流 · light+dark]+ **D6 清 DocuWare 文案**;順手修探針 `201`→`@HttpCode(200)`) | 🟡 `lastProbe` 唔持久化 = D4 直接後果 —— 運維若要探測歷史 = **重開 OQ-B 訊號**,唔好靜靜加 model | `W30-integration-status/` · `docs/adr/0010-*` |
| **W37** | **排程 sync sweep**（ADR-0015 落地:`@Cron` 每 10 分鐘向 Graph `findUser` 證實仲未過 gate 嘅 request,命中即開 `azureSyncedAt` + `RequestEvent(SYNC)`;人手 `PATCH /sync` 保留做 break-glass 但文案明文自認未經證實） | ✅ **完成 · closed**（2026-07-27;OQ1=A · OQ2=B · OQ3=A;**零 schema / 零新 dep**;api 410→**429 / 42 suites**,lint 0;**全 repo 第一個 `@Cron`**） | 🔴 **兩處偏離 ADR**(都 owner approved + changelog):**D5** 放棄 `SYNC_SWEEP_CRON`(`@Cron` 參數喺 DI 之前求值;動態註冊要引 `cron` = **H2**)· **D4** audit 形狀(同 W36 D6 同一類,但**今次開工前就發現**)· 🔴 checklist 自己寫錯一項(audit 要同 transaction)實作時修正,跟 `outbound-retry` 先例 · ⚠️ 多實例重複跑**照 ADR 刻意唔解** · 📌 **RISK R3** 由 ⚠️ Open → 🟡 Mitigating | `W37-sync-sweep/` · `adr/0015-*` · `RISK_REGISTER` R3 |
| **W36** | **OpCo 預算 assign 硬 gate + ADMIN 具名 override**（ADR-0016 落地:`assigned+1 > allocated` → 400;ADMIN 必填理由 override 並入 audit;前端 override 入口;部署 preflight SQL + runbook） | ✅ **完成 · closed**（2026-07-27;OQ1 = **選項 A**;F1–F5 全交付;**零 schema / 零新 dep**;api 390→**410** · web 151→**180**,兩邊 lint 0 + `tsc && vite build` 過;**D7 四條硬邊界 diff 全部為 0**;live 拒絕路徑**零 DB 改動**驗 —— dev 天然就有冇 ledger row 嘅 READY line item） | 🔴 **D6 偏離**(owner approved):擴 ADR-0009 白名單,否則 `budgetOverride`/`allocated`/`assignedBefore` 會被 `pickAuditMetadata` **靜靜丟棄** · 🚧 **gate 400 本身 dev 驗唔到**(D5 排喺 `graph.findUser` 之後)→ **runbook 步 4 部署後第一項檢查**;**冇用真人 UPN 硬闖** · 🔴 F5 SQL 捉到 **plan §2 自己錯**(inactive 12 唔係 6 ⇒ active **10**);根因 **`skuPartNumber` 唔唯一**(§13) · ➕ 揪出新一類:**冇 ledger row 但有 pending line item**(D1 下每次 assign 都擋) · 🟡 **TD-1**(`/audit` 篩選 option 落後 backend 5 樣,W31/W34 遺留,**冇順手修**) | `W36-opco-budget-gate/` · `adr/0016-*` · `docs/05-usage/OPCO-BUDGET-GATE-ROLLOUT.md` |

> **開發路線（2026-07-10）**:W02 C ✅ → W03 D-1 ✅ → W04 D-2 ✅（後端業務層）→ **W05 FE-scaffold ✅ → FE-1 ✅**（W06）→ **FE-2 ✅**（W07）→ **FE-3 ✅**（W08,Drift + BE-graph-harden）→ **AUTH-1 ✅**（W09,後端 JWT 驗證 + role guard）。**路線調整**:原打算 FE-Assets,但 discovery 揭 **FE-Assets 被 allocation-import[deferred Excel data 決定]卡死**（seed 唔播 ledger、`allocatedQuantity`=0 → owned/utilization 無真數）→ Chris 轉做 AUTH。下一個:**AUTH-2**（FE SSO,前置 IT 開 SPA app reg）→ **AUTH-3**（OPCO_IT scope）/ **FE-Assets**（前置 allocation import 決定）→ Settings/Login（隨 AUTH-2）→ DEPLOY。**BUG-002 ✅ / BE-graph-harden ✅ / AUTH-1 ✅**。
>
> **路線更新（2026-07-14）**:AUTH 全鏈完成 — W09 **AUTH-1** → W10 **AUTH-2a** → W11 **AUTH-3a** → W18 **AUTH-4a**(本地登入)→ W19 **AUTH-4b**(user 管理)→ W20 **AUTH-4c-A**(密碼生命週期)→ W21 **AUTH-4c-B**(session hardening)→ W22 **AUTH-3b**(前端真 role scope)全 ✅;FE-Assets 鏈 **W13→W17** 全 ✅。**ADR-0008 rollout 全 4 階段 ✅ 收官**(2026-07-15;甲 inbound intake[W24]→ 乙 IT 開單 create SN + two-level mirror + 前端 /requests/new + GET /opcos[W25]→ 丙 平台開單改走 n8n webhook[`N8nWorkflowProvider` + env 選路,default direct,抽象紅利 consumer 零改,W26]→ **丁 D365 license 完整納入[W27]**[grounding 揭平台早 SKU-agnostic 無 code filter → scope A confirm+test+doc,零 schema field/機制改,4 D365-parity lock-in test + CURATION-D365 runbook];api 201 · web 85 test)。**真數 D365 curation + SN/n8n 合約 = deploy-time 對齊**。**SN 建單合約對齊 checklist 已備**(可填答交 ServiceNow owner):`docs/05-usage/SERVICENOW-CONTRACT-ALIGNMENT.md`(🅐機制[Table API 直插 vs Catalog order]/🅑REQ 欄/🅒RITM cat_item[⚠️現 skuId 係 placeholder]/🅓user·OpCo 解析/🅔response/🅕auth/🅖回寫/🅗n8n webhook/🅘inbound 核對/🅙idempotency)。其餘 pending:🔴 **AUTH-2b**(真 SSO e2e,卡 IT app reg)· 🟡 **AUTH-4c-C**(email reset,deferred)· 🟡 **DD-2**(npm vuln,deferred)· honest-gap 三項(activity feed / Drift Resolve / AI-Assist)· **DEPLOY**(生產部署 + 真數 curation)。詳見 A 區候選行 + B/C/D 區。
>
> **路線更新(2026-07-27)**:Chris 提出兩個 onboarding 缺口 → **CH-009**(assign 前可用量可見度,✅ PR #29)+ **ADR-0015**(sync sweep,Accepted **未實作**)+ **ADR-0016**(預算 gate)→ **W36 ✅ 收官**(gate + ADMIN 具名 override + 前端入口 + preflight SQL/runbook;api 410 · web 180)。
> **W37 ✅ 收官**(2026-07-27):**SYNC-sweep** 落地,全 repo 第一個 `@Cron`;**RISK R3** 由 ⚠️ Open → 🟡 Mitigating。
> **CH-008 ✅ 收官**(2026-07-27):0/0 ledger 行**預設隱藏**(server-side,兩個 GET 收 `?includeEmpty=true`)+ `assetStatus` 加 `Empty`/neutral 修正「0/0 顯示成綠色 Headroom」誤導。零 schema / 零 dep / 零 ADR;api **433** · web **188**。⚠️ **R1 仍然成立**:隱藏之後靠 toggle 搵返該格,因為 **DD-3**(冇 create endpoint)未解 —— toggle = 唯一入口,唔可以之後順手收埋。
> **下一個候選**(全部無新阻塞):**DEPLOY-harden** / **AUTH-2b**(兩者都卡 IT app reg)· **TD-1**(`/audit` 篩選 option 補齊)· **OD1 daily reconcile**(W37 已鋪好 `@Cron` pattern,但 ADR-0015 明文只係「鋪路」,要做就開新 phase)· **Drift-resolve**(⬆️ 相對更值錢咗:CH-008 之後 DD-3 嘅 create 缺口更加浮面)。

---

## A — 可立即開工

| ID | 任務 | 狀態 | 前置 / 下一步 | 來源 |
|---|---|---|---|---|
| **SEC-001** | 🔴 **n8n reference JSON 內含明文憑證** —— `docs/06-reference/03-n8n-workflow/phase 1/1002 …AD.json` 個 disabled node `Execute Command (Setup ABW Share Folder) (PRD)2` 入面有 WinRM 服務帳號 **+ 明文密碼**(Python `winrm.Session` 個 `auth=(...)` tuple) | ✅ **完成 · 收(2026-07-27)** —— 四步全做齊:①**從來冇入過 git history**(`git log --all` 該路徑零 commit ⇒ 唔使改寫歷史)②**已 scrub**(全目錄掃 52 處,**只有呢一處係真 secret**;user+password → placeholder,精確 1 處命中、53 個 node 原封、JSON 仍 parse)③**已 gitignore**(`git add -A --dry-run` 實證:連 `git add .` 都再拉唔到)④**Chris 已 rotate 密碼 + 拆走硬寫**(`auth=(...)` 改用 credential / `$env`) | ⚠️ **仍然生效嘅約束**:`docs/06-reference/03-n8n-workflow/` **維持 gitignore** —— 要收返入 repo = **有意識決定**(刪 `.gitignore` 嗰段),因為仍帶內部 email / AI system prompt / 基建 hostname / n8n credential id;真相 SSOT 係 n8n instance 本身 · 連帶(**非 secret,唔急,唔開新 item**):phase 2 四條仍 `CHANGE_ME_SHARED_SECRET` placeholder;2004 hardcode DEV host | **`docs/13-deployment/08-n8n-integration-go-live.md` §3(rotate 三步 runbook)** · `docs/06-reference/03-n8n-workflow/` · `.gitignore` · CLAUDE.md §5 **H4** · W36 retro C6 |
| INIT | `git init` + 首個 baseline commit（框架落地基線） | 完成（`5ff2cae`,main） | — | CLAUDE.md §4 |
| BUG-001 | **H4:`GraphService` log 咗 UPN（PII）**（assignLicense + findUser 錯誤） | ✅ 完成（2026-07-09；Sev3;fix + regression test,實證 fails-before） | — | `docs/03-implementation/bugs/BUG-001-graph-logs-upn-pii/` |
| BUG-002 | **後端 assign crash**:`findUser` throw Graph/MSAL error（非 return null）未 wrap → invalid status(-1)→ **NestJS process crash**（critical path robustness;FE-2 assign 測試揭出） | ✅ **完成**（2026-07-09;Sev2;3 個 Graph await wrap → 503 + regression test,實證 fails-before;api 40 test 綠） | — | `docs/03-implementation/bugs/BUG-002-assign-graph-error-crashes-api/` |
| BE-graph-harden | **catalog sync / reconcile 呼 `getSubscribedSkus` 相同 latent crash**（Graph throw 未 wrap）——BUG-002 carry-over | ✅ **完成**（2026-07-10,W08 OD2=A;新 `graph-unavailable.ts` 共用 helper,reconcile/catalog wrap→503 + regression test,assign 改用同 helper;live 驗 reconcile/catalog 皆 clean 503 + API 唔 crash） | — | `W08-fe-drift-harden/` · `integration/graph/graph-unavailable.ts` |
| BUG-003 | **outbound 建單整合失敗 → opaque 500**:`/requests` 提交打真 SN(placeholder 不可達)`fetch failed`,`provider.submit()` 未 wrap → generic「Internal server error」（error-handling gap,同 BUG-002 pattern 但漏咗 outbound SN/n8n 路徑） | ✅ **完成**（2026-07-15;Sev3;`OutboundRequestService` wrap `provider.submit()`→`ServiceUnavailableException`[503,provider-agnostic direct+n8n,fail-closed 不變]+ 更新 spec test;前端零改[已 surface message];api 201 test;live 驗 500→503「ServiceNow is unavailable…」） | — | `docs/03-implementation/bugs/BUG-003-outbound-sn-fetch-opaque-500/` |
| **BUG-004** | **H4:`graphUnavailable()` 把 vendor error 原封 `logger.error`,而 Graph 404 body 個 request path 帶 UPN ⇒ UPN 入咗 log**。該檔自己個 comment 仲寫住「H4: never log the target UPN」—— **佢做唔到自己聲稱嘅嘢**。影響**全部**直接 Graph caller(assign / reconcile / catalog / sync-sweep / probe),由 BE-graph-harden 起就存在 | ✅ **完成**(2026-07-28,**Sev3**;Chris 拍板 fix = **scrub email pattern**,保留 AADSTS 碼 / HTTP status 等診斷內容)。新 `integration/scrub-pii.ts` **一個共用 helper**;掃全 repo 揾到 **12 處** raw-message log,**只修 4 處** —— 即直接處理 **user identity** 嘅 vendor 呼叫(`graph-unavailable` · `sync-sweep` ×2 · `n8n-license`);其餘 8 處(SN write-back / token / 探針)vendor 呼叫**唔涉及特定 user**,同一 pattern **唔係同一風險**,明列唔順手改(§1.3)。⚠️ **`sync-sweep` 原本唔喺登記範圍而佢最高危**(佢個 vendor 呼叫就係 `findUser`)。🔴 **條 test 要 assert 個 logger**:W38/W39 兩條既有 test 一直綠,因為佢哋 assert **exception message**(嗰度一直乾淨),而洩漏喺 **log line**,冇嘢望住 ⇒ 潛伏 18 日。➕ **靜態守門**防第五個 call site 漏(assert 冇 raw pattern **兼且**真 call `scrubPii`)· fails-before **2 failed / 16 passed** · **postmortem 已寫**(Sev3 但 recurring) | ⚠️ **已知限制,明文寫成一條 test**:`scrubPii` 係**網唔係保證** —— 捉 email-shaped token,捉唔到 `CN=jdoe,OU=Users` 呢類。**BUG-001 嗰條規矩仍然成立**(唔好自己攞 identifier 砌 log 字串),兩者互補唔互相取代 · 🔴 **唔可以把 regex 寫闊去捉更多** —— 咁會開始食走 AADSTS 碼,而嗰啲正正係我哋 log 呢段文字嘅**唯一原因** | `bugs/BUG-004-vendor-error-logs-upn/`(report / checklist / progress / **postmortem**)· `apps/api/src/integration/scrub-pii.ts` · CLAUDE.md §5 **H4** |
| **BUG-005** | **Integrations 面板嘅 provider 選路只讀 env,睇唔到 UI 改咗嘅 DB override** —— 面板話「未啟用」而平台其實已經行緊 n8n。`IntegrationStatusService` 用 `config.get()`(env only),而 runtime factory 用 `connectorConfig.resolve()`(**DB-then-env**,W34 起) | ✅ **完成**(2026-07-28,**Sev3**)。root cause:**W34 / ADR-0013 引入 resolver 時改咗 runtime 三個整合點,冇改報告狀態嗰個**;佐證 = `fulfilment.module:96` 個 comment 仍寫住「picked by **env**」,**stale 咗兩個月冇人察覺**,因為冇任何嘢對比過兩邊。Fix:兩個 selection 一次過改用 resolver + **移走 `ConfigService`** —— 唔係順手清理:兩者並存嘅話,「淨係讀一個值」離重新 drift 只差一行;而家呢個 service **冇任何路徑**繞得過 resolver。test mock 由「env bag」改成**真 DB-then-env resolver,而且用 production 同一份 `CONNECTOR_CONFIG` 做 env-key mapping**(手寫多一份 mapping 就係本 bug 同一種病)· fails-before **3 failed / 13 passed** | ⚠️ **連帶影響冇扮冇**:移走 `ConfigService` 令原本個 G1 secret leak test **攞唔到 secret 去餵** ⇒ 會變 trivially pass。已講清楚佢而家證緊咩(保證由「我哋檢查過 output」升級成「**根本冇一條線通去 secret**」),結構嗰半**另開一條 test** assert 個 service 冇 `@nestjs/config` · ⚠️ fails-before 順帶揭到 `a DB override back to the default…` 嗰條 **壞咗都照綠**(expect `inactive`,而壞嘅結果啱好都係 `inactive` —— **結果啱原因錯**),係 assert 單一 boolean 嘅固有限制,已記低 | `bugs/BUG-005-connector-state-ignores-db-override/` · ADR-0013 **D3** · ADR-0010 **D3** · W39 progress(當時明文延後) |
| DS-flag | **Avatar brand gradient `#8a0018`**:handoff 用非 token gradient,衝突 design-system.md DS-7「唯一 gradient=login」 | ✅ **完成**（2026-07-10;Chris 揀保留 hifi → tokenize `--accent-deep`[index.css]消硬 hex + DS-7 明文加 Avatar 例外 + SKILL.md 同步） | — | `apps/web/.../ui/avatar.tsx` · `index.css` · design-system.md DS-7 |
| FE-vuln | **npm dev/build-chain vulnerabilities**（monorepo root 32:apps/web 7[vite/vitest/esbuild/js-yaml/picomatch]+ apps/api 側 uuid/webpack/nest CLI;**全 dev-only 唔入 production bundle**） | 🚧 **defer → DD-2**（2026-07-10;實跑證非-force `npm audit fix`[root+`-w`]一個都清唔到,全繫 breaking major） | vite@8 生態 stabilize → 專門 phase 一次過升 + revalidate（H2,需 ADR） | `DEFERRED_REGISTER.md` DD-2 |
| FE-bundle-split | **前端 bundle > 500KB warning**（AUTH-2a 引入 MSAL 後 587KB;ADR-0003 已知） | ✅ **完成**（2026-07-13,CH-001;`manualChunks` 拆 react/msal/query vendor → 最大 chunk 254KB « 500KB,警告消失;零 runtime 改;lint/8 test/build + preview live 驗;**MSAL 不可 lazy 已查證**故只拆 vendor,route-lazy out） | — | `docs/03-implementation/changes/CH-001-fe-bundle-split/` |
| FE-fidelity | **全站 UI fidelity audit + harden**（shell topbar/sidebar + Login + Overview/Requests/Settings/Drift 對 prototype） | ✅ **完成**（2026-07-11,W12;Chris 報 UI 未跟 mockup → 全站 audit → 修 Tier1-3 🔧 真 drift:topbar collapse/⌘K/divider/tenant pill/user menu · sidebar CATALOG/admin nav/D365 · Login copy/footer/副標 · Settings 左 sub-nav + role block · Requests pill filter · Overview links/label · Drift 時間;token-only,web 8 test 綠,light+dark live 驗） | 🚧 **honest gap**（唔造假）：**已解** → Licenses-assigned KPI[W14 BE-ledger-read ✅]· My-queue/handler/真 role[W22 AUTH-3b ✅]· Users 表[W19 AUTH-4b ✅];**仍 pending** → Overview activity feed[需 events endpoint]· Drift Resolve/per-OpCo[DESIGN §5 方案甲 + §10 對回待設計]· AI-Assist[DESIGN §11 Roadmap]（見 A 區候選 FE-activity / Drift-resolve / AI-Assist） | `W12-fe-fidelity-harden/AUDIT.md` |
| CH-002 | **前端 fidelity 對齊**（Chris 手測發現跨 4 畫面 drift:request 內頁 remark/sync-gate 灰底盒 · timeline 連接線 · requests filter → bordered chip · Assets mode 容器/順序/Platform 雙段 bar+圖例/By-OpCo in-table 總數行 · Settings 用 Card 去多餘 h1/Account tab 重砌/Users 表頭併 Card+角色圖例卡） | ✅ **完成**（2026-07-16;A1–A10 + B1 + settings padding/寬度 2 修;build/lint/85test 綠;Chris browser 確認視覺一致;決策 B = 紅 accent + design-system §0.2 澄清;branch `feat/api-ch-003-catalog-edit` stacked）| **C 組**[Compare 矩陣 / Sync·Export·Manage·Adjust / ~~OpCos CRUD tab~~ ✅ CH-004 / AI 解析]仍 scope 外 | `docs/03-implementation/changes/CH-002-ui-fidelity-alignment/` |
| CH-003 | **SKU Catalog 編輯**（`PATCH /license/catalog/:id` + 前端 Edit dialog 改 alias/category/base-flag;解禁 catalog Edit honest gap）| ✅ **完成**（2026-07-16;PATCH endpoint + Edit dialog;api **205**/web 85 test 綠;live curl edit[trim/immutable]/404/400/restore 驗;H1 評估 = Change 無新 ADR[3 欄已存在/非 hard gate/businessAlias 與 ADR-0004 一致]）| `@Roles(ADMIN,REGIONAL)`;flag:businessAlias=curation 副作用[allocation-import 匹配]、isBaseLicense=OQ §10 | `docs/03-implementation/changes/CH-003-catalog-edit/` |
| CH-004 | **OpCo 管理**（settings › **OpCos tab** + `admin/opcos` CRUD:GET rich[`?includeInactive`] / POST 建立[code 唯一 409] / PATCH 改[code immutable] / deactivate;補 prototype opcos tab honest gap = CH-002 C 組「OpCos CRUD tab」）| ✅ **完成**（2026-07-16;新 `OpcoAdminController` `@Roles(ADMIN,REGIONAL)` + **D1 令 GET relocate 出 user-admin**[path `/admin/opcos` 不變,前端零改]+ orphan `AdminOpcoDto` 刪;前端 `OpcosPanel` + Add/Edit dialog[code Edit 唯讀、company/costCenter 忠 schema、deactivate 非 hard-delete]；api 205→**213**/web 85 test;live curl 201/409/400/200[immutable strip `HACKED`]/404/deactivate 全驗;H1=Change 無 ADR 無 schema[Opco 5 欄已存在]）| 決策 D1=ADMIN+REGIONAL·D2=忠 schema 無 `region`·D3=code immutable·D4=新 controller;前端 Edit round-trip + light/dark 待 browser 手測 | `docs/03-implementation/changes/CH-004-opco-management/` |
| Assets-cat-group | **By-OpCo 表按 category 分組**（問題 2b;純前端,Platform 已有分組）| 候選 | 前置:CH-003 後或獨立;需 ledger row 帶 category | CH-002 audit / `by-opco-view.tsx` |
| **AUDIT-1** | **ADR-0009 平台 audit 定位**（通用 `AuditLog` 與 `RequestEvent`/`LedgerAdjustment` **共存** · 白名單 before/after · 權限矩陣 code-derive 不起 permission table · 平台 vs ServiceNow audit 分工）—— rollout item 1 | ✅ **完成**（2026-07-20;**ADR-0009 Accepted**;Chris 拍板 **OQ-1 = 記白名單 before/after** · **OQ-2 = P-B**[白名單可含 email·displayName]+ 4 條連帶義務[ADMIN-only 唔可放寬 / 白名單係 PII 唯一 enforcement / 將來 retention 要納 AuditLog / LedgerAdjustment「never PII」註解不變]） | — **item 2-3 已解封** | `docs/adr/0009-platform-audit-trail.md` · `02-architecture/audit-and-integration-observability.md` |
| **AUDIT-2** | **權限矩陣**（由 `@Roles` derive 成文件 + 唯讀 UI + **drift test**:code 改咗矩陣冇改 → test 紅）—— rollout item 2;解 Chris「權限可訪問功能列表」需求 | ✅ **完成**（2026-07-20,**W28**;G1–G7 全 pass;**零 schema/dep/新 ADR,零行為改動**;`GET /admin/permissions` runtime derive[`DiscoveryService`]+ `derivePermissions()` 純函數 + Settings › Permissions 唯讀 tab + **drift/unguarded test 雙 fails-before 實證**;api 213→**223** · web 85 不降;**live 34 route / 10 controller / 零 unguarded**;403 三重驗證[扮 OPCO_IT → `/me` 確認身分 → 403 → 同身分 `/license/ledger` 200 對照 → 還原 200]） | — **AUDIT-3 為下一個** | `W28-permission-matrix/` · ADR-0009 Decision 8.5 |
| **AUDIT-3** | **`AuditLog` 落地 + Audit UI**（覆蓋 user CRUD/role 變更/密碼重設/登入成敗/OpCo CRUD/catalog 編輯/import/drift resolve;篩 actor·時間·對象·action）—— rollout item 3 | ✅ **完成**（2026-07-21,**W29**;G1–G8 全 8/8 pass;13 事件 + 三層白名單 + `GET /admin/audit` + 獨立 `/audit` 頁;api 213→**263** · web 85→**92**;三條硬約束全部守住 —— H4 白名單 test 先行[13 條]、讀取 ADMIN-only 冇放寬、audit 與主操作同一 `$transaction`[rollback test 證]） | — **`FE-activity` 已解封**;🟢 audit retention 登為新 candidate | `W29-audit-log/` · ADR-0009 · `audit-and-integration-observability.md §2.2` |
| **audit-retention** | **`AuditLog` retention / 清理政策**（ADR-0009 Decision 8.3 刻意唔做[避免過早優化],但 **Decision 7 連帶義務 ③ 講明將來 retention 必須涵蓋 `AuditLog`** —— 因採 P-B,表內含 email/displayName） | 候選（W29 解封,無外部阻塞） | 前置:量大先做;需定政策[保留期 / 匿名化 vs 刪除 / GDPR-style 請求點處理]→ 可能觸發 ADR;現有三個 index 短期夠用 | ADR-0009 Decision 7 連帶義務 ③ · Decision 8.3 · `W29-audit-log/` retro |
| **INTEG-1** | **Integration 狀態 + Test connection**（Graph/ServiceNow/n8n 已配置 ✓✗ + 最後成功時間 + `Test connection` 掣;補 `settings.tsx:232-242` EmptyState 自認嘅 gap）—— rollout item 4 | ✅ **完成**(2026-07-21,**W30**;**G1–G9 全 9/9 pass**;**零 schema / 零新 dep / 零新 ADR**;`GET /admin/integrations` + `POST /admin/integrations/:key/test`[`@Roles(ADMIN)`]+ Integrations tab 真內容取代 placeholder EmptyState;探針**重用既有唯讀方法**零新 vendor 方法;**兩條硬紅線 test 先行** —— G1 餵六個假 secret assert 回應零洩漏 / G2 跑晒探針 assert `createRecord`·`updateRecord`·`addWorkNote` 從未被呼叫;api 266→**286** · web 93→**99**;live 全驗[回應+頁面零 env 值 · 403 三重兩條 route · 切 provider `inactive`→`active` · 第二撳 **429** · light+dark · 頁面零 DocuWare];順手修探針 `201`→`@HttpCode(200)`;**ADR-0010 D4 補註** n8n inbound 派生落空 → 交白卷) | ✅ **DocuWare 文案已清**(D6);🟡 `lastProbe` 唔持久化 = D4 直接後果,運維若要探測歷史 = 重開 OQ-B 訊號,唔好靜靜加 model | `W30-integration-status/` · **ADR-0010** |
| **INTEG-3** | **Outbound 交付失敗持久化 + 人手補救**（`OutboundFailure` model + 三種失敗各自補救 + UI 佇列）—— rollout item 6 | ✅ **完成**(2026-07-21,**W31**;**G1–G9 全 9/9 pass**;**ADR-0011 事前 Accepted,零 plan deviation**;api 286→**324** · web 114→**123**)。**埋身查證改寫咗 scope**:ADR-0010 D8 個模型只啱三分之一 —— 實際有**三種**失敗,補救動作互不相容(F1 submit=重新提交 / **F2 mirror=只可補寫本地,🔴 絕不重新提交否則開多張飛** / F3 worknote=重發)。照 D8 字面做會解咗最唔痛嗰個、漏低最痛兩個。**兩條負面斷言硬紅線**:G1 餵八個 credential 欄位 assert 零持久化 · G2 mirror 補救 assert provider/createRecord 從未被呼叫。**live 最強證據** —— mirror 補救喺 **SN 死緊嘅環境**成功(本地 Request 建成 REQ0099),證明真係純本地 | ⚠️ **D6 刻意同 ADR-0009 D8.1 相反**(failure 紀錄唔綁主 transaction —— F2 嘅失敗原因就係本地 write 失敗,綁埋會一齊 rollback);🟡 **無 `opcoId` 欄位**(將來要 OPCO_IT scope 要 join 或補欄);🟡 retention 未做(同 `audit-retention`) | **ADR-0011** · `W31-outbound-failure-recovery/` |
| **INTEG-4** | **Connector 配置 UI 管理**（Model C:非機密欄[tenant/client id · instance URL · default table · provider 路由 · webhook URL]經 UI[ADMIN]改,落新 `ConnectorConfig` model + DB-then-env resolver;**真 secret 仍只經 env,唔落 DB/API/audit**;ADR-0013 擴展 ADR-0010 item 4,唔 supersede D2）| ✅ **完成**（2026-07-23,**W34**;**ADR-0013 Accepted**,4 OQ[C2 restart / `SERVICENOW_USER` env-only / Graph+SN+n8n outbound 三 connector / 一 row 一 connector];**api 345→367 · web 131→136 test 全綠 + lint clean**;S1 `ConnectorConfig` model+migration[SQL 無 secret 欄]→ S2 resolver[DB-then-env;**🔴 secret 邊界三重守:schema 無欄 / describe 唔洩漏 / audit 白名單只 6 非機密欄**]→ S3 wire[Graph/SN `onModuleInit` + factory async,**G3 env fallback 零迴歸**]→ S4 controller[擴 W30 `admin/integrations`,PATCH ADMIN,permission matrix 證實 `→[ADMIN]`]→ S5 audit[同 transaction,`connector.config_update`]→ 前端[Configure 編輯面,secret 唯讀];**DS-4 live 驗 light rgb(245,245,246)↔dark rgb(8,8,10) token swap**） | — | `W34-connector-config-ui/` · `docs/adr/0013-connector-config-ui-management.md` |
| BE-ledger-read | **後端 read-model:per-OpCo ledger + SKU 用量 stats endpoint**（FE-1 OD1-A carry — **License Assets 前端整個畫面** + Overview seat KPI 需此） | ✅ **完成**（2026-07-13,W14;`GET /license/ledger`[rows + headroom/overAllocated]+ `GET /license/ledger/stats`,opco-scoped[AUTH-3a scopeWhere],active-only;無 schema/ADR;api 92→96 test;live scoped 驗 ADMIN 4/2-OpCo · OPCO_IT 2/自己） | — | `W14-ledger-read/` |
| FE-Assets | **前端 License Assets 畫面**（By-OpCo 誠實表:allocated/assigned/available + utilization bar + 狀態 + OpCo filter;Overview seat KPI）—— FE-1 deviation 移出 | ✅ **完成**（2026-07-13,W15;G1–G7 全 pass;純前端無 schema/dep/ADR;web 8→17 test;live DOM ADMIN 4/2-OpCo · OPCO_IT 2/RHK · Overview Licenses assigned · light+dark;honest gap Platform tenant-Owned→BE-tenant-owned 新 candidate） | — | `W15-fe-assets/` |
| BE-tenant-owned | **後端 tenant-owned 層 endpoint**（`GET /license/tenant-skus` + `/stats`,per-SKU owned/allocated/assigned/unallocated;ADMIN/REGIONAL）—— 解封 Assets **Platform mode** | ✅ **完成**（2026-07-13,W16;純 query-layer[TenantSkuSnapshot.prepaidEnabled 已存 owned 數,唔打 Graph]無 schema/dep/ADR;api 96→100 test;live ADMIN e3 over-allocated/e1 unalloc 14 + stats · OPCO_IT 403） | — | `W16-tenant-owned/` |
| FE-Assets-platform | **FE Assets Platform mode tab**（Platform/By-OpCo mode 切換 + Owned/Allocated/Assigned/Unalloc 三層表 + recon tile + over-alloc pill;403 graceful） | ✅ **完成**（2026-07-13,W17;純前端無 schema/dep/ADR;web 17→24 test;live 截圖 ADMIN grouped table + e3 over-allocated + light+dark · OPCO_IT 403 restricted state;Compare mode 仍 future） | — | `W17-fe-assets-platform/` |
| FE-activity | **Overview activity feed**（真活動流取代 placeholder;需後端 events / audit endpoint）—— W12 FE-fidelity honest gap | ✅ **完成**（2026-07-21,**CH-005**;A1–A12 全 pass;**零 schema / 零 dep / 零 ADR / `apps/api` 零 diff**;`lib/activity.ts` + `components/overview/activity-feed.tsx` + `canSeeAdminNav` gate;web 99→**114** test;live 驗 ADMIN 6 行真內容 + light/dark 四值全 swap + **OPCO_IT `hasActivityCard: false` 而其餘 card/KPI 照 render** + 後端三重驗證[`/me` OPCO_IT · `/admin/audit` **403** · `/license/ledger` 200];**ui-design 揪到 DS-5 violation 即場修**[ref 識別碼改 mono]） | **D1 = ADMIN-only,non-admin 隱藏整張 card**(`/admin/audit` 冇放寬);**D2 = AuditLog 來源 + 措辭誠實**(wording guard test 鎖死) | `docs/03-implementation/changes/CH-005-overview-activity-feed/` · ADR-0009 Decision 7 |
| FE-activity-ops | **Overview 營運活動流**（prototype 真正示範嗰種:assign / stage 推進 / 退回 —— 即 `RequestEvent`,唔係 `AuditLog`）—— CH-005 carry-over | ✅ **完成**（2026-07-22,**CH-006**;B1–B12/B14 pass,B13 部分[見下];`GET /fulfilment/activity` 新 controller + **`@@index([createdAt])`** [**H1-lite**:純 additive/零遷移/零 interface/冇掂 locked 決策,Chris approve,理由入 spec §1.1 **無 ADR**];**card 解除 ADMIN-only**,全 role 見到、後端 opco-scope;`EVENT_TONE` 由 `request-detail` 抽出共用[B7 grep 驗單一定義];api 324→**333**,web 123→**123**[換來源非加來源,CH-005 audit 措辭 test 隨映射一齊刪 —— R3 記入 changelog];live **三方對照** ADMIN 18 跨 OpCo vs OPCO_IT-RHK 2 條全 RHK[洩漏 0] vs `/admin/audit` **403**） | **D1 = RequestEvent 取代**(唔合併 —— `auth.login_success` 會洗版 6 行 feed)· **D2 = index 當 H1-lite,先例僅限「純 index 增補」** · **D4 = `request-detail` raw-enum 唔改**(default out-of-scope) | `docs/03-implementation/changes/CH-006-overview-operational-activity/` |
| FE-overview-test | **`overview.test.tsx` 回歸鎖**（鎖死「Overview activity card 對 non-admin 可見」—— CH-006 移除 gate 後,呢個行為目前只有 live 驗證,冇 component test 守住） | 候選 | 前置:要 mock `useRequests`/`useDrift`/`useLedgerStats`/`useActivity` + router;CH-006 刻意唔順手做(避免為補測試數字而擴 scope) | `CH-006-overview-operational-activity/progress.md` 未達標項 |
| Request-edit | **Request 建單後可編輯**（header inline edit 除同步鍵 + line item 發送前加減）| ✅ **完成**（2026-07-22,**CH-007**;C1–C12 全 pass;零 schema/dep/ADR;`PATCH :id` + `DELETE …/:lineItemId` + addLineItem origin gate,鎖全部 backend 為準;前端 `canEditUpn`/`canRemoveLine`/`canAddLine` 純函數鏡像 + inline edit UI;api 333→**345**,web 123→**131**;live 兩-origin 對照 + **C3 夾帶同步鍵被剝**[event 自證]+ **C8 ledger 全程 10358/6049 不變**+ dark） | **D2 targetUpn sync-後鎖**(backend 409)· **D3 opcoId 鎖** · **D4 同步鍵+origin 永遠鎖** · **D5 刪只限 REQUESTED+無RITM** · **D6 加只限 intake 單** | `docs/03-implementation/changes/CH-007-request-edit/` |
| **CH-008** | **By-OpCo ledger 空白行(0/0)預設隱藏 + 修正誤導狀態**(Chris 提「改成 0 之後 row 唔會消失」;查證揪到 **`assetStatus()` 令 0/0 顯示成綠色 Headroom** = 讀落似「有剩餘容量」實際乜都冇)—— read-model 加 empty 過濾 + `?includeEmpty`(鏡射 CH-004 `?includeInactive`)· By-OpCo 加「Show empty rows」toggle · `assetStatus` 加 `Empty`/neutral | ✅ **完成**(2026-07-27;A1–A11 **11/11 全 pass,零延後項**;**零 schema / 零新 dep / 零新 endpoint**[`schema.prisma` + 三個 `package.json` diff 全 0];api 429→**433** · web 180→**188**,兩邊 lint 0。D1–D5 原封落地:過濾寫入**既有** `where(actor, includeEmpty)` **而唔另起 query**[另一條 query = 另一個可以漏 `scopeWhere` 嘅地方]· `assetStatus` 加 `Empty`/neutral **排喺 `overAllocated` 之後** · toggle 用既有 `Checkbox` 且 **stats 跟同一個 flag**,所以 tile 副標「N SKUs tracked」同表行數永遠對得返。live 插 **3 個全新 fixture row** 驗完即 `DELETE`,teardown 後 7 個 count 逐個對返 baseline、三個 `*_left` 全 0。**三個「驗到嘢」嘅位**:①**A8 第一次係假驗** —— `changes:0` 睇落過關,但嗰個 SKU 根本未 curate、唔喺範本入面,補 curated fixture 重跑先算數 ②**A7 真正守門嗰條**係 OPCO_IT **加咗 `?includeEmpty=true`** 之後仍然見唔到別家嘅 0/0(即 R4 驚嗰條漏 scope 路徑)③**A1 有對照組** —— 「fixture 唔見咗」證明唔到規則啱,逐行 diff 證**只差嗰一行**) | **唔做真刪除**:`LedgerAdjustment` 帶 `onDelete: Cascade` ⇒ 硬刪會靜靜殺埋 ADR-0007 audit;平台一貫 soft-deactivate · ✅ **R1 Chris 已明確接受、toggle 已落地**:隱藏後因為冇 create endpoint(**DD-3**),操作員冇路徑把該格由 0 調返非零 ⇒ **toggle 係唯一入口而唔係裝飾**;文案兩處**常駐**講明「hidden, not deleted」(checkbox `title` + card subtitle —— 只靠 tooltip 等於冇講) · ⚠️ **query key 由兩層變三層** `['license','ledger',{includeEmpty}]` —— 既有 mutation 嘅 prefix invalidate 仍然成立,但**冇嘢逼佢成立**,已加一條 test 驅動真 `QueryClient` 守住(下次改 key 形狀要留意) · ➕ 順帶查清 spec §1 個「**21 行**」從來就唔係畫面數(兩個 GET 由 W14 起一直有 `sku.active`/`opco.active` 過濾,畫面實際 **14**),同 CH-008 無關,已入 spec §7 changelog · **選項 B/C(soft-delete / DELETE)明確 out of scope**,留返同 DD-3 一次過喺 `Drift-resolve` 設計 | `changes/CH-008-ledger-empty-rows/` · `DEFERRED_REGISTER` DD-3 · ADR-0007 |
| **CH-009** | **Assign 前可用量可見度**(Chris 提「冇檢查可用數量」→ 查證**修正一半前提**:tenant seat gate **已存在**[`assign.service.ts:127-132` live Graph fail-closed],真缺口 = **事前不可見**)—— request detail 每個可 assign line item 顯示 **OpCo 預算 headroom**(全 role)+ **tenant seat**(ADMIN/REGIONAL) | ✅ **完成**(2026-07-26,**CH-009**;零 backend/schema/endpoint/dep,無 ADR;web **151→167** test;live 驗 A1-A5 **ADMIN vs OPCO_IT 對照**[OPCO_IT `tenant-skus` **完全唔喺 network list** + 0 console error,run-as 先驗 `/me` 真返 OPCO_IT 防 ADMIN fallback 假驗證]· A4 **零 DB 改動**[用天然缺 row 而唔係造 0/0]· A9 light+dark + `getComputedStyle` 證 token 隨 `.dark` swap;**A6 由 component test + mutation check 覆蓋**[移走 invalidate → test 真紅]) | 🔴 實作揪到 **spec A6 前提錯**:`useAssignLineItem` 原本**冇** invalidate `['license','ledger']` ⇒ 照原樣 assign 後容量數字 stale = 本 change 反效果,已修(spec §7 changelog)· ➕ 查證 `tenant-owned.service` 確認 tenant 層係 **`TenantSkuSnapshot` snapshot 唔係 live Graph** ⇒ 標 `last sync`、`owned=null` 顯示 `unknown` 而**唔係 0** · 🚧 **carry-over**:CH-008 落地後重跑 §2.4 交互驗 · 真 assign 端到端仍未 live 驗(打真 tenant,§3.4)· ⚠️ DS-11 部分:prototype 冇此資訊行,截圖已交 owner | `changes/CH-009-assign-capacity-visibility/` | **零 backend / 零 schema / 零新 endpoint**(兩個數據源都已有 hook,且 `GET /license/ledger` scope 天然對齊 AUTH-3a)· 🔴 `tenant-skus` 對 OPCO_IT **403**(controller-level roles,無 override)⇒ 必須 lazy `useTenantSkus(canSeePlatform)`,OPCO_IT **唔顯示該層**(唔可以扮 0)· ⚠️ **同 CH-008 有真交互**:0/0 格被隱藏後 lookup 落空 → 顯示 `no allocation set`,**唔可以圖方便傳 `?includeEmpty=true`** | `changes/CH-009-assign-capacity-visibility/` · **ADR-0016**(顯示嘅正是佢將擋嘅數字)· CH-008 §2.4 |
| **SYNC-sweep** | **排程 sync sweep —— `azureSyncedAt` 由「宣稱」升級為「平台證實」**(Chris 提「AD→Azure 同步時間唔確定,需要排程定時檢查」)—— `@Cron` 掃 `azureSyncedAt IS NULL` 嘅 open request,`findUser` 命中即開 gate + 寫 `RequestEvent(SYNC)`;人手 `PATCH /sync` 保留做 break-glass 但 message 分辨 | ✅ **完成**(2026-07-27,**W37**;OQ1=A · OQ2=B · OQ3=A;**零 schema / 零新 dep**;api 410→**429**,lint 0;**live 真 Graph 命中**驗到 gate 自動開 + `RequestEvent` verified 文案 + **一條** `sync.sweep` audit `after {scanned:1,opened:1}`,同輪其餘兩張唔合資格嘅單**冇被誤掃**;kill switch A/B 驗;測試數據**造完即刪**,四個 count 全 0) | **全 repo 第一個 `@Cron`**(`ScheduleModule` 早已註冊,`app.module.ts:15` 註解已預言)· **零 schema / 零新 dep**(H2 不觸發)· 語意升級 = H1 觸發點 · **唔抵觸 ADR-0010 禁 `@Cron` 探針**(domain-driven,閒置時零 Graph call)· ⚠️ 多實例 scale-out 會重複跑(現 UAT 單實例;明文記低唔預先解) | `adr/0015-sync-gate-scheduled-sweep.md` · RISK **R3** · `app.module.ts:15` |
| **Budget-gate** | **OpCo 預算成為 assign 硬 gate + ADMIN 具名 override**(補 tenant seat gate 睇唔到嘅 OpCo 層)—— `assigned+1 > allocated` → 擋;`OPCO_IT`/`REGIONAL` 無 override;ADMIN 必須寫理由並入 audit | ✅ **完成 · closed**(2026-07-27,**W36**;OQ1 = **選項 A**;F1 gate + F2 override&audit + F3 前端入口 + F4 test + F5 SQL&runbook 全交付;**零 schema / 零新 dep**;api 390→**410** · web 151→**180**,兩邊 lint 0;**D7 四條硬邊界 diff 全部為 0**[`reconcile` / `allocation-import` / `schema.prisma` / 三個 `package.json`];live 驗拒絕路徑**零 DB 改動**[dev 天然有冇 ledger row 嘅 READY line item]:DTO 400 · 空白 400 · **REGIONAL 403 + 同 actor 唔帶欄 400 對照** · browser **ADMIN 見 Override / REGIONAL 唔見** · 事後 DB 零副作用) | 🔴 **D6 偏離 ADR**(owner approved,plan changelog):D6 三個前提逐字核對後全部唔成立 ⇒ 擴 ADR-0009 白名單[新 action `assign.budget_override` + target `RequestLineItem`(白名單 `[]`)+ 3 個非 PII metadata key],否則三個數字會被 `pickAuditMetadata` **靜靜丟棄** · 🔴 **gate 嘅 400 本身喺 dev live 驗唔到**(D5 把 gate 放喺 `graph.findUser` 之後,seed UPN 唔喺真 tenant)⇒ **已寫入 runbook 步 4 做部署後第一項檢查**,**冇用真人 UPN 硬闖**(gate 一有 bug 就真派 licence 畀真人 = R6) · 🔴 **F5 SQL 真跑捉到 plan §2 自己錯**:inactive 係 **12 唔係 6** ⇒ active 影響面 **10 唔係 16**;根因 = **`skuPartNumber` 唔唯一**(§13),SQL 已改成一律出 `skuId` · ➕ SQL 揪出 plan 冇計過嘅一類:**冇 ledger row 但有 pending line item**(D1 下每次 assign 都擋,最嚴重)· race condition **照 ADR 刻意唔解** | `W36-opco-budget-gate/` · `adr/0016-*` · **`docs/05-usage/OPCO-BUDGET-GATE-ROLLOUT.md`** + `sql/opco-budget-gate-preflight.sql` |
| n8n-intake-handoff | **交畀 n8n team 嘅照抄範本**(真 SKU GUID + 23 個 active OpCo code + payload + 錯誤碼對照 + 已知落差)—— 解封「第一次真對接」 | ✅ **完成**(2026-07-26;純 doc,零 code;**唔重造 smoke script**,指返 demo-harness Scenario 3) | 🔴 **「E5」喺 catalog 真係有兩個**(`SPE_E5` vs `Microsoft_365_E5_(no_Teams)`)⇒ 傳名必然歧義,GUID 係硬要求 · ~~🔴 最大 blocking 未知 = n8n 側交唔交得出 `skuId` GUID~~ ⇒ ✅ **W36 已解:加第二條 adapter route**(`/requests/intake/n8n`),**LOCKED 合約一個字冇改**;doc-sync 已做(§0 / §7 #1 #5 / 新 §8)· GUID 表標 **as-of 2026-07-26 + 唔係 SSOT**(防 AP-3 stale)· ~~順帶揪到 intake 唔檢查 OpCo `active`~~ ⇒ **adapter route 已收緊,canonical route 刻意維持原狀** | `docs/05-usage/N8N-INTAKE-HANDOFF.md` · `N8N-INTEGRATION-SETUP.md` · W24 `CONTRACT.md`(LOCKED) |
| **N8N-SEAMS** | **n8n 執行接縫化**(Chris 交 phase 1[1001/1002/1004/1005/1007/1009]+ phase 2[2002/2003/2004/2005]全部 workflow,要求規劃可轉換連接架構)—— 三接縫逐個可切換:①outbound 建單[既有,不改] ②`LicenseOperationsProvider` graph\|n8n[2002/2003/2005] ④`TicketUpdateProvider` direct\|n8n[2004];③inbound intake **必然不可切換**但加 adapter 解合約落差 | 🟢 **ADR-0017 Accepted**(2026-07-26,Chris Lai)—— 待開 phase;**戊 = 最高優先(唯一 blocker)** | 🔴 **D0 只換執行器唔換決策者** —— 所有 gate(scope/stage/sync/座位/ledger/audit/預算)永遠留平台,違反即兩路徑行為分叉 · 🔴 **D4 就係 `N8N-INTAKE-HANDOFF` §0 要求嗰個 ADR**(owner approve + 成文,非「靜靜 map」;canonical `CONTRACT.md` 一個字不改,adapter 係另一條 route)⇒ 落差 #1 由 blocking 改為平台解決,**handoff §0/§7 需 doc-sync** · 🔴 **RITM 分工**:1007 管 AD 類、平台只管 license 類,唔可以互踩(否則 SN 狀態打交) · ⚠️ **D2 正規化 outcome 詞彙**係真設計功夫(Graph throw/void vs 2003 返 `already_assigned`/`not_synced`),H5 主戰場 = 同一組 case 兩 provider 同一 outcome · ⚠️ 預設值 = 現行行為(`graph`/`direct`),部署唔揀就零改變 · **D7 明確唔做**:唔實作 ADR-0015 sweep / 唔改 ledger invariant 同 `reconcile` / 唔做 auto-assign / 唔搬 n8n 嘅 AD 能力入平台 | **`adr/0017-n8n-execution-seams-switchable-integration.md`** · ADR-0008(範圍註記已加)· ADR-0013(D5 配置沿用)· ADR-0010/0011/0009 · `docs/06-reference/03-n8n-workflow/` |
| **N8N-SEAMS-戊** | **戊:inbound intake adapter**(`POST /requests/intake/n8n` 接 n8n 原生信封 → resolve licenseCode→skuId GUID / department→opcoCode / REQ number→sysId → 交既有 `IntakeService`)—— **場景一嘅唯一 blocker** | ✅ **完成**(2026-07-27,**W36**;G1-G9 9/9;**零 schema / 零新 dep / 零新 ADR**[ADR-0017 事前 Accepted]。3 個新檔[`opco-department-map.ts` 18 條常數表 = **OQ-1 拍板 code 常數** · `dto/n8n-native-intake.dto.ts` · `intake-adapter.service.ts`]+ 3 個改動[controller / module / **`seed.ts` 加 `RAPO/IT (RDC2)` = OQ-2**];api **390→407** test;live **8 個 case** 真 output[401 / 201 / 冪等 / 三種 resolve 400 / RDC2 / 壞信封],DB 逐行查過。**G2 三個受保護檔零 diff**) | 🔴 **真 SN 端到端未驗證** —— live 行嘅係 demo-harness mock(憑證 placeholder,plan R2)⇒ 證到 adapter→`IntakeService`→DB 通 + 用啱 `sc_request` + 用啱 REQ number,**未證**真 SN 回應;**唔當 pass** · 🔴 **F4 揪出:兩個 `WF1 - Call UOP Intake` 完全冇送 `X-Intake-Key`**(無 `sendHeaders`、`credentials` 空)⇒ **一 enable 就全部 401**,連同「`UOP_INTAKE_URL` 要指去 `/requests/intake/n8n`」「1005 個 node 仍 disabled」= **Chris 喺 n8n UI 要做嘅三件事** · ⚠️ `licenseCode` 可為 `null` ⇒ **成張單** 400 而非跳過該 line(刻意 fail-closed) · ⚠️ **各環境要各自補 OpCo `RAPO/IT (RDC2)`**(`POST /admin/opcos`);該 OpCo `allocated=0` ⇒ **ADR-0016 gate 上線前要設 allocation** · ✅ 1001 vs 1005 payload shape **實讀確認一致** · ✅ 順帶收緊 handoff 落差 #5(**adapter route 檢查 OpCo `active`;canonical route 刻意不變**) | **上線 runbook = `docs/13-deployment/08-n8n-integration-go-live.md`**(平台側前置 + n8n 三項 + 分階段驗收表)· ADR-0017 **D4** · `W36-n8n-intake-adapter/`(plan/checklist/progress/**MAPPING**/**N8N-WF1-CHANGES §2.5**)· `N8N-INTAKE-HANDOFF.md §8` · `N8N-INTEGRATION-SETUP.md §1.5` · W24 `CONTRACT.md`(LOCKED,**零改動**) |
| **N8N-SEAMS-己庚辛** | **己:`LicenseOperationsProvider` + `GraphLicenseProvider`**(純重構,零行為改變)→ **庚:`N8nLicenseProvider`**(2002/2003/2005 + `n8n-license` connector + probe)→ **辛:`TicketUpdateProvider`**(2004 雙實作 + close/WIP 擴充) | **己 ✅ 完成**(2026-07-27,**W38**;G1–G8 達標;**零 schema / 零新 dep / 零新 ADR** —— ADR-0017 事前 Accepted,本 phase 只**收緊**唔推翻,補註寫入 ADR「實作補註」節)。api 450→**467** · lint 0 · tsc 0。**Chris 四項拍板**:①介面由 D2 表列 5 個方法收到 **3 個**(`checkSync`/`listUsersBySku` 零 caller 唔加)②seam **只覆蓋 assign 路徑**(`getSubscribedSkus` 其實有 **4 個** consumer)③🔴 **`sync-sweep` 永不經 seam** ④transport 失敗 **throw** 唔入 `AssignOutcome`。<br>**G2 = `16 加 / 0 刪`** —— 一條既有 assertion 都冇郁。關鍵手法:spec **唔 mock provider**,而係 wire **真 `GraphLicenseProvider` 包住原本個 `GraphService` mock` —— mock 走 provider 會令 raw→503 個 wrap 跌出測試鏈,兩條 BUG-002 regression 會靜靜降級成「503 會向上傳」但**照樣綠**。<br>**G5 fails-before**:插一行 seam import 落 `reconcile` → `1 failed / 7 passed` → 還原。<br>**G8 live**:真 boot(3200)+ 真 HTTP → **503 message 逐字不變** + log 出自 `[GraphLicenseProvider]` + 真 `AADSTS900021` + 四個 count 零變動。🚧 **assign 成功路徑明標未驗**(要真憑證,跑通就真派 licence 畀真人 —— 同 W36 一樣唔開呢個先例)。<br>⇒ 庚解封。<br>**庚 ✅ 完成**(2026-07-28,**W39**;api 467→**499** · lint 0 · tsc 0):`N8nLicenseProvider`(2002/2003/2005)+ `n8n-license` connector + **probe**。**Chris 五個 OQ + 一個 H1 拍板**。🔴 **H1**:kickoff 假設「零 schema」破產 —— `ConnectorConfig` 係**具名 column** 唔係 key-value bag ⇒ 加 connector **必然** `ALTER TABLE`(additive,兩個 nullable 欄;**教訓入咗 ADR-0013 補註**,下個加 connector 嘅人會踩同一坑)。**實讀 workflow JSON 揪到 D2 表四處對唔上**(`success`≠`assigned` · 2003 **唔檢查座位** ⇒ `no_seats` 兩邊都產生唔到 · **兩種** response 形狀 · `details` 原封塞 Graph error body = **同 BUG-004 同源**,OQ-2 拍板唔傳遞)。**三次被自己嘅守門捉到**:①`secret()` 喺 `try` 內求值 ⇒ 「冇設 key」被包成「n8n unavailable」,運維會去查第三方 ②`PROBEABLE` 加咗但冇加分支 ⇒ 撳 n8n-license 測試**會探 ServiceNow 而標籤寫 n8n**;fails-before 更揭到舊 G2 **冚唔到**(fall-through 打嘅係 `query` = 讀唔係寫)③W38 boundary test 捉到 probe import —— 查清楚後判定**條 test 實作寬過意圖**,**收緊**(明文禁抽象 import,舊版從來冇真正 assert 過)而唔係放寬。➕ 順手把 G2 由**手抄 4 個 key** 改成 iterate `CONNECTOR_KEYS`(**靠人手同步嘅守門 = 有窿嘅守門**,同 TD-1 同病)。**F2 contract test ✅**(= ADR-0017 rollout 表列明嘅庚驗收標準):**7 個 case 各寫兩次**[一次 Graph 語言、一次 n8n 語言]—— **令兩個 arrangement 意思相同就係全部價值所在**,mistranslation 只匿喺嗰度。**兩樣刻意唔 assert**:①error message **必須唔同**[vendor 掛咗要知係邊個掛]⇒ contract 係**失敗類別**唔係字眼 ②replay **獨立釘死**[有人日後「統一」佢哋就會紅,逼佢返去睇 OQ-1]。fails-before 揀咗**最痛**嗰個分岔[`not_synced` 返 non-null ⇒ sync gate 400 唔再觸發、未 sync 被當已 sync]。**F3 secret 硬紅線 ✅** + fails-before[真塞個 secret 落 `lastSuccessNote` → leak test 真紅]。⚠️ **F3 途中揪到我 Day 2 判斷錯咗**:寫低「`CONNECTOR_KEYS` 自動 derive」但 `list()` 其實係**手寫陣列** —— 條 test 叫 `reports all four connectors` 而佢**仍然綠**,正因為新 connector **根本冇顯示喺 `/admin/integrations`**。**「test 全綠」唔等於「嘢做咗」**;綠色係關於 test 嘅事實,唔係關於 code 嘅事實。➕ 刪走自己寫嘅一條**永遠綠**嘅廢話 test。🚧 **真切換仍然零 live 驗證**(n8n secret 仍 `CHANGE_ME_SHARED_SECRET` · UAT 未接通 · 平台未部署)—— **唔可以當通過**。✅ **當時登嘅 follow-up 已收** —— `integration-status` 兩行 selection 只讀 env 呢個缺口 = **BUG-005**,2026-07-28 修好(一次過修埋 `n8n-outbound`,正如 W39 當時寫低嘅做法)。<br>⇒ **辛(`TicketUpdateProvider`)解封,號碼 W40**<br>**辛 ✅ 完成**(2026-07-28,**W40**;api 528→**588** · lint 0 · tsc 0):`TicketUpdateProvider` + `DirectTicketProvider` + `N8nTicketProvider`(2004)+ `n8n-ticket` connector + **close/WIP trigger 接落 assign 路徑**。**Chris 三個 OQ + 兩個 H1 拍板**。⇒ **ADR-0017 rollout 戊己庚辛 4/4 全數交付**。🔴 **D3 三處同真 workflow 對唔上,其中一處 blocking**:2004 **冇任何 mode 係「加 note 而唔改 state」**,而 D3 把 `addWorkNote` map 咗落 mode 1(必然 `state='2'`)⇒ 照做嘅話,切一個掣就會令 **assign 成功嘅單被標成 Work in Progress** —— 違反 D0 而且語意相反。**OQ-A 拍板:`addWorkNote` 唔入介面**,介面比 D3 表窄(同 W38 收窄 D2 同一判斷:**vendor 冇對應能力就唔好假裝有**)。**兩個 H1**:①加 connector 必然 `ALTER TABLE`(**事前就知** —— W39 把教訓寫入 ADR-0013 補註,今次冇再撞)②`RequestLineItem.ticketHeldAt` —— 因為預算 gate 擋 = throw,而操作員會**不斷重試**,冇 flag 就每次 PATCH 一次真客戶單;**只喺寫入成功之後先記**。對照:**`closeComplete` 唔使守門**,stage gate(`!== READY` → 400)已經保證只成功 assign 一次(**查證得出唔係假設**)。🔴 **close 唔 fallback 去 parent REQ**(REQ 係 `sc_request` 而 seam ④ 只寫 `sc_req_item`;close 一張 REQ ≠ close 一張 RITM)。**OQ-D 收窄** —— F1 把佢實作成「retry 完全唔可以 import seam」,F4 證明**闊過個決定本身**:work note retry 重發嘅係 direct call ⇒ 直連啱;ticket state 嘅失敗係**當時選中嗰個 provider** 產生 ⇒ 必須走 seam。**同 W39 方向相反(嗰次係收緊)**,所以冇靜靜改:靜態嗰條收窄,真正保證改由**行為 test** 守。**1007 分工邊界由聲明變事實**:實讀 1001 `Prepare Approval Data` —— `actionItems` 三個全部由 `phase1Items` 造、`other_items` 一次都冇入過;而 `licenseItems = other.filter(pending_license‖/O365/i)` ⇒ **兩套 sys_id 來自同一份 AI Brain output 嘅兩條互斥分支**;⚠️ 但**冇任何自動守門**(RITM sys_id 兩邊一模一樣),所以 comment 寫明「一旦 n8n 側改咗,呢個 comment 會第一個唔再成立而平台偵測唔到」。**四次被自己嘅守門捉到**:①boundary test 個**正面半邊**揭穿我用 `getOwnPropertyNames(prototype)` 讀 abstract 方法 —— **TS abstract 方法冇 runtime 存在**,個 array 淨係 `["constructor"]` ⇒ negative assertion **永遠綠** ②`resolve()` 參數係 typed `ConnectorKey` ⇒ 揭穿 F2/F3 條線畫喺一個**唔存在嘅接縫**上(R3 changelog)③leak test fixture 係**手抄清單** ⇒ 加守門令覆蓋面由 `CONNECTOR_CONFIG` derive,**揭穿 4 個從來未驗過嘅 key**(含 `SERVICENOW_USER` 呢個 basic-auth 帳號)④fails-before 證實 **TS 完全唔投訴 `list()` 少一個 row** ⇒ W39 個病確實可以完全靜靜發生。🚧 **真切換仍然零 live 驗證**(2004 secret 仍 `CHANGE_ME_SHARED_SECRET` · patchUrl **hardcode DEV host** · credential `n8napiservice1` **row-level ACL** —— 呢個失敗**唔會**令 webhook 報錯,2004 用 `neverError:true` 照返 200)⇒ runbook 08 新增 **§6**(其他兩個 seam 嘅 n8n 側前置 + **§6.3 切之前必做:n8n sticky 寫死 RITM 分工**) | 🔴 **庚必須正面拍板一個 W38 揪出嘅真不對稱**:同一個 replay,**Graph 講 `assigned` 而 n8n 講 `already_assigned`** —— Graph 個 POST 冪等而且唔報告,呢個實作**根本分唔到**。兩條路:caller 一視同仁(今日就係咁 —— replay 唔當錯),或者 `GraphLicenseProvider` 每次 assign 前先探一次 user licenses(**每單多一個 round-trip**)。**唔准喺 Graph 側靜靜加個 probe 當修好咗** —— W38 已喺 `assign.service` 對非 `assigned` outcome 寫咗 fail-loud,令庚唔可能靜靜落地 · 庚 = 2002 mode 1 唯讀故**可 probe**(n8n 首次有嘢探得);2003/2004 **永不探** · 辛 = close/WIP 係**新行為**(非純 refactor),要同 1007 寫死分工邊界 · ⚠️ 走 n8n 時 audit 降級為「委派 + 結果」(ADR-0017 D6 已聲明) | ADR-0017 **D2/D3/D5/D6** · ADR-0013(Model C)· ADR-0010(`PROBEABLE`) |
| Request-edit-more | **CH-007 明列延後項**:改 handler `handledById`(D7)· 改 REQUESTED 行 quantity · 把 intake 後補行 push 返上 ServiceNow（解封 platform-created 加行）| 候選 | 前置:各自獨立;push-上-SN 需 provider write + 對 SN schema 拍板(≈ ADR-0008 延伸）| `CH-007-request-edit/spec.md §3.3` |
| Drift-resolve | **Drift Alerts Resolve + per-OpCo 對回**（drift 逐條 resolve + 差異落到邊個 OpCo;DESIGN §5 對帳方案甲 + §10 對回機制）—— W12 honest gap | 候選 | 前置:**DESIGN §10「對回機制」明確 deferred·待設計** + endpoint | `W12-fe-fidelity-harden/AUDIT.md` · `docs/02-architecture/licenseops/DESIGN.md §5 · §10` |
| AI-Assist | **Request AI 協助**（DESIGN §11 Roadmap「AI 抽 free-text remark 成結構化 license 清單」;入口 = §6 `rawRequestText`;項目「逐步引入 AI」嘅首個落點）—— W12 honest gap | 候選（scope 待 owner 確認優先級） | 前置:DESIGN §11 範圍具體化 + owner 拍板 | `docs/02-architecture/licenseops/DESIGN.md §11 · §6` |
| DEPLOY-prep | **Azure UAT 部署準備**（W32:topology ADR + 文件集 + Dockerfile + RCI PAR 輸入 pack;**不含實際 provision**） | ✅ **完成**（2026-07-22,**W32**;ADR-0012 = **ACA 單一 origin** + PostgreSQL Flexible + Key Vault,Redis 暫緩[BullMQ 未 wired],**H1/H2 解鎖**,dev 仍用 compose;`docs/13-deployment/` 6 份文件 + `apps/{api,web}/Dockerfile` + `nginx.conf.template` + root `.dockerignore` + `prisma:deploy` script;零 `src/` 改動;env reference 逐個對返 code getOrThrow;**G-BUILD 🚧 blocked** = 公司 proxy 擋 Docker Hub CDN 503[同 R1 類],改用 `az acr build`） | **D1 topology = ACA 單一 origin**(對 SSO Bearer 同源零 CORS + break-glass cookie 都啱)· **D2 auth = dev 帳密 / Azure Entra SSO**(dual-provider,owner 2026-07-22 釐清)+ break-glass 本地 admin · **D3 = 納入 RCI PAR** | `docs/13-deployment/` · `docs/adr/0012-azure-uat-deployment-topology.md` · `W32-deploy-uat/` |
| DEPLOY-exec | **Azure UAT 實際部署**（W33:provision + `az acr build` + deploy + migrate + seed + smoke） | ✅ **成功上線**（2026-07-22,**W33**;rcitest sub · RG-RCITest-RAPO-N8N · eastasia;ACA `ca-uop-web`[external 單一 origin]+`ca-uop-api`[internal]+`psql-uop-uat`+LAW+KV+ACR;**smoke PASS** break-glass `admin@uop.local` 登入 200 ADMIN;全走 management plane[proxy 擋所有 data-plane]+ 手寫 ARM[bicep CLI 裝唔到]+ 自 migrate/seed[operator 連唔到 DB];4 個環境 debug fix 見 progress） | **D1 break-glass 先行** · **D2 Go provision** · **D3 placeholder 憑證**（SP 建唔到 app reg）| `docs/01-planning/W33-deploy-exec/` |
| DEPLOY-harden | **UAT hardening + SSO + 真整合**（W33 deferred）:SSO[待 IT app reg]· 真 Graph/ServiceNow · KV wiring / private Postgres / Managed Identity · 改 admin 初始密碼 | 候選 | 前置:IT app registration（SSO）· 真 UAT 憑證 · 部分 hardening 需唔受限網路或 Azure 側做 | `docs/13-deployment/06-prod-hardening-checklist.md` · `W33-deploy-exec/progress.md` |
| **DATA-INIT** | **License assets 生產數據初始化**（W35,統籌四個缺口:**F1** 端到端 runbook `docs/05-usage/DATA-INITIALISATION.md`[現只有 D365 一份 partial,`W33 plan.md:37` 明列「❌ 真數 curation」]· **F2** CSV 範本下載 + upload UI 格式說明[全 repo 零 download 實作,格式只活在 ADR-0004/DTO 註解]· **F3 🔴** `assignedQuantity` go-live baseline 批量機制[import **明確拒絕**寫 assigned + PATCH 逐格 ⇒ 23×37≈851 格無路徑;唔填則首次 reconcile **每個 SKU 爆 drift**]· **F4 🔴** `POST /license/ledger` 建空 row?[現只有 import upsert / assign upsert,純手動開格做唔到]） | 🚧 **F1+F2+F3+F4 全部完成,只剩 G3**（2026-07-25;**F1** runbook `docs/05-usage/DATA-INITIALISATION.md`[7 步]· **F2** CSV 範本下載 + 格式說明[web 136→151 test]· **F3** baseline ops script[**ADR-0014**;api 367→**390** test;dev dry-run + scratch DB commit 真驗]· **F4** = **defer → DD-3**） | ✅ **完成 · closed**(2026-07-25;G1–G7 達標;**ADR-0014**;api 367→**390** · web 136→**151**,兩邊 lint 0;F1 runbook 七步[scratch DB 真走步 1]· F2 動態範本 + 格式說明[真撳 Download → 真檔 BOM `EF BB BF` → 原封 POST → `changes:0`]· F3 baseline ops script[dev dry-run ×2 + scratch DB commit ×2,DB 實查 `allocatedQuantity` 全 0]· F4 = **defer → DD-3**;G3 用 **Playwright MCP** 驗 light+dark[`claude-in-chrome` 三次連唔上];anti-patterns skill 掃過) | ⚠️ **carry-over(3 項)**:①**G5 只達機制層** —— 「drift 清零」終局 + runbook 步 2/6 需真 Graph 憑證 → **`DEPLOY-harden`** ②**DD-3**(F4 defer;解封 = `Drift-resolve` 或 OpCo self-service,屆時 H1 需 ADR)③**design-system §0.1 px 契約 vs repo idiom 張力**(建議獨立 change 由 owner 拍板)· 🟢 `OQ-W35-2` 收為 moot · 🔴 **升級訊號**:若需**重複**批量 assigned 更新 → 新 ADR 升級去 bulk endpoint,唔好擴 script | `W35-data-initialisation/`(retro)· **`docs/adr/0014-*`** · `docs/05-usage/DATA-INITIALISATION.md` · **DD-3** | `W35-data-initialisation/` · **`docs/adr/0014-*`** · `docs/05-usage/DATA-INITIALISATION.md` · **DD-3** · `DESIGN.md §5` |

---

## B — 已設計 / Accepted,用戶主動暫緩(等 driver,非技術阻塞)

| ID | 任務 | 狀態 | 解封條件 | 來源 |
|---|---|---|---|---|
| MOD-C | Module C：SKU Catalog 字典 + 總量層對帳 / drift | ✅ 完成（W02,2026-07-09） | — | `W02-catalog-reconcile/` |
| MOD-D | Module D：Request 履行 —— **D-1 生命週期 ✅（W03）+ D-2 履行動作 ✅（W04）= 全完** | ✅ 完成 | — | `W03-request-lifecycle/` · `W04-assign-fulfilment/` |
| AUTH-1 | **後端 Entra JWT 驗證 + role guard**（`@Roles(ADMIN,REGIONAL)` 落 license/fulfilment,關 unguarded gap;dev-bypass 本地） | ✅ **完成**（2026-07-10,W09;ADR-0002;api 56 test;401/200 wiring live 驗） | — | `W09-auth-backend-guards/` · `docs/adr/0002-entra-jwt-validation.md` |
| AUTH-2a | **前端 SSO scaffold + Login/Settings + token attach**（MSAL provider + `authHeader` acquireTokenSilent→Bearer + Login/Settings 畫面 + 真 identity/sign-out + dev-bypass 相容） | ✅ **完成**（2026-07-10,W10;ADR-0003;8 deliverable + G1-G6/G8 過;web 8 test 綠[含 authHeader 6 分支]） | — | `W10-auth-fe-sso/` · `docs/adr/0003-msal-frontend-sso.md` |
| AUTH-2b | **真 SSO e2e 驗證**（真 sign-in → token → API 200 → identity → sign-out,G7） | 🔴 **blocked on IT app reg**（見 C 區;**readiness prep ✅ 2026-07-15** — 前端 wiring 驗就緒[web build+85 test 綠]、新 `apps/web/.env.example`、`AUTH-2b-RUNBOOK.md`[IT handoff + config + G7 清單];值到約 10 分鐘跑完） | **前置:IT 開 SPA app registration** → 照 `AUTH-2b-RUNBOOK.md` Part A 交 IT → 填 env(Part B)→ live 驗 G7(Part C)→ close W10 | `W10-auth-fe-sso/AUTH-2b-RUNBOOK.md` · `plan.md §7` |
| AUTH-3a | **OPCO_IT per-OpCo scope 後端強制 + /me**（fulfilment read scope filter + 5 write 面 fail-closed 403 + /me + license GET 放行 OPCO_IT + dev run-as `AUTH_DEV_USER_EMAIL` + seed OPCO_IT） | ✅ **完成**（2026-07-10,W11;純 query-layer 無 schema 改[H1 不觸發],無 ADR[OD-A];api 56→81 test;G1-G8 過;live 對照驗 OPCO_IT 見 1 vs ADMIN 見 7） | — | `W11-auth-opco-scope/` · `auth/opco-scope.ts` |
| AUTH-3b | **前端真 role scope**（consume /me 真 role/opcoScope + 移除假 `role` toggle + "My queue" 解封 + Overview/Requests 隨 role scope + 真 OPCO_IT SSO e2e） | ✅ **完成**（2026-07-14,W22;G1-G10 全 pass;**純前端 display/gating + identity-hook,無 schema/dep/ADR**;useMe `GET /me` SSOT + local profile initialData · 退 `store/ui.ts` 假 toggle · `roleScopeLabel` subtitle · role badge[menu/sidebar/settings] · `canSeePlatform`/`canSeeAdminNav` proactive gate · "My queue"[handledById===me.id];web 48→63 test;live **ADMIN vs OPCO_IT 對照**[subtitle/admin nav/Platform switcher/My queue] + backend RHK scoping 仍 work） | 真 OPCO_IT **SSO** e2e 隨 AUTH-2b(🔴 IT app reg) | `W22-auth-frontend-role/` |
| AUTH-4b | **本地 user 管理**（admin 建/列/改/停用本地 user + 設 role/opcoScope + Settings › Users&roles UI 取代 coming-soon stub） | ✅ **完成**（2026-07-13,W19;G1–G8 全 pass;決策:admin 打初始密碼 · 表涵蓋 local+SSO · 停用非刪除 · @Roles(ADMIN) · 安全閘;api 121 · web 35 test;live 端到端） | — | `W19-auth-user-admin/` |
| AUTH-4c-A | **密碼自助管理**（改自己密碼 + admin 重設 + 嚴格 policy[min12/≥3類/≠email/新≠舊] + force-change-on-first-login + lockout per-account 5次/15min） | ✅ **完成**（2026-07-13,W20;G1–G10 全 pass;**ADR-0006**[H1 additive schema,無新 dep];api 121→140 · web 35→40 test;live curl 端到端[create mustChange · weak 400 · force-change gate 403 · change 204→解封 · admin-reset→再鎖 · lockout 5次→正確密碼都401] + FE browser[temp→/change-password gate→改→app · Settings password section · admin Must-change badge + local reset 欄 · dark token]) | — | `W20-auth-password-lifecycle/` |
| AUTH-4c-B | **Session hardening**（refresh token + httpOnly cookie:localStorage→cookie + rotation + guard 讀 cookie + CSRF SameSite + POST /auth/refresh·logout） | ✅ **完成**（2026-07-14,W21;G1-G10 全 pass;**ADR-0006 §7**[H1 RefreshToken schema / H2 cookie-parser dep / H4 cookie·refresh];access 15min+refresh 7d rotating;guard cookie-first[local]/Entra header 不變;api 140→157 · web 40→48;live curl cookie 端到端[set httpOnly · rotate 防重放 · logout revoke · force-change gate cookie]+ browser[vite proxy Set-Cookie 轉發 · UI 入 app storageHasJwt=false · sign-out 401]） | — | `W21-auth-session-hardening/` · ADR-0006 §7 |
| AUTH-4c-C | **Email self-service reset**（忘密碼 → email reset link/token） | 🔴 **deferred**（卡 IT 授 Graph `Mail.Send` app permission[同 AUTH-2b app-reg]或改 SMTP[新 dep];**admin-reset[4c-A]已 cover 忘密碼**故非必需） | email transport 決定（Graph Mail.Send / SMTP）+ IT 授權後開 | ADR-0006 §分階段 4c-C |
| FE | LicenseOps 前端（`apps/web`；React+TS+Tailwind+shadcn；滾動 build order：app shell→theme→Overview→License Assets→Requests→Request detail→Drift→Catalog→Settings→Login） | 已設計（hifi handoff + 設計系統就緒；ADR-0001 已定 in-repo；H6 已生效） | 前置 `apps/web` scaffold（W01 monorepo 之後）；每段一滾動 phase | `docs/02-architecture/design-system.md` · `design_handoff_licenseops/` |

---

## C — Blocked on 外部(IT / 用戶決定 / 第三方)

| ID | 任務 | 狀態 | Blocker | 來源 |
|---|---|---|---|---|
| AUTH-2b | **真 SSO e2e 驗證**（AUTH-2a 已交前端全部;剩真 token round-trip G7） | 🔴 blocked（**readiness prep ✅ 2026-07-15**:wiring 驗就緒 + `.env.example` + runbook） | **IT 未開 SPA app registration**（redirect URI + Expose an API scope + audience 對齊 ADR-0002）。`AUTH-2b-RUNBOOK.md` Part A 可直接交 IT;回傳 5 值 → 填 env(Part B)→ live 驗 G7(Part C) | `W10-auth-fe-sso/AUTH-2b-RUNBOOK.md`（IT handoff + config + G7 清單) |
| **INTEG-2** | **n8n 回程 webhook**（外部可推 stage:n8n 跑到中途回寫「批咗 / quote 好咗 / 完成咗」;現時 m2m 面**只有** inbound intake 一條,stage 推進只能靠平台 UI 人手撳）—— rollout item 5 | 🔴 **blocked on 外部合約** | **前置:同 n8n owner 對真合約**(現 `n8n-workflow.provider.ts:22-28` 自認 URL/fields/auth 係 REPRESENTATIVE)。唔對齊就做 = 又造多一層 representative。**性質同 AUTH-2b 卡 IT app reg 一樣**。另 **H1 觸發** — stage 掛 line item + stage 推進係 locked 決策,開放外部推 stage = 改寫入來源 | `audit-and-integration-observability.md §2.4 · §7.1` · ADR-0008 |

---

## D — 已實證 defer(不重開,除非新 driver)

> 細節 + 恢復條件全部喺 `DEFERRED_REGISTER.md`,本表只做指標。

| DD | 類別 | 狀態 |
|---|---|---|
| DD-1 | Prepaid `allocatedQuantity` Excel import 方式未定 → 卡 BE-ledger-read / FE-Assets | ✅ **Close**（2026-07-13,W13;admin CSV upload ADR-0004 建成;殘留生產 curation = deploy ops step） |
| DD-2 | npm dev/build-chain vulns 需 breaking major 先清（全 dev-only 唔入 production） | defer（等 vite@8 生態 stabilize + 專門升級 phase） |
| DD-3 | **「憑空建一個 ledger row」冇路徑**（冇 `POST /license/ledger`;⚠️ OPCO_IT **改得但 create 唔到**）→ drift 對回撞 0-assigned 組合會 404。**⬆️ CH-008 之後更浮面**(2026-07-27):0/0 格預設隱藏,搵返佢**只得 toggle 一條路**;真係想「開返一格」仍然無門 | defer（Chris 2026-07-25,W35 F4;解封 = `Drift-resolve` 動工 **或** OpCo self-service 開放,屆時 H1 需 ADR） |

---

## E — 持續性技術債(對應模組再動時順帶補)

| ID | 類別 | 解封條件 |
|---|---|---|
| **TD-1** | 🟡 **`/audit` 篩選 option 落後 backend**(`apps/web/src/lib/audit.ts` 手抄 mirror,OD2-A 無 generated client):缺 `outbound.retry` / `outbound.abandon` / `connector.config_update` 三個 action + `OutboundFailure` / `ConnectorConfig` 兩個 target type —— **W31 + W34 加 audit 事件時冇補前端 option**,所以 ADMIN 喺 UI **篩唔到**呢五樣(row 仍然睇得見,只係要自己 scroll)。發現於 **W36**(2026-07-27,加 `assign.budget_override` 時);W36 **只補自己嗰兩個**,冇順手改(§1.3 surgical)。⚠️ 原有嘅 `toHaveLength` 守門**捉唔到呢類漏**(對死數字唔對 backend),已改名 + 註明「唔代表完整」 | 下次動 `/audit` 頁 或 Chris 要求時一次補齊;真正解法係令兩邊唔使手抄(= 重開 OD2-A generated-client 決定,**唔喺技術債裡面偷偷做**) |

---

## F — 明確 out-of-scope(記錄防混入)

開以下任何一項前必須 STOP + approval + 平台級 ADR(H1/H3):

- **其他 IT ops 模組**:offboarding / license 回收、Cost Insights、**D365 業務模組(F&O 工作流)**、其他 support 工作流 —— 未來 tier(`docs/architecture.md §11`)。⚠️ **D365 license** 已由 **ADR-0008** 納入 in-scope(≠ D365 業務模組)。
- **LicenseOps 排除項**:ticket 申請表單 / 審批鏈 / SLA 管理 / service catalog / CMDB 當 source of truth / 成本發票金額(→ DocuWare,只記 `quoteRef`/`poRef`)。⚠️「非 onboarding 的獨立 license request」已由 **ADR-0008** 納入 in-scope(見 `DESIGN §2`)。

---

## 維護規則(對應 CLAUDE.md §10 R7 / PROCESS.md R7)

1. **新 candidate 被識別**(分析 / 討論 / ADR Accept / phase retro carry-over / 用戶提出)→ 加一行,狀態 `候選`,填來源連結。
2. **phase kickoff**(plan 建)→ 對應項改 `進行中`(或新增做 `已規劃`)。
3. **phase closeout** → 對應項改 `完成`;若產生**反覆 / 結構性** deferral → 同步 `DEFERRED_REGISTER.md` 加 DD-N,本表 D/E 區加指標行。
4. **defer / blocked 決定** → 改對應狀態 + link DD-N 或阻塞源。
5. **single source 原則** — 本表唔複製細節,只 link;細節改去 source-of-truth,本表只更新「狀態 + 一句摘要」。
6. **更新即改「最後更新」日期** + 必要時喺對應 phase `progress.md` Day-N entry mention(per R2)。

### 🔴 7. Merge 衝突:**逐行 resolve,永遠唔好 `--ours` / `--theirs`**

本檔係**並行 session 必撞**嘅共享檔 —— 每個 session 收 phase 都要改「最後更新」同自己嗰行,而各人喺獨立 worktree、基於唔同時點嘅 `main`。撞係常態,唔係意外。

**危險喺於:衝突段通常係一大舊連續 table rows,兩邊各自有對方冇嘅行。** 揀任何一邊 = **靜靜刪走對方整行 record,零警告**。

Resolve 規矩:

1. **逐 row 睇,唔好逐 hunk 睇。** 對每個 row ID 問「邊邊嘅狀態較新」——「✅ 完成」永遠贏「🟢 待開 phase」。
2. **一邊獨有嘅 row 一律保留**(通常係基於較新 `main` 嗰邊先有)。
3. **「最後更新」多數唔會撞**(因為好多人收 phase 時漏咗改)—— 但要**人手確認兩邊嘅 phase 摘要都喺入面**,唔好靠 auto-merge。
4. **`git merge-tree --write-tree <a> <b>` 可以喺唔郁 working tree 嘅情況下預覽衝突**,resolve 之前先睇清楚規模。
5. 若同時 auto-merge 咗 **code**(尤其 DI module / barrel file)—— 文字合得埋 ≠ 語意啱,**merge 完要跑 test**。

> 實例見本檔頂部 2026-07-27 嗰段(6 行 vs 3 行,揀 `theirs` 會刪走三行 N8N-SEAMS record)。

---

> **與 `DEFERRED_REGISTER.md` 分工**:BACKLOG = **全部** pending 嘅 dashboard(含可開工 / 候選 / blocked);DEFERRED_REGISTER = **recurring deferred-debt** 嘅 close 條件細節庫。BACKLOG 嘅 D/E 區只指向 DEFERRED_REGISTER,唔重複內容。
