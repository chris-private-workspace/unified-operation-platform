# Unified Operation Platform — Architecture Decision Records (ADRs)

> Format per `CLAUDE.md §6` + `0000-TEMPLATE.md`。Filename pattern:`NNNN-short-kebab-title.md`（NNNN = 4-digit zero-padded sequential）。

## 咩情況要寫 ADR
任何觸發 `CLAUDE.md §5` hard constraint（架構變更 / vendor 換 / storage layout / scope 邊界）嘅改動,喺用戶 approve 之後 **必寫一份**。ADR = 架構紅線嘅「解鎖記錄」。

## 慣例
- 一旦 `Accepted` **唔改內容**;要推翻 → 寫新 ADR 標 `Superseded by ADR-MMMM`,舊嗰個 status 改埋。
- Status:`Proposed`(起草,未拍板)→ `Accepted`(owner 拍板)→ `Superseded`。
- 每寫一份 → 喺下面 index 加一行。

## Index

| ADR | Title | Status | Date | Source |
|---|---|---|---|---|
| [0001](./0001-frontend-in-repo-monorepo.md) | 前端納入本 repo,採 monorepo(`apps/api` + `apps/web`) | Accepted | 2026-07-09 | 用戶 approval(H1/H3);design handoff 入 repo |
| [0002](./0002-entra-jwt-validation.md) | 後端 Entra ID JWT 驗證策略(`jwks-rsa`+`jsonwebtoken` · 全域 guard · dev-bypass) | Accepted | 2026-07-10 | 用戶 approval(H2);W09 AUTH-1 |
| [0003](./0003-msal-frontend-sso.md) | 前端 Entra ID SSO 策略(MSAL `@azure/msal-browser`+`@azure/msal-react` · auth code PKCE · redirect · dev-bypass 相容) | **Superseded by ADR-0028** | 2026-07-10 | 用戶 approval(H2);W10 AUTH-2。**2026-08-07 推翻** —— 本 ADR 對 app registration 有三個硬需求(SPA platform / Application ID URI / delegated scope),W44 實測 infra 交嘅 registration 三樣都冇;MSAL 已由 `apps/web` 移除 |
| [0004](./0004-allocation-import-mechanism.md) | Allocation import 機制(admin CSV upload + dry-run + `businessAlias` 對映 + curation-as-scope + allocatedQuantity-only) | Accepted | 2026-07-13 | 用戶 approval(H1/R5);W13 allocation-import。**適用範圍隨 ADR-0008 擴至含 D365 SKU curation** |
| [0005](./0005-local-password-auth.md) | 本地密碼認證,與 Entra SSO 並存(dual-provider AppUser · argon2 · 本地簽發 JWT · dual-issuer guard;分階段 AUTH-4a/b/c) | Accepted | 2026-07-13 | 用戶 approval(H1/H2/H4);AUTH-4a(W18)起 |
| [0006](./0006-password-lifecycle-session-hardening.md) | AUTH-4c 密碼生命週期 + session hardening(嚴格 policy · force-change · lockout · admin-reset · refresh + httpOnly cookie;分階段 4c-A/4c-B/4c-C) | Accepted | 2026-07-13 | 用戶 approval(H1/H4);AUTH-4c-A(W20)起 |
| [0007](./0007-opco-ledger-manual-management.md) | OpCo ledger 手動管理(逐格校正 allocated/assigned · `PATCH /license/ledger/:id` · `LedgerAdjustment` audit · 對回機制啟動 · assigned 語意擴展) | Accepted | 2026-07-14 | 用戶 approval(H1);W23-assets-manual-ledger |
| [0008](./0008-request-creation-n8n-d365-scope.md) | 獨立 license request 建單 + n8n 雙向整合啟用 + D365 完整納入 scope(thin create-ticket · inbound intake API · `RequestSubmissionProvider`[direct/n8n] · sc_request/sc_req_item · D365 catalog/ledger/對帳) | Accepted | 2026-07-15 | 用戶 approval(H1+H2+H3);rollout W24+(甲 inbound 起)。**write-integration 抽象適用範圍隨 ADR-0017 擴至 license 操作 + ticket 狀態兩個接縫** |
| [0010](./0010-integration-observability-delivery.md) | 整合可觀測性 + 交付保證(connector 狀態三態 + 唯讀 test connection · 絕不回傳 secret / masked · 最後成功時間由既有 domain timestamp 派生 · n8n 回程只可經 stage machine · outbound retry 路線) | Accepted | 2026-07-21 | 用戶 approval(H1/H4);Chris 拍板 **OQ-A = 容許唯讀主動探針** · **OQ-B = 派生既有 timestamp(零新 schema)** · **OQ-C = 先做人手 retry(唔啟用 BullMQ)**;OQ-D(n8n 合約會)仍 open → **item 4 = INTEG-1 解封**;前置分析 `02-architecture/audit-and-integration-observability.md` §6 item 4-6 |
| [0011](./0011-outbound-delivery-failure-recovery.md) | Outbound 交付失敗持久化 + 人手補救(新 `OutboundFailure` model · **三種失敗 F1 submit / F2 orphan mirror / F3 work-note 各自唔同補救** · 🔴 `request.mirror` 絕不重新提交 · 白名單 payload 絕不存 secret · failure 寫入**唔**綁主 transaction) | Accepted | 2026-07-21 | 用戶 approval(H1);Chris 拍板 **Q1 = F1+F2+F3 三種都做** · **Q2 = ADMIN + REGIONAL**;係 **ADR-0010 D8** 嘅埋身落實,並校正「解 RISK R3」錯引用 |
| [0009](./0009-platform-audit-trail.md) | 平台 audit trail(通用 `AuditLog` 與 `RequestEvent`/`LedgerAdjustment` **共存** · 白名單 before/after · 權限矩陣 code-derive 不起 permission table · 平台 vs ServiceNow audit 分工) | Accepted | 2026-07-20 | 用戶 approval(H1);Chris 提 audit 需求 → 拍板 **OQ-1 = 記白名單 before/after** · **OQ-2 = P-B(白名單可含 PII)**;前置分析 `02-architecture/audit-and-integration-observability.md`;rollout AUDIT-2(權限矩陣)起 |
| [0012](./0012-azure-uat-deployment-topology.md) | Azure UAT 部署 topology(**Container Apps 單一 origin** + managed PostgreSQL Flexible + Key Vault · Redis 暫緩[BullMQ 未 wired] · 決定性理由 = `SameSite=Strict` cookie 跨 origin 唔帶 → 必單一 origin · dev 仍用 Docker Compose,只 supersede「雲部署都用 compose」) | Accepted | 2026-07-22 | 用戶 approval(H1/H2);W32 kickoff 三問拍板 **D1 topology = ACA 單一 origin** · **D2 auth = dev 帳密 / Azure Entra SSO(dual-provider ADR-0005)+ break-glass 本地 admin** · **D3 = 納入 RCI PAR**;部署目標 = Ricoh RCI |
| [0014](./0014-assigned-baseline-initialisation.md) | `assignedQuantity` go-live baseline 初始化(**一次性 ops script** `prisma/init-assigned-baseline.ts` · dry-run 先行 · 只寫 assigned 鏡像反向 invariant · audit 走 `LedgerAdjustment` · **唔擴 allocation-import、唔加 bulk API、唔修訂 ADR-0004 invariant**) | Accepted | 2026-07-25 | 用戶 approval(H1/H5);W35 grounding 揭 baseline 無批量路徑 → 唔填則首次 reconcile 每個 SKU 爆 drift;Chris 拍板 **F3 = 選項 C**(否決 A 擴 import[違 ADR-0004 #5]/ B 永久 bulk API[過早,保留為升級路徑]/ D 逐格手填[851 格]) |
| [0015](./0015-sync-gate-scheduled-sweep.md) | Phase 1 sync gate 由「宣稱」升級為「平台證實」——排程 sync sweep(**全 repo 第一個 `@Cron`** · `azureSyncedAt` 語意升級但**零 schema** · 人手 `PATCH /sync` 保留做 break-glass 並喺 `RequestEvent.message` 分辨 · domain-driven 故不抵觸 ADR-0010 禁 `@Cron` 探針) | Accepted | 2026-07-26 | Chris 2026-07-26 提「問題 2」前半(排程檢查 sync);架構早已預留(`app.module.ts:15` 註解 + `architecture.md §3`);對應 RISK **R3** |
| [0016](./0016-opco-budget-assign-gate.md) | OpCo 預算(`allocatedQuantity`)成為 assign 硬 gate,ADMIN 可具名 override(**只擋「再抽多一格」`assigned+1 > allocated`** · `OPCO_IT`/`REGIONAL` 無 override · 零 schema · 🔴 **`reconcile` 一個字都唔改**,只改 allocated 嘅「純顯示」性質而非「不參與 drift」性質) | Accepted | 2026-07-26 | Chris 2026-07-26 提「問題 2」後半;拍板 **既有 22 行超支 = 只擋進一步惡化**(否決 grandfather / 先修數據)· **硬擋 + ADMIN override**(否決只警告 / 無 override) |
| [0017](./0017-n8n-execution-seams-switchable-integration.md) | n8n 執行接縫化(**三接縫逐個可切換** — ①outbound 建單[既有] ②`LicenseOperationsProvider` graph\|n8n[2002/2003/2005] ④`TicketUpdateProvider` direct\|n8n[2004] · ③inbound intake **必然不可切換**但加 adapter 解合約落差 · 🔴 **D0 只換執行器唔換決策者**,所有 gate/ledger/audit 留平台 · RITM 分工:1007 管 AD 類、平台管 license 類) | Accepted | 2026-07-26 | Chris 2026-07-26 交 n8n phase 1+2 全部 workflow 要求規劃可轉換連接架構;拍板 **Q1 = 逐接縫 3 個掣** · **Q2 = 平台加 adapter endpoint** · **Q3 = `getRecordByNumber` 反查 REQ sysId(零 schema)** · **Q4 = 擴至 close/WIP**;rollout 戊(intake adapter)/己(Graph provider 純重構)/庚(n8n license)/辛(ticket) |
| [0013](./0013-connector-config-ui-management.md) | 整合 connector 非機密配置經 UI 管理(**Model C** — 非機密欄[tenant/client id · instance URL · default table · provider 路由 · webhook URL]落新 `ConnectorConfig` model 可 UI 改 · **真 secret 仍只經 env、唔落 DB/API/audit** · config resolver DB-then-env fallback · ADMIN-only + audit · **擴展 ADR-0010 item 4,唔 supersede D2**)| Accepted | 2026-07-22 | 用戶 approval(H1);owner 揀 Model C(拆 A 全 DB 加密 / B KV 代理 / C);拍板 **OQ-1 = C2 restart** · **OQ-2 = `SERVICENOW_USER` env-only** · **OQ-3 = Graph+SN+n8n outbound 三 connector** · **OQ-4 = 一 row 一 connector 結構化**;rollout W34 |
| [0018](./0018-servicenow-catalog-task-closure.md) | ServiceNow 履行完成改為 **close catalog task(`sc_task`)**,RITM 交返 SN 自己嘅 workflow 推(**修改 ADR-0017 D3 嘅寫入對象,其餘不 supersede** · close/hold 兩個 transition 都落 task · 選 task = **唯一 `active=true` 嗰張**,0 或 ≥2 → **fail-closed 唔 PATCH** · close 寫 `state=3` · 零 schema · 🔴 **2004 未同步改之前 `n8n-ticket` 掣鎖死 `direct`**) | Accepted | 2026-07-29 | Chris 2026-07-29 指出「RITM 要 catalog task 完成先 close 得到」;查證證實三邊實作全部只寫 `sc_req_item`、`sc_task` 零命中。**OQ-1 = Chris 拍板「UOP 只需處理 catalog task,RITM 交 SN workflow」⇒ 方案 B 成立** · **OQ-2 = 有寫權(admin 帳號 → 順帶揭 least-privilege 缺口,轉 DEPLOY-harden)** · OQ-3/4/5 由 read-only 查證自解(`state=3` 經驗值域 · **772 張 RITM 零反例支持「唯一 active」**)。落地 = **CH-010** |
| [0019](./0019-acs-email-notification-transport.md) | Email 通知傳輸採用 **Azure Communication Services**,並以此**解封 AUTH-4c-C**(單一 transport **唔起 seam**[得一個執行器]· vendor SDK 只准喺 `src/integration/email/` · 最小通知底座[typed-code template · caller 傳地址 · 失敗**沿用 ADR-0011** 唔新發明]· connector `email` **optional 絕不 `getOrThrow`** ⇒ 永遠唔會 `required` · **唔畀探**[一撳就寄真信,跟 `n8n-outbound` 先例]· 🔴 H2 新 dep `@azure/communication-email` + H1 `ConnectorConfig` additive 欄 · **D8 一次過定死 4c-C 設計**[`PasswordResetToken` 跟 `RefreshToken` 形狀 · 30min 單次 · 一律 204 抗枚舉 · 只服務 local user · 重設後撤銷全部 refresh token + 清 lockout + **唔設** `mustChangePassword`]) | Accepted | 2026-07-29 | Chris 2026-07-29 要求開 ADR 做 ACS email + 一併處理 AUTH-4c-C。**決定性理由 = 繞開 IT app-reg blocker**(ADR-0006 原本兩條路 Graph `Mail.Send` / SMTP,前者同 AUTH-2b 一樣卡 IT;ADR 有公道寫低:純以架構整潔度計 Graph 其實**優於** ACS,佢輸嘅係「而家攞唔到授權」)。Chris 三項拍板:**拆兩份**(CH-011 = transport)· **順便鋪通知底座**(AI 曾 push back over-engineering,Chris 揀咗)· **唔畀探**。**三條 OQ 全答**:**OQ-1 sender = `UnifiedOperationsPortal@rci-t.com`**(⚠️ **custom domain**,失敗模式係「ACS 收貨但唔送達 + API 照返 202」⇒ **A11 要以收件人真係收到為準**)· **OQ-2 approve 收窄**(唔建收件人推導層)· **OQ-3 接受 ops script**。落地 = **CH-011** + 後續 AUTH-4c-C phase |
| [0020](./0020-default-onboarding-sku-injection.md) | Onboarding **default SKU 注入** —— ServiceNow 零 licence RITM 時,平台自動補一行 E5 line item(🔴 **平台第一次成為 line item 嘅來源**,對 ADR-0008 D6「line item 一律鏡像 RITM」開**有界例外** · **只喺 `IntakeAdapterService`**,canonical CONTRACT 逐字不變 · 只喺**完全冇** licence 行時補,有 E3 就唔補 · default 存 `ConnectorConfig.defaultOnboardingSkuId`(**GUID only**,新 `kind:'sku'` 驗存在性)· native DTO `@ArrayMinSize` **1→0**,空 list 由非法 payload 升格為有意義訊號 · **fail-soft**:未配置照收零行 + 只 warn 唔 audit,注入成功先寫 audit) | Accepted | 2026-07-31 | Chris 2026-07-31 提出「所有 user onboarding default 需要 E5,即使 SN 冇一行 licence 相關」。四項拍板:**注入放 adapter**(否決放 canonical `IntakeService`)· **只喺完全冇 licence 行時加**(否決永遠加 / 冇 E5 就加)· **default 落 connector config**(否決 env-only)· **要驗存在性**(「如果是自行填的,一定要驗證是否真實存在」)。實證觸發:catalog 已有**兩個** E5 變體(`SPE_E5` / `Microsoft_365_E5_(no_Teams)`),`intake-adapter.service.ts:138` 註解「no-Teams 未 curated」前提已過期。🔴 **未證實前提**:假設 n8n 零 licence RITM 時會 POST 空 list,若佢根本唔 POST 則本 ADR 永不觸發(W42 OQ-2)。落地 = **W42** |
| [0022](./0022-ledger-full-reset.md) | Ledger full reset —— `assignedQuantity` 批量歸零成為常設路徑(**allocated + assigned 兩個都設 0,row 一律唔 delete** —— 刪 row 冇額外收益[`ledger-read.service.ts:33` 早已隱藏 `0/0` 行]但會 cascade 走 `LedgerAdjustment` · **獨立 endpoint/service,CH-016 一個字唔改** ⇒ 佢嗰條「write path 唔准出現 `assignedQuantity`」invariant test 原封保留 · **ADMIN only**[CH-016 係 ADMIN+REGIONAL,因為 assigned import 救唔返] · 清 assigned **逐格寫 `LedgerAdjustment`**、清 allocated 唔寫[跟 `schema.prisma:154-158` 已寫低嘅分工] · dry-run default + **打字確認 scope** · 🔴 `warning` 必須分兩句:中間態 vs **assigned 冇回頭路** · 零 schema · `reconcile` 一個字唔改) | Accepted | 2026-08-02 | Chris 2026-08-02 試用 CH-016 後指出「要嘅係把整個/所有 OpCo 記錄清走,先可以重新導入真正要管理嘅 license 記錄」。**本 ADR 由 ADR-0014 指定要寫**(`assigned-baseline.ts:14-17` 明文:「要開 repeatable bulk assigned 路徑就寫新 ADR,唔好把呢個檔養大成嗰樣」)。實況查證:全平台 `sum(alloc)=0` 但 `sum(assigned)=6049`,127 行因此仍顯示。Chris 兩項拍板:**① 清零兩個數字、row 留低**(否決 hard delete[語意最貼但零收益有代價]/ delete+audit snapshot[多一層邏輯換一個 UI 睇唔到嘅差異])· **② API + UI + ops script 三邊都要**(否決純 ops script —— 已證實唔係一次性初始化)。落地 = **CH-017** |
| [0021](./0021-user-authenticated-servicenow-request-import.md) | 由真 ServiceNow REQ 號碼導入 request —— **`IntakeService` 開第二個 caller(user-authenticated)**(🔴 兩條既有 intake route 都係 `@Public()`+`IntakeKeyGuard`,ADR-0017 D4 OQ-3 明文揀「one caller, one trust boundary, one secret to rotate」;前端唔可以持 `INTAKE_API_KEY`[H4]⇒ **無論點做都要開第二個 caller**,本 ADR 正式更新嗰個前提 · 新增 `GET /requests/servicenow-lookup`[唯讀]+ `POST /requests/import-from-servicenow`,**唔碰亦唔接受 m2m key** —— 被改嘅係「入口唯一性」唔係「secret 強度」 · 沿用 ADR-0017 D4「另開一條 route、canonical 合約一個字唔改」pattern 嘅**第三次應用** · **ADMIN only**[OpCo 要反查完先知 ⇒ 「先做外部呼叫先答到授權」嘅 gate 形狀易錯] · 🔴 **licence code 必須人手揀,平台唔猜**;絕不可把 SN 標籤塞入 `businessAlias`[屬 ADR-0004] · `ritmSysId` 一律 server 反查,唔信 client · script 唔刪,同 endpoint 共用同一份 lookup) | Accepted | 2026-07-31 | Chris 2026-07-31 要求把 PR #61 個 ops script 嘅能力搬上 UI(「只提供request號碼就可以自動抓取其他的id」)。三項拍板:**① H1 approved** · **② 定位 = 長期 admin 補救工具**(唔係「n8n 通咗就刪」—— 呢個係本 ADR 值唔值得寫嘅前提;若係即棄品,理性答案係唔做 UI 繼續用 script,故 Option D 係**被定位 reject 而唔係被質素 reject**)· **③ ADMIN only**。落地 = **CH-013** |
| [0023](./0023-bulk-catalog-curation.md) | SKU catalog 批量 curation —— **`POST /license/catalog/import`**,CH-018 export 改完傳返上去(對帳鍵 = **`SkuId` GUID exact**,絕不靠名[§13 locked;`businessAlias` 存在嘅原因就係「唔可以信名」DESIGN §93] · 只寫三個 curated 欄,`Display name`/`Part number`/`Active`/`Last synced` **照收無視** ⇒ export 原封 round-trip · **永不新建 catalog row**[identity 係 sync-owned,打錯 GUID 會生出 reconcile 永遠對唔到嘅幽靈 SKU] · dry-run default,跟 ADR-0004 #4 · 🔴 **重複 alias = 整批 400 零寫入**,範圍係成個 active catalog 唔止上傳嗰幾行,而且**同時管批量同單筆 `PATCH`**[OQ-1] · 🔴 **清空 alias 要 `confirmClears`** —— 清 alias 令 SKU 退出 import scope 但 ledger `allocatedQuantity` **凍結喺舊值**,係唯一一個後果喺 catalog 畫面睇唔到嘅改動;用 boolean 唔用 CH-017 打字確認因為**可復原** · audit 兩層 = 每 SKU `CATALOG_UPDATE`[重用] + 一條 `CATALOG_BULK_CURATE` summary,跟 ADR-0022 D4 分工 · 零新 dep 零 schema) | Accepted | 2026-08-03 | Chris 2026-08-03「現在是需要逐個修改它們的 alias，這樣會有些麻煩，可能批量修改嗎？」。實測支持:**99 個 active SKU 得 8 個有 alias / 8 個有 category ⇒ 91 個未 curate**,而唯一路徑係一次開一個 Dialog。🔴 **起草期間揭到既有 latent bug**:`businessAlias` schema 冇 unique constraint(`schema.prisma:124`),而兩個 consumer 取態**相反** —— 前端範本 first-wins(`allocation-template.ts:63-67`)、後端 import last-wins 兼 `findMany` 冇 `orderBy`(`matrix-csv.ts:86-90`)⇒ 撞 alias 會令 allocation **靜靜寫落另一個 SKU**,冇錯誤訊息冇 audit 分辨得出;今日靠「得 8 個且逐個人手打」壓住,開咗批量貼上就變日常失誤 ⇒ 本 ADR 順帶封呢個洞(實測今日 **0 個撞**,故閘門擋唔到任何今日行得通嘅嘢)。Chris 四項拍板:**① 重複 alias 整批擋**(否決「只擋撞嗰幾行」/「照寫出 warning」—— 後果係靜默,唔可以留半應用中間態)· **② 清空 alias 獨立標示 + 額外確認**(否決「當普通改動」/「完全唔准清」)· **③ UI 放 Catalog 頁 Export 隔籬** · **④ OQ-1「都做」** ⇒ 閘門一併加落單筆 PATCH。落地 = **CH-019** |
| [0024](./0024-onboarding-task-closure.md) | Onboarding catalog task closure —— UOP 存低 n8n 1001 送嚟嘅 **`sc_task` sys_id**,派完 default E5 之後直接 close 佢(🔴 **route 共用但 canonical contract 唔放寬**:`POST /requests/intake` 靠 body 有冇 `mode` 分流,冇 `mode` = 今日隻 `N8nIntakeRequestDto` 一個字唔改 ⇒ 被共用嘅係 URL 唔係 contract;否決「令 `serviceNowSysId`/`lineItems` 變 optional」—— 嗰個係 `@unique` idempotency key,放寬即係成張 contract 嘅重複保護冇埋 · `RequestLineItem` 加 `serviceNowTaskSysId`/`serviceNowTaskNumber` 兩個 nullable 欄,**放 line item 唔放 request** 因為 close 發生喺 assign 一條 line 之後 · flat 路 **重用** `resolveReqSysId()`+`applyDefaultSku()`,idempotency 仍係 `Request.serviceNowSysId`,零新 key · seam ④ target 由 bare string 升做 discriminated union `{kind:'ritm'|'task', sysId}`,`ritm` 分支逐字保留 · 🔴 **close 前必驗 `active=true`,fail closed** —— `kind:'task'` 由構造上繞過咗 ADR-0018 D3 嘅「唯一 active task」保護,而 REQ0044049 就係活例 · 失敗仍 non-fatal[ADR-0011 OD4]) | Accepted | 2026-08-03 | Chris 2026-08-03 澄清 n8n 1001 **刻意**把 Windows Domain Account 嗰張 catalog task 嘅 sys_id 送畀 UOP。實測三個洞全部確認:**收唔到**(1001 已由 nested 改 flat,canonical DTO 兩個 required 欄佢都冇送 ⇒ 今日一定 400)· **存唔到**(任何 UOP DTO 都冇呢兩個欄,`whitelist:true` 靜靜剝走)· **用唔到**(`RequestLineItem.serviceNowSysId` 一路當 RITM,`pickTask` 跑 `request_item=<id>` ⇒ 餵 task sys_id 落去永遠 0 row)。Chris 兩項拍板:**① n8n 零改動**(⇒ UOP 側 route 共用,否決「n8n 改送 RITM sys_id」—— 呢個係**被時序 reject 唔係被質素 reject**,日後 2004 更新仍係較乾淨嘅終態)· **② close 前必驗 active**。🔴 **OQ-3 硬前提**:1001 個 HTTP node 用 credential「n8n Academy API Key」,workflow JSON 睇唔到佢送咩 header;若唔係 `X-Intake-Key` 就連 401 都過唔到 —— 唔係 code 問題,但係「打通」嘅前提。落地 = **CH-020** |
| [0025](./0025-onboarding-license-request-creation.md) | Onboarding licence request creation —— UOP **另建一張 `O365 User License Maintenance Request`**,再喺派完 licence 之後閂返自己張單(🔴 **部分 supersede ADR-0024**:D2 `mode` 分流 / D3 adapter 重用 **原封保留**;D1 兩個 task 欄 **改用途做 traceability 但唔 drop column**;D6① by-task close **停用**;D4 union / D5 `active` 閘保留但**理由更新** · 走 **Service Catalog API `order_now`/cart**,順帶修 **BUG-010**(`sc_request` insert 403 table-level)並更新 ADR-0008 D3 已 lock 嘅寫入路徑 · 🔴 **`target_user` 係 mandatory reference → `sys_user`,收 sys_id 唔收 email**,而 gate ② 講緊嗰個人未同步到 SN ⇒ 建單時填 **requester** 做 placeholder、gate ② 通過時**回填**真人,`target_users_email` 由頭到尾承載「為邊個開」**任何邏輯都唔可以靠 `target_user` 認人** · **gate ② 新增** `serviceNowUserSyncedAt`/`serviceNowUserSysId` 兩個 nullable 欄,**擴既有 `SyncSweepService` 唔開第二個 `@Cron`**,兩個 gate **各自 try/catch** 免得一個 vendor 掛拖跨另一個,≥2 命中 fail-closed · assign 由單閘變**雙閘**,既有嗰行**逐字不變**,`budgetOverrideReason` **override 唔到** · close **零新 code**:1 RITM : 1 task 正正係 ADR-0018 D3 嘅形狀 · `license_type` 實測 **`mandatory=false`** ⇒ best-effort,48-choice 對照表 defer) | Accepted<br>（**D3 後半 + D4 尾條 superseded by ADR-0026**） | 2026-08-03 | Chris 2026-08-03 喺 CH-020 收官同日更正流程:「n8n 嗰邊已經會 complete 咗張 request,佢只係喺 workflow 最後傳資料畀 UOP,讓 UOP 建另一張 [O365 User License Maintenance Request]，而這張表單會在 license assign 成功之後就 close」。🔴 **ADR-0024 建基於一個錯誤前提**,而錯誤有明確成因:嗰個結論**唯一來源係 workflow JSON 嘅註釋同 sticky note**(1001 `Rule A: Phase 2 closes ONLY the WDA task` / 1007 `MODE_BY_CATEGORY.new_hire_domain_account = 'note'`),由頭到尾冇打過真 SN 去對。實測推翻:`close_notes LIKE 'Handled by n8n'` **11 行含 2 張 WDA task**(SN 側串錯字 `Windwos`,所以查 `Windows` 會 miss)· `Awaiting E5 licence` journal **0 行** ⇒ note-only 分支從未跑過 · 1007 rewrite 係 29 Jul 而 30 Jul 仍然照閂。🔴 **附帶揭到 `SERVICENOW_USER === 'n8napiservice1'` = true**(UOP 同 n8n **共用 SN 帳號**)⇒ `sys_updated_by` 永遠分唔到邊個系統做,唯一指紋係 `close_notes`;ADR-0024 **D5 個 rationale 就係咁記錯**(SCTASK0071807 唔係「被人手閂」,係 UOP 自己喺 CH-020 驗證期間閂嘅)。Chris 兩項拍板:**① `target_user` placeholder 用 requester**(否決 integration account —— 語意上睇得出邊個部門發起)· **② 即刻建單**(否決「等 gate ② 通過先建」)。落地 = **W43** |
| [0026](./0026-target-user-backfill-via-work-note.md) | `target_user` 回填改行 **work note** —— `sc_item_option` 寫入被 ACL 封死(🔴 **supersede ADR-0025 D3 後半 + D4 尾條**,其餘全部不變,尤其 D3 前半 requester placeholder 同整個 gate ② 機制 · **拆走** `updateCatalogVariable()` + sweep 嗰個 call:成功率係**已證實嘅 0%**,一段永遠 403 嘅 code 唔係 resilience 係誤導,下一個讀 code 嘅人會以為張單有自我修正 · 同一個資訊改行 `addWorkNote()`(寫 `sc_req_item`,CH-010 已證實寫得到)—— 唯一同時滿足「寫得到」「O365 Support 一定見到」「唔使新權限」嘅路 · 🔴 **`target_user` 由「回填之前係 placeholder」升級成「永遠係 placeholder」**,適用範圍**包括 ServiceNow 側**:任何靠佢嘅 SN report / assignment rule / notification 都會指向 requester,呢個係本 ADR 最大代價而且 UOP 補唔到 · work note 失敗維持 non-fatal,但今次係**真** resilience[路徑已證實可行,失敗即係真 outage]而唔係遮住必然失敗 · 攞寫權**唔做 blocking dependency**,攞到要恢復回填 = 另寫 ADR · 零 schema 零新 dep · **gate ② 行為一個字唔變**) | **Accepted** | 2026-08-04 | W43 **G7** 實測,Chris 2026-08-04 批准打一次真 PATCH(限 RITM0047366 = G6 嗰張 `[UOP TEST]`,本來就等緊人手 cancel)。行 production class、寫**同一個值**(唔改張飛任何事實,證據係 HTTP status 唔係新值)、覆核走**獨立 read 路** ⇒ `PATCH /api/now/table/sc_item_option/… -> 403 "ACL Exception Update Failed due to security constraints"`,`sys_mod_count` **0 → 0** = ServiceNow 零副作用。🔴 **呢個 instance 逐個 table 分開開權**,唔可以由「某張表寫得」推論「另一張表寫得」——`sc_request` insert 403(BUG-010)· `sc_item_option` update 403(本 ADR)· `sc_req_item`/`sc_task` update ✅(CH-010)· catalog `order_now` ✅(G6)。ADR-0025 D3 後半就係踩咗呢個推論。順帶揭到**回填/work note 冇限制只寫平台自己建嘅 RITM**(ADR-0021 import 路指住已存在嘅 SN RITM)⇒ **OQ-2**。Chris 拍板 **(ii)+(iii) 一齊做**(否決 A 攞寫權做 blocking[被時序 reject 唔係被質素 reject]/ B 保留做 best-effort / C 建單就填真人[時序上根本冇 sys_id])。落地 = **W43 批 C** |
| [0027](./0027-azure-dev-deployment-topology.md) | Azure **DEV** 部署拓撲:api ingress 對外定收返 internal(**擴充 ADR-0012 唔推翻** —— ADR-0012 定嘅係 UAT 一個環境,本 ADR 決定第二個雲環境點做 · 🔴 **D1 兩個選項並列待拍板**:**Option A 收返 internal**[推薦 —— web 個 nginx **已經** proxy `/api`,n8n 打 `https://rapo-uop-web-dev.rci-t.com/api/requests/intake` 就到,即係 external 係**多出嚟嘅暴露唔係需求**;對外攻擊面同 UAT 一樣細;一套 template 兩個環境]vs **Option B 保持 external**[infra 現狀,但**整個 API 含 `/docs/api` 直接暴露互聯網**,必須同時做 D2 三項收窄:關 `/docs/api` · IP restriction 只放行 n8n 出口 · api URL 當半公開 secret]· **D3 browser 流量永遠行 web 同源 proxy 唔議** —— cookie/CORS/前端行為**一個字唔變**,兩個選項嘅分別**只在 machine-to-machine 行邊條路**,唔涉及瀏覽器[防「api external ⇒ 前端跨 origin」呢個誤讀,即 Option D 已明確 reject]· **D4 Redis 唔接**[環境有 `redis-rapo-uop-dev` 但 `apps/api/src` 實測**零** BullMQ 用法,沿用 ADR-0012 §D4 不預批]) | **Accepted**（Chris 揀 **Option A**） | 2026-08-04 | W44 F1 discovery 實測(SP 真登入):infra team **已預先建好**兩個 container app 且 **api ingress = `external`**,同 ADR-0012 §D1「api = internal」相反 ⇒ 觸發 **H1**(部署 topology)+ **H4**(把 API 直接暴露互聯網 = 安全邊界改變)。🔴 **OQ-1 blocking D1**:n8n UAT 解析唔解析到企業 custom domain `rapo-uop-web-dev.rci-t.com`?**resolved** —— Chris 確認 n8n 住**企業內網**(on-prem / 內部 VM)⇒ 解析得到企業 domain,Option A 冇已知障礙(實測降級做 W44 F7-1)。**Chris 2026-08-04 拍板 Option A** ⇒ `aca-dev.json` 把 api ingress 改 `external:false` + `allowInsecure:true`,對外只剩 `rapo-uop-web-dev.rci-t.com` 一個 hostname,同 ADR-0012 完全同形。⚠️ infra 開嘅 `azure_url_for_api_call` 收窄後**唔再可達**,要同佢哋講(OQ-2) |
| [0028](./0028-sso-server-side-code-exchange.md) | SSO 改行 **server-side authorization code exchange**(🔴 **supersedes ADR-0003** MSAL 前端 SSO;**ADR-0002 唔推翻** —— JWKS/issuer/audience 驗證邏輯照用,只係由「每個 request 嘅 guard」移去「一個 callback endpoint」· 前端只負責把用戶送去 Entra + 交返個 `code`,**API 用 client secret 喺 server 側換 token** → 驗 `id_token` → upsert `AppUser` → 發平台自己嘅 httpOnly cookie(**完全沿用 ADR-0006 §7**)⇒ **SSO 同 break-glass 統一一套 session** · scope 只用標準 `openid profile email`,**唔需要 Application ID URI / 自訂 scope** · `state` 必驗 + PKCE 照用(confidential client 唔強制但成本近零)· 🔴 **配置由 build-time 變 runtime** —— 前端唔再需要 `VITE_ENTRA_*`(vite 編譯期烘死),client id / tenant / redirect URI 全部由 **API env** 讀 ⇒ **改配置唔使重 build image** · `AUTH_JWT_SECRET` / break-glass **完全保留**(ADR-0005 dual-provider 不變)· guard 現有 Bearer 路徑**保留唔刪**(有 test、零成本、ADR-0002 仍成立)· 可移除 `@azure/msal-browser`,bundle 細 **254 KB**) | **Accepted** | 2026-08-07 | W44 **F9 / B9** 實測:infra 交嘅 app registration `08fa14bf-…` **三樣用戶登入必需品全部冇**,全部有錯誤碼 —— `<client-id>/access_as_user` → `AADSTS65005 scope doesn't exist`(**對照組**用假 scope 得同一個錯 ⇒ 測試有區分度)· `api://<client-id>/.default` → `AADSTS500011 resource principal not found` · device code → `AADSTS7000218`(冇開 public client flows)。🔴 **而佢有嘅嘢啱啱好係另一種 flow 嘅完整配置**:client secret ✅ + redirect URI ✅ + confidential client ✅ ⇒ **同 server-side code exchange 完全匹配,同 MSAL SPA 完全唔匹配**。**三輪往返攞唔到 Application ID URI**(infra 先後答咗「web portal 網址」/「OAuth authorization endpoint」/「Application ID = client id」)—— 唔係態度問題,係個詞本身容易被讀成「應用程式嘅網址」。**先例**:同一 tenant 已上線嘅 `ai-it-project-process-management-webapp` 用 NextAuth + AzureAD provider,行嘅正正係呢條路,所以從來唔需要 Application ID URI。**Chris 2026-08-07 拍板**:「跟隨番 infra 那一邊的配置去改動本項目的 SSO auth 流程,不要再浪費時間去堅持原來的設計」 |
| [0029](./0029-assign-step-results.md) | Assign **回傳每步結果**(`{outcome, failedAt?, steps[]}`),**維持一個 atomic call**(**擴充 ADR-0016 / 0017 / 0011 唔推翻** —— 只改「gate 結果點講畀 caller 聽」,**唔改擋唔擋** · 每個失敗帶 **`retryable` + `whoFixes`**,因為「下一步搵邊個」今日完全靠人記 · 失敗一樣返完整 steps,睇得出行到第幾步先撞牆 · **`ticket: skipped`** 揭示 line item 冇 RITM ⇒ SN 側乜都冇 complete[W44 F7-12 追緊嗰個問題]· 🔴 **否決分步 endpoint**:拆開 atomic transaction 後 `execute` 成功但 `commit` 前斷線 = **licence 已派 + ledger 冇加 = DRIFT**,即 mockup 自己個 `ledger_conflict` 災難 · 🔴 **否決 SSE**(暫時):要 NestJS + nginx `proxy_buffering off` + ACA ingress 三層配合,而 DEV 部署先一個禮拜;steps 契約 lock 咗之後將來加串流只係換 transport) | **Accepted**(Chris 2026-08-10) | 2026-08-09 | Chris 2026-08-09 逐步對帳端到端流程,要求 assign 跟返 mockup 顯示過程。查證發現 mockup **真係有完整設計**(`IT Ops Platform.dc.html:1444-1450` 五步 + `:1347-1355` 七個失敗場景),而現況只係一個 toast ⇒ 觸發 **H1**(改 API 契約形狀)。🔴 **但 mockup 同實際對唔齊,而個差異啱好就係同週實測撞到嗰個**:mockup 個 `precheck` 把 **OpCo budget** 同 **tenant seat** 合併成一步,實際係兩層兩道 gate 且 budget 喺前;2026-08-07 撞嘅正正係 tenant 嗰層(`POWER_BI_PRO` `prepaidEnabled=0`)而 OpCo budget 係綠(`80/90`)—— **合併就講唔出係邊層擋住**,而兩者下一步完全唔同(叫採購買 vs 加 allocation)⇒ 照抄 mockup 會做出一個講唔出真相嘅畫面。**Chris 2026-08-09 選定方案** = 回傳每步結果。落地 = **W45** |
| [0030](./0030-requester-from-request-opened-by.md) | **Requester sys_id 改由 REQ 自己個 `opened_by` 攞**,唔再靠 n8n 送 email 去 `sys_user` 反查(**supersede ADR-0025 D1 嘅「requester 來源」一格**,D1 其餘 — `target_user` 係 placeholder、真 target 睇 `target_users_email` — 不變)· **零額外 API call / 零額外權限**:`resolveReqSysId` 行緊嘅 `getRecordByNumber` **冇落 `sysparm_fields`** ⇒ 成個 REQ record 一直喺手,`opened_by` 從來冇人攞 · **D3 email 反查保留但只服務 outbound 路**(IT 自己開單冇 REQ 可攞),payload 加 optional `requesterSysId`,🔴 **刻意唔做 fallback 鏈** —— intake 傳咗就唔准跌返去(ADR-0026 D1 同一判斷:已知 0% 嘅路留低會被下手當成 repair 機制)· **D4 唔寫 migration**,08-07 三張靠 Delivery failures `REQUEST_SUBMIT` retry 補 | **Proposed**(待拍板) | 2026-08-10 | 🔴 **端到端第 2 步由 W43 交付到今日,喺真流量下一次都冇成功過** —— 08-07 三次 n8n intake(`REQ0043934`/`REQ0044049`/`REQ0044057`)全部掛喺 `The requester was not found in ServiceNow`,SN 側 cross-check 08-07 零新 RITM。**根因唔係邊一邊寫錯,係接縫冇人對過**:n8n `1001…json:1486` 把 `requesterEmail` map 做 **Outlook trigger 個 sender**,而 ADR-0025 D1 消費佢嗰陣當咗係「ServiceNow 用戶」—— 兩個 domain 冇任何保證對得上(UOP 自己 `intake-adapter.service.ts:584` 個 comment 一早寫明佢係 Outlook trigger)。**失敗係必然唔係偶發**:實測 `REQ0043934` 個 `opened_by`(`Operator@rapo.com.hk`)用**同一條** query **命中 1 行** ⇒ 查詢方式冇壞,壞嘅係餵入去嗰個值嘅語意;而整合帳號 **`n8napiservice1` 個 `email` 欄係空** ⇒ 凡佢開嘅單永遠命中 0。⚠️ 已知代價:`target_user` 對 n8n 建嘅單會變成 `n8napiservice1`,**SN owner 要知道**呢個係設計唔係 bug。落地 = **W44 `INTAKE-REQUESTER`** |
