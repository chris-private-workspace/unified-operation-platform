# apps/web — env 範本(Vite)。Copy 做 .env / .env.local 填實際值。
# .env* 已 gitignore,唔 commit(CLAUDE.md §4.4 / §5 H4)。
# ⚠️ 只有 VITE_ 前綴變數會 bundle 入前端 → 絕不放 secret。

# ── 本地開發 dev-bypass(pairs with backend AUTH_DEV_BYPASS) ──
# true → 跳 login gate + 唔行任何登入(配後端 bypass),本地零登入照跑。
# prod build 一律唔設 / false(對應 RISK R2)。
VITE_AUTH_DEV_BYPASS=true

# ── API base(dev 由 vite proxy /api → NestJS;跨 origin 才需覆寫) ──
# VITE_API_BASE_URL=/api

# ── Entra SSO ─────────────────────────────────────────────────────
# 🔴 呢度冇嘢要填。ADR-0028 之後,SSO 配置**全部喺 API 側**
# (`apps/api/.env` 嘅 ENTRA_TENANT_ID / ENTRA_CLIENT_ID /
#  ENTRA_CLIENT_SECRET / ENTRA_REDIRECT_URI),由 API runtime 讀。
#
# 點解值得寫低:VITE_* 係 **build-time** —— vite 會烘死落 bundle,即係
# 「改個 client id」都要重 build image(~10 分鐘)。W44 就係喺呢度卡過。
# 而家前端唔知道 tenant / client id 係乜,只係問 `GET /auth/sso/status`
# 應唔應該亮個掣,再問 `GET /auth/entra/start` 攞條 URL 跳過去。
#
# ⇒ 換 app registration / 改 redirect URI = 改 API env + restart,唔使重 build。
